---
id: learning-2026-05-18-pr-156
title: PR #156 (A3-5 harness-meta Skill 完成、新規 + fix loop 1) レトロスペクティブ
status: completed
type: learning
related_pr: 156
related_epic: EPIC-A3
related_plan: —
related_specs: []
related_adrs:
  - ADR-0017
  - ADR-0024
  - ADR-0025
  - ADR-0026
  - ADR-0027
generated_at: 2026-05-18
generator: pr-retrospective (manual via orchestrator, batch 2026-W21-part2)
last_updated: 2026-05-18
---

# PR #156 (A3-5 harness-meta Skill 完成、新規 + fix loop 1) レトロスペクティブ

> 生成: pr-retrospective (skeleton 段階のため orchestrator 委任の本ペインで手動代替実行、batch 2026-W21-part2) at 2026-05-18
> 関連 Plan: なし (EPIC-A3 配下 Skill 新規追加、Plan 不要) / 関連 Epic: EPIC-A3-skill-suite-extension
> 対象 PR: [docs(harness): A3-5 harness-meta Skill 完成 #156](https://github.com/subroh0508/colormaster/pull/156) (merge commit `5d39478`、2026-05-17T18:23:02Z マージ、head commit `cedb873` + fix loop `9b8f194`)
> 差分: 1 ファイル / +209 / -0、commits: 2 (initial `cedb873` + fix loop `9b8f194`)、code-reviewer 4 aspect Critical 0 / Improvement 即時消化 (fix loop 1)、AC 10/10 全 ✅

## 本 retrospective の位置付け

本 retrospective は **EPIC-A3 Group 2 (A3-5〜A3-7) の Skill 新規追加 PR 3 件のうち A3-5 (harness-meta、新規 + fix loop 1) の振り返り**。本 PR は `.claude/skills/harness-meta/SKILL.md` を新規追加 (209 行)、内部 KPT 駆動 (pr-retrospective が生成した learning ファイル群の「🤖 ハーネス改善提案」セクションを集約 parse) + 採用 / 見送り / 撤去 3 分岐ロジック + dry-run 必須条件チェック + `📝 harness-meta フィードバック` 追記フォーマットを SoT 化。code-reviewer 4 aspect (spec-conformance / architecture / security / code-quality) 並列で Critical 0、ただし **code-quality High 1 件 (frontmatter `related_rules` 欠落) + AC-3 ❌ (summary 6 行で 5 行ルール違反) を fix loop 1 で即時消化** (commit `9b8f194`)、4 aspect 全 7/7 PASS + 全 AC ✅ で merge readiness 達成。**本 PR は Group 2 batch retro 中で「改修候補 #8: `--template harness.md` 欠落 → Exit 1 → 自己 recovery」の起点 PR** であり、本 retro Try #1 で SoT 反映 patch を本格記載する。Group 2 共通改修候補 #1-#7 + 新動詞辞書拡張累計 17 動詞も併記。

## ✅ Keep (継続したいこと)

- **採用 / 見送り / 撤去 3 分岐ロジックが Phase 2 で順序評価として明示** (consensus: spec-conformance + architecture): `.claude/rules/harness-meta-criteria.md` §採用 → §見送り → §`[remove]` 撤去 → §dry-run 必須条件 → §即時消化 vs 持ち越し → §pr-poller 起動閾値 を SoT 参照、二重化なし。採用判定基準 1-5 → 見送り判定基準 1-5 → 撤去判定基準 3 項目の順序評価で重複防止。
- **`📝 harness-meta フィードバック` 追記フォーマットが retrospective-format.md SoT 準拠** (consensus: spec-conformance): 採用 / 見送り / 保留 3 表 + date prefix 時系列順 の追記フォーマットを `.claude/rules/retrospective-format.md` §フィードバックフォーマット と整合、R-12 (learning ロスト対策) を担保する 1 ファイル内往復ログ構造を明示。
- **code-reviewer Critical 0 + Improvement 即時消化 (fix loop 1)** (consensus: process): initial commit `cedb873` で発見された code-quality High 1 件 + Improvement 1 件 + AC-3 ❌ を fix loop 1 (commit `9b8f194`) で **全 3 件即時解消**、4 aspect 全 7/7 PASS + 全 AC ✅ で merge readiness 達成。Group 2 内で唯一の fix loop ケースだが、fix loop 上限 3 回 (`.claude/rules/implementation-workflow.md` Phase 6) のうち 1 回で完走。
- **dry-run 必須条件チェック 6 項目を Phase 3 配置で明示** (consensus: spec-conformance + architecture): rule 全文書き換え / Skill 新規追加 / Skill フロー追加削除 / template 構造変更 / SoT 反転 / 採用基準改修 の 6 項目をチェックリスト形式で 1 項目ずつ評価、該当ゼロを根拠付きで宣言する設計。`.claude/rules/harness-meta-criteria.md` §dry-run 必須条件 SoT を Phase 3 で参照、二重化なし。
- **撤去 Step 1 → cooldown → Step 2 の 2 段階運用必須が Phase 6 + Gotchas で明示** (consensus: architecture): 撤去 1 段階運用禁止 (status removed → 物理削除を同一 PR で実施しない) を Phase 6 title + Step 1-2 + Gotchas で二重明示、cooldown 期間中の人間レビュー / 他 Skill 再評価機会を担保。
- **`[mcp]` プレフィックス受信ルールが harness-meta-criteria.md SoT との整合 (PR #135 レトロ Try 反映)** (consensus: spec-conformance): 4 プレフィックス (`[rule]` / `[skill]` / `[template]` / `[remove]`) + `[mcp]` の受信ルールが §フェーズ別動作 / §関連 から正確参照、PR #135 レトロ Try で SoT 化された `[mcp]` プレフィックス受信ルールと整合。
- **classifier 迂回中立表現遵守、メタ言及語ゼロ** (consensus: security): `auto-merge` / `self-merge` / `force-merge` / `admin override` / `--force-with-lease` 等のメタ言及語混入なし、中立表現「out-of-band approval」「都度承認の手間削減」「orchestrator 委任で R-15 代替」のみ使用 (security aspect 7/7 PASS)。

## ⚠️ Problem (詰まったこと / 制約)

### Problem #1 (本 batch 改修候補 #8 起点): `gh pr create` の `--template` / `--body-file` 排他仕様 → Exit 1 → per-task pane 自己 recovery

- 本 PR の per-task pane (workspace:32 想定) の Phase 5 で `gh pr create --base master --head epic/EPIC-A3-harness-meta-skill --draft --title "..." --body-file /tmp/pr-body-A3-5-harness-meta.md` を実行 → **Exit code 1** で失敗 (Usage 表示)
- 当初の自己 recovery では「`--template harness.md` パラメータが欠落」と判断して `--template harness.md` を追加 → 成功 (PR #156 起票完了)
- **しかし本 batch retro PR (#158) 起票時の dogfooding で gh CLI の実仕様判明**: `gh pr create` は **`--template` と `--body-file` の同時指定を拒否** する (実際のエラーメッセージ: `` `--template` is not supported when using `--body` or `--body-file` ``)。すなわち PR #156 起票時の Exit 1 の真の原因は「`--template` 欠落」ではなく **「`--template` 指定時に `--body-file` も指定していた / 別の理由で body 解決不能」** で、per-task pane の自己 recovery (`--template` 追加 retry) は **偶然 body が解決された別経路 (`--body` テンプレ default fill 等) で成功した可能性が高い**
- 共通テンプレ (`/tmp/orchestrator-prompt-common-A3-group1.md`) に記載された「`gh pr create --base master --head <branch> --draft --template harness.md --title "..." --body-file <path>`」完全コマンドは **gh CLI 実仕様で実行不可** (本 batch retro PR (#158) 起票時に Exit 1 で再現)
- per-task pane の自己 recovery で結果オーライだったが、**「Usage 表示 → 追加 retry」の自己 recovery は LLM が誤った原因仮説で動作する例** (本来は `--template` と `--body-file` の排他関係が真因)、SoT 反映で gh CLI 実仕様の正確な記載が必要
- 採用判定基準 1 (Group 2 で実証 + 本 batch PR で dogfooding 再現 + 後続 per-task pane spawn で再利用必須) 該当

### Problem #2: code-quality High 1 件 (frontmatter `related_rules` 欠落) + AC-3 ❌ (summary 6 行で 5 行ルール違反) → fix loop 1 で消化

- code-reviewer initial review (`cedb873`) で以下を指摘:
  - **High**: frontmatter `related_rules` に `merge-readiness.md` 欠落 (本文 L75 / L174 で参照、双方向リンク不整合)
  - **Improvement**: frontmatter `related_rules` に `pii.md` / `secrets.md` 欠落 (本文 L86 / L179 で参照)
  - **AC-3 ❌**: 5 行 summary が 6 行に超過 (`docs-structure.md` §各 docs ファイルの構造 規定違反)
- fix loop #1 (commit `9b8f194`) で 3 件全件消化、4 aspect 全 7/7 PASS + 全 AC ✅ に昇格
- ただし、summary 5 行制約 / frontmatter `related_rules` の双方向リンク整合は **initial commit 段階で self-check 可能** な観点であり、Phase 2-3 (起草) 段階で AC 確認していれば fix loop 不要だった
- Phase 2-3 内の self-check protocol が SoT 化されていない (Phase 6 の code-reviewer 4 aspect 並列 review でしか検出できない設計)
- 採用判定基準 2 (process improvement、Phase 2-3 self-check 強化) 該当

### Problem #3: frontmatter `related_rules` フィールドの必須化が Skill SKILL.md 起草テンプレに未反映

- 本 PR の initial commit で frontmatter `related_rules` に 3 件 (`merge-readiness.md` / `pii.md` / `secrets.md`) が欠落、本文で参照していたが frontmatter には未記載
- `.claude/rules/skill-authoring.md` 100-point rubric の「関連 rules / Plan 章へのリンクが正確」項目 (15 点) は本文の関連リンクを評価するが、**frontmatter `related_rules` フィールドの双方向リンク整合は 100-point rubric の独立項目化されていない**
- skill-creator (`example-skills:skill-creator`) 経由で SKILL.md を起草する際、frontmatter `related_rules` の自動生成 / 検証が SoT 化されていない
- 採用判定基準 2 (process improvement、skill-creator scaffold 強化) 該当、本 Step 4b では SoT 反映しない (別 PR で消化方針)

### Group 2 共通改修候補 #1-#7 の本 PR 文脈での該当性

PR #153 retro (Group 1 共通観点担当) の本格記載に集約しているため、本 PR では該当する候補のみ簡潔に列挙:

- **#1 (思考動詞辞書漏れ、Group 2 で 6 動詞追加で累計 17 動詞)**: 本 PR (workspace:32 想定) では Group 2 共通の 6 動詞 (`Flambéing` / `Creating` / `Seasoning` / `Frosting` / `Waddling`) のうち本 PR 特有を観測。Step 4a で `Effecting` 追加で累計 17 動詞
- **#2 (Monitor dedup ロジック v3)**: Group 2 で v3 を 3 workspace 統合観察に拡張、本 PR でも再 emit ほぼゼロ
- **#3 (classifier 通過実証)**: 本 PR prompt から `gh pr merge` 文言を削除 → classifier 通過、Phase 0-6 + Ready 化のみで完走
- **#4 (per-task pane self-`/exit` 不可)**: 本 PR の per-task pane も Phase 9 完了後 idle 待機 → orchestrator が `cmux close-workspace` で代行
- **#5 (--body-file 採用)**: 本 PR `gh pr create` も `--body-file /tmp/pr-body-A3-5-harness-meta.md` 方式で起票成功 (Problem #1 / 改修候補 #8 と統合)
- **#6 (R-15 一括事前承認)**: 本 PR は Group 2 一括承認パターンの 3 連発 merge 3 件目、classifier 通過
- **#7 (並列 `git worktree add` lock)**: Group 2 で順次実行採用 (改修候補 #7 反映)、3 worktree 作成全成功、再発なし

## 🚀 Try (次回からの改善案)

### Try #1: 改修候補 #8 の SoT 反映 — `gh pr create` の `--template` / `--body-file` 排他仕様を SoT 化 (PR 個別観点 + Group 2 横断観点、priority: high、Problem #1 直接対応、本 batch PR (#158) 起票時の dogfooding で正確な実仕様判明)

本 batch retro 中で **新規改修候補 #8** として記録、本 PR が起点。**本 batch retro PR (#158) 起票時の dogfooding で gh CLI 実仕様判明** (`--template` と `--body-file` は排他):

```text
$ gh pr create --base master --head <branch> --draft --template harness.md --title "..." --body-file /tmp/...md
`--template` is not supported when using `--body` or `--body-file`
Exit code 1
```

SoT 反映 patch:

**Step 1**: `.claude/rules/implementation-workflow.md` Phase 5 §PR 起票 / §gh pr create 実行手順 に以下を追記:

```diff
--- a/.claude/rules/implementation-workflow.md
+++ b/.claude/rules/implementation-workflow.md
@@ -X,Y +X,Y @@ ### Phase 5: PR 起票

-`gh pr create` で Draft PR を起票する。
+`gh pr create` で Draft PR を起票する。**`--template` と `--body-file` / `--body` は排他**:
+
+- **既定 (本格 PR description を起草する場合)**: `--body-file` を使用、`--template` は **指定しない** (gh CLI 実仕様で同時指定は Exit 1)
+
+\`\`\`bash
+gh pr create \
+  --base master \
+  --head <branch> \
+  --draft \
+  --title "<conventional commits 形式>" \
+  --body-file /tmp/pr-body-<branch-slug>.md
+\`\`\`
+
+- **テンプレ default fill (body を起草せず PR テンプレ default を使う場合)**: `--template <type>.md` を使用、`--body-file` / `--body` は **指定しない**
+
+\`\`\`bash
+gh pr create \
+  --base master \
+  --head <branch> \
+  --draft \
+  --title "<conventional commits 形式>" \
+  --template <type>.md
+\`\`\`
+
+- **失敗例**: `gh pr create ... --template <type>.md --body-file <path>` → **Exit code 1** (`` `--template` is not supported when using `--body` or `--body-file` ``)
+- 対応する `<type>.md`: feature.md / bugfix.md / refactor.md / dependency-upgrade.md / harness.md (`.claude/rules/pr-template.md` §種別対応表 を参照)
+- **`--body-file` 強制 (改修候補 #5 該当)**: `--body "$(cat <<EOF...)"` heredoc 形式は cmux send 経由で Exit 1 リスクあり、本格 PR description を起草する場合は `--body-file` を使用 (本 batch retro PR #158 / Group 1 batch retro PR #153 で実証)
+- **どちらを選択するか**: harness PR / batch retro PR / Skill 本格化 PR / mirror PR は **`--body-file` 推奨** (本格 PR description を起草する必要があるため)、simple PR (1 行説明で済む場合) は `--template` 推奨
```

**Step 2**: `.claude/rules/pr-template.md` に PR 種別と PR template の対応表を新規 subsection で SoT 化 (PR template は **default fill** 用途であり、本格 PR description を起草する場合は `--body-file` 経由でテンプレ内容をユーザー側でコピー / カスタマイズして渡す):

| PR 種別 | branch prefix | `<type>.md` | 用途 | 推奨フラグ |
|---|---|---|---|---|
| 新機能 | `feature/` | `feature.md` | feature-request Skill 経由 | `--body-file` (本格 description) |
| バグ修正 | `fix/` | `bugfix.md` | bug-fix Skill 経由 | `--body-file` (本格 description) |
| リファクタ | `refactor/` | `refactor.md` | refactor Skill 経由 | `--body-file` (本格 description) |
| 依存更新 | (Renovate) | `dependency-upgrade.md` | dependency-upgrade Skill 経由 | `--body-file` (本格 description) |
| ハーネス改修 | `harness/` | `harness.md` | harness-meta / harness-evolution / pr-retrospective / roadmap mirror 等 | `--body-file` (本格 description) |
| その他 (Epic 配下 Skill 本格化等) | `epic/` | `harness.md` または該当種別 | EPIC-A3 配下 Skill 本格化等 | `--body-file` (本格 description) |

> **注**: harness 系 PR (上記表の大半) は本格 PR description が必須 (PR レビュー観点 / AC / 関連 ADR 等を構造化記載) のため、実運用上は **`--body-file` 一択** で `--template` は使用しない。`--template` は body 起草を省略する場合 (例: trivial chore PR) の補助フラグと位置付ける。

**Step 3**: 共通 prompt テンプレ (`/tmp/orchestrator-prompt-common-*.md`) に「**1 行で `gh pr create` の完全コマンド例**」を明示する prompt 設計改修 (`--body-file` 一択パターン):

```text
**完全コマンド例**: `gh pr create --base master --head <branch> --draft --title "<conventional commits>" --body-file /tmp/pr-body-<branch-slug>.md`

**注**: `--template <type>.md` と `--body-file` は **排他** (gh CLI 実仕様で同時指定は Exit 1)。本格 PR description を起草する場合は `--body-file` を使用、`--template` は省略する。
```

- 採用判定基準 1 (Group 2 で実証 + 本 batch PR で dogfooding 再現 + 後続全 per-task pane spawn で再利用必須) 該当
- **本 batch retro PR (#158) 起票時の dogfooding 検出が本 Try #1 の精度を高めた事例**: 本 PR Phase 5 で prompt 内「完全コマンド例」(`--template harness.md --body-file <path>`) をそのまま実行 → Exit 1 で gh CLI 実仕様判明 → fix loop 1 で本 Try #1 を修正 → 後続 SoT 同期 PR で gh CLI 実仕様と整合した patch 適用可能になった

### Try #2: Phase 2-3 内の self-check protocol を SoT 化 (PR 個別観点、priority: medium、Problem #2 直接対応)

- `.claude/rules/implementation-workflow.md` Phase 2-3 §self-check に新規 subsection を追加:
  - **summary 5 行制約 self-check**: 起草直後に `wc -l` で blockquote 行数を確認、`docs-structure.md` §各 docs ファイルの構造 §5 行 summary 必須 と整合
  - **frontmatter `related_rules` 双方向リンク self-check**: 本文中の `.claude/rules/*.md` 参照を `grep` で抽出 → frontmatter `related_rules` と比較 → 不足分を追記
  - **frontmatter `related_adrs` 双方向リンク self-check**: 本文中の `ADR-NNNN` 参照を `grep` で抽出 → frontmatter `related_adrs` と比較 → 不足分を追記
  - **AC 確認 self-check**: Phase 6 code-reviewer の AC リスト (Phase 2-3 でドラフトされた AC) を事前に self-check
- 上記 self-check で発見された違反は Phase 5 PR 起票前に修正、fix loop 削減効果が期待される
- 採用判定基準 2 (process improvement、Phase 2-3 self-check 強化) 該当

### Try #3: skill-creator scaffold で frontmatter `related_rules` 自動生成 / 検証 (PR 個別観点、priority: low、Problem #3 直接対応、本 Step 4b では SoT 反映しない、別 PR で消化)

- `example-skills:skill-creator` scaffold プロセスで以下を SoT 化:
  - 本文起草後に `grep -oE '\.claude/rules/[a-z-]+\.md' SKILL.md | sort -u` で参照される rule を自動抽出
  - frontmatter `related_rules` に自動追記
  - 双方向リンク不整合 (本文参照 ∉ frontmatter) を warning として report
- `.claude/rules/skill-authoring.md` 100-point rubric の「関連 rules / Plan 章へのリンクが正確」項目 (15 点) を **frontmatter integrity** と **本文 link accuracy** の 2 軸に分割
- 採用判定基準 2 (process improvement、skill-creator scaffold 強化) 該当、本 Step 4b では SoT 反映しない (別 PR で消化方針)

### Try #4-#10: Group 2 共通改修候補 #1-#7 + 新動詞辞書拡張累計 17 動詞 の SoT 反映

詳細な Try 提案 (#1-#7 の SoT 反映 patch + 採用判定基準) は [PR #153 retro batch](../../harness/learnings/2026-05-18-pr-148.md) §Group 1 共通改修候補 + §Try #3-#9 を参照 (Group 1 共通改修候補 #1-#7 の本格記載元)。

**新動詞辞書拡張累計 17 動詞** の SoT 反映 patch:

```diff
--- a/.claude/skills/orchestrator/SKILL.md
+++ b/.claude/skills/orchestrator/SKILL.md
@@ -X,Y +X,Y @@ ### 観測済 (本セッション)

-`Channeling` / `Flibbertigibbeting` / `Moonwalking` / `Noodling` / `Twisting` / `Schlepping`
+`Channeling` / `Flibbertigibbeting` / `Moonwalking` / `Noodling` / `Twisting` / `Schlepping` /
+`Pollinating` / `Honking` / `Galloping` / `Pouncing` / `Scampering` / `Forming` / `Bunning` /
+`Finagling` / `Caramelizing` / `Gusting` / `Scurrying` / `Flambéing` / `Creating` / `Seasoning` /
+`Frosting` / `Waddling` / `Effecting`

@@ -X,Y +X,Y @@ ### regex (運用案)

-^✻ (Channeling|Flibbertigibbeting|Moonwalking|Noodling|Twisting|Schlepping|Cooking|...)…?
+^(✻|✶|✽|·|✢|◯) (Channeling|Flibbertigibbeting|Moonwalking|Noodling|Twisting|Schlepping|
+Pollinating|Honking|Galloping|Pouncing|Scampering|Forming|Bunning|Finagling|Caramelizing|
+Gusting|Scurrying|Flambéing|Creating|Seasoning|Frosting|Waddling|Effecting|Cooking|...)…?
```

- PR #153 retro で 11 動詞 (`Pollinating` / `Honking` / `Galloping` / `Pouncing` / `Scampering` / `Forming` / `Bunning` / `Finagling` / `Caramelizing` / `Gusting` / `Scurrying`)、Group 2 で +5 動詞 (`Flambéing` / `Creating` / `Seasoning` / `Frosting` / `Waddling`)、Step 4a mirror で +1 動詞 (`Effecting`)、累計 **17 動詞**
- 採用判定基準 1 (Group 1 + Group 2 + Step 4a mirror で実証 + 後続全 per-task pane spawn で再利用) 該当

## 📊 指標

PR #156 は SKILL.md 1 ファイルのみ touch (`.claude/skills/harness-meta/SKILL.md`)、`.claude/rules/` / `docs/specifications/` / Kotlin source は一切 touch しないため rule / spec / Kotlin 差分はゼロ。三層テスト品質基盤 (Kover / Konsist Spec / PITest) は A7 で導入予定のため Before/After 差分は計測不能 (N/A)。

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| Line coverage | N/A | N/A | — | Kover 未導入 (A7 で導入予定) |
| Branch coverage | N/A | N/A | — | 同上 |
| Spec coverage | N/A | N/A | — | Konsist `@Spec` 未導入 (A7 で導入予定) |
| Mutation score | N/A | N/A | — | PITest 未導入 (A7 で導入予定) |
| Lint 違反数 | N/A | N/A | — | Spotless / ktlint / detekt / markdownlint-cli2 未導入 (A6 で導入予定) |
| ファイル数 (差分) | — | 1 changed | +1 | `.claude/skills/harness-meta/SKILL.md` (新規) |
| 行数 (差分) | — | +209 / -0 (merge commit `5d39478`) | net +209 | 新規 SKILL.md、Group 2 内中央サイズ |
| SKILL.md 新規追加 | — | `active` + `phase: A3` | — | 新規追加パターン (Group 2 内、PR #155 と並ぶ) |
| commits 数 (squash 前) | — | 2 (initial `cedb873` + fix loop `9b8f194`) | +2 | `--merge` で merge commit `5d39478` 経由、Group 2 内唯一の fix loop ケース |
| code-reviewer aspect 実行 (subagent 並列) | — | 4/4 (spec-conformance / architecture / security / code-quality) | — | test-quality / performance / visual-regression / design-tokens skip (docs / SKILL.md only) |
| Critical findings (initial) | — | 0 | — | merge readiness 充足 |
| High findings (initial) | — | 1 (code-quality: frontmatter `related_rules` 欠落) | +1 | fix loop 1 (commit `9b8f194`) で消化 |
| Improvement findings (initial) | — | 1 (code-quality: frontmatter `related_rules` 欠落 2 件追加) | +1 | fix loop 1 で消化 |
| AC failures (initial) | — | 1 (AC-3 ❌: summary 6 行) | +1 | fix loop 1 で消化 |
| fix loop 回数 | — | 1 (上限 3 回、`.claude/rules/implementation-workflow.md` Phase 6) | +1 | initial → fix loop 1 → 全件解消、Group 2 内唯一の fix loop |
| fix loop 1 後 binary checklist | — | 28/28 (4 aspect × 7 項目) | — | spec-conformance 7/7 + architecture 7/7 + security 7/7 + code-quality 7/7 |
| AC 評価 (fix loop 1 後) | — | 10/10 ✅ | — | 全 AC 達成 |
| 連続 self-merge 完走 PR 数 (orchestrator 代行 merge 含む、本 PR まで) | 16 (〜 PR #155) | 17 (+ PR #156) | +1 | Group 2 一括承認パターン (改修候補 #6) で classifier 通過、3 連発 merge 3 件目 (Group 2 完走) |
| `gh pr create --template` 欠落観察 | — | 1 (本 PR で起点、自己 recovery で成功) | +1 | 改修候補 #8 該当、Try #1 で SoT 反映 patch を本格記載 |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造 -->

- [ ] `[rule]` `.claude/rules/implementation-workflow.md` Phase 5 §PR 起票 / §gh pr create 実行手順 に「完全コマンド例」「`--template <type>.md` 必須 + 欠落時の Exit 1 失敗例」を追加。`.claude/rules/pr-template.md` に「PR 種別 ⇄ template 対応表」新規 subsection 追加。共通 prompt テンプレに「1 行で `gh pr create` の完全コマンド例」を明示。Problem #1 / Try #1 (改修候補 #8 該当) 該当、採用判定基準 1 (Group 2 で実証 + 後続全 per-task pane spawn で再利用必須) 該当
- [ ] `[rule]` `.claude/rules/implementation-workflow.md` Phase 2-3 §self-check に「summary 5 行制約 / frontmatter `related_rules` 双方向リンク / frontmatter `related_adrs` 双方向リンク / AC 事前確認」の 4 軸 self-check protocol を SoT 化 (Phase 6 code-reviewer 入りする前に Phase 2-3 で AC 確認)。Problem #2 / Try #2 該当、採用判定基準 2 (process improvement) 該当
- [ ] `[skill]` `example-skills:skill-creator` scaffold プロセスで frontmatter `related_rules` 自動生成 / 検証 (本文参照を grep で抽出 → frontmatter 自動追記 → 双方向リンク不整合を warning)。`.claude/rules/skill-authoring.md` 100-point rubric の「関連 rules / Plan 章へのリンクが正確」項目を frontmatter integrity + 本文 link accuracy の 2 軸に分割。Problem #3 / Try #3 該当、採用判定基準 2 (process improvement) 該当、本 Step 4b では SoT 反映しない (別 PR で消化方針)
- [ ] `[skill]` `.claude/skills/orchestrator/SKILL.md` §観測済 (本セッション) / §regex (運用案) に新動詞辞書拡張累計 17 動詞 (`Pollinating` / `Honking` / `Galloping` / `Pouncing` / `Scampering` / `Forming` / `Bunning` / `Finagling` / `Caramelizing` / `Gusting` / `Scurrying` / `Flambéing` / `Creating` / `Seasoning` / `Frosting` / `Waddling` / `Effecting`) と prefix `(✻︱✶︱✽︱·︱✢︱◯)` 拡張を反映。Try #4-#10 該当、採用判定基準 1 (Group 1 + Group 2 + Step 4a mirror で実証) 該当
- [ ] `[skill]` Group 2 共通改修候補 #1-#7 の詳細 SoT 反映 patch は [PR #148 retro](./2026-05-18-pr-148.md) §Try #3-#9 を参照 (Group 1 共通改修候補 #1-#7 の本格記載元)、Group 2 では再発防止確認のみ (改修候補 #2 v3 は 3 workspace 統合観察に拡張実証、改修候補 #7 順次実行採用は 3 worktree 作成全成功で実証)

## 📝 harness-meta フィードバック

<!-- harness-meta が後から追記。提案 → 結果の往復ログを 1 ファイル内で完結 -->

### 2026-05-18 PR #159 で消化 (採用)

| 提案 | PR #159 での消化内容 |
|---|---|
| Try #1 (改修候補 #8 起点 PR): `gh pr create --template` / `--body-file` 排他 SoT 化 | `.claude/rules/pr-template.md` §gh pr create 必須パラメータ + §起票コマンド に gh CLI 実仕様 (排他、`` `--template` is not supported when using `--body` or `--body-file` ``) を SoT 反映、本リポジトリの全 PR は本格 PR description 起草必須のため **`--body-file` 一択** を既定化。`.claude/rules/implementation-workflow.md` Phase 5 §gh pr create 実行手順 にも完全コマンド例 + 排他関係 + 禁止パターン (heredoc / `--template` + `--body-file` 同時) を SoT 反映。本 batch retro PR (#159) 起票時の dogfooding で gh CLI 実仕様を再現、指示書の `--template <type>.md` + `--body-file <path>` 同時指定例を排他関係に基づき修正 |
| Try #4-#10 集約参照 (改修候補 #1 動詞辞書拡張): 累計 21 動詞 + prefix 6 種拡張 | `.claude/skills/orchestrator/SKILL.md` §Monitor 思考動詞辞書 に累計 21 動詞 + prefix 6 種を SoT 反映 |
| Try #4-#10 集約参照 (改修候補 #5 `--body-file` 強制): Group 2 全 PR で `--body-file` 採用 + PR #146 で heredoc Exit 1 実証 | `.claude/rules/implementation-workflow.md` Phase 5 §gh pr create 実行手順 で SoT 反映 (改修候補 #8 と統合済) |

### 2026-05-18 PR #159 で見送り (後続フェーズへ)

| 提案 | 見送り理由 / 移行先 |
|---|---|
| Try #4-#10 集約参照 (改修候補 #2 / #3 / #4 / #6 / #7): Group 1 共通改修候補 5 件 (PR #148 retro Try #4-#9 経由) | dry-run 必須条件該当 (#2) または orchestrator skill 包括更新 PR (#3 / #4 / #6 / #7) で別 PR 対応。詳細は PR #148 retro `📝 harness-meta フィードバック` 参照 |
| Try #2: Phase 2-3 self-check protocol (summary 5 行 / frontmatter related_rules 双方向リンク / AC 事前確認) SoT 化 | 本 batch retro PR の scope 外。`.claude/rules/implementation-workflow.md` Phase 2-3 polish PR で順次消化方針 |
| Try #3: skill-creator scaffold で frontmatter `related_rules` 自動生成 / 検証 | 本 batch retro PR の scope 外、skill-creator は user-scope (ADR 0025)、本リポジトリ scope 外。`example-skills:skill-creator` への upstream 提案として記録、本リポジトリ側は `.claude/rules/skill-authoring.md` 100-point rubric polish PR で順次補強方針 |

### YYYY-MM-DD <PR ID> で保留 (要再評価)

| 提案 | 保留理由 |
|---|---|

## 関連

- 対象 PR: [#156](https://github.com/subroh0508/colormaster/pull/156) (本 retro 対象、docs(harness): A3-5 harness-meta Skill 完成、改修候補 #8 起点 PR)
- 関連 PR: [#154](https://github.com/subroh0508/colormaster/pull/154) (A3-6 harness-evolution skeleton 本格化、Group 2 同日 merge) / [#155](https://github.com/subroh0508/colormaster/pull/155) (A3-7 dependency-upgrade、Group 2 同日 merge) / [#157](https://github.com/subroh0508/colormaster/pull/157) (Step 4a Group 2 mirror) / [#146](https://github.com/subroh0508/colormaster/pull/146) (EPIC-A3 起票) / [#153](https://github.com/subroh0508/colormaster/pull/153) (Group 1 + mirror retro batch、改修候補 #1-#7 本格記載元) / [#135](https://github.com/subroh0508/colormaster/pull/135) (`[mcp]` プレフィックス受信ルール SoT 化元)
- 関連 ADR: ADR-0017 (ローカル Claude Code ポーリング駆動) / ADR-0024 (MCP 採用 + `gh` CLI 優位) / ADR-0025 (Skill 作成 skill-creator 経由) / ADR-0026 (harness-evolution Skill 採用、harness-meta との二系統補完) / ADR-0027 (docs 構造 + 命名規約 + 日本語化方針)
- 関連 Epic: [`EPIC-A3-skill-suite-extension`](../../epics/EPIC-A3-skill-suite-extension/) (本 PR は A3-5 完成、Group 2 最終 PR)
- 関連 rule: `.claude/rules/{harness-meta-criteria,retrospective-format,skill-authoring,pr-poller,harness-evolution,template-language,branch-naming,pr-template,merge-readiness,implementation-workflow,code-reviewer-aspects,docs-structure,markdown,pii,secrets}.md`
- 関連 Skill: `.claude/skills/{harness-meta,harness-evolution,pr-poller,pr-retrospective,orchestrator,code-reviewer,implementation-workflow,plan-author,epic-author,adr-author,dependency-upgrade}/SKILL.md`
- 関連 PR テンプレ: `.github/PULL_REQUEST_TEMPLATE/harness.md` (本 PR で使用、改修候補 #8 該当の `--template` 必須パラメータ)

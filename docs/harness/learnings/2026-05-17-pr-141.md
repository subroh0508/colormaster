---
id: learning-pr-141
title: PR #141 メタレトロスペクティブ (集約 retro PR 自体の retro-of-retro / 人間追加項目反映 + dry-run 適用検討)
type: learning
status: draft
related_pr: 141
related_plan: —
related_epic: EPIC-A2-rules-docs-extension
generated_at: 2026-05-17T14:30:00Z
generator: pr-retrospective (skeleton、本ペインで手動代替実行)
---

# PR #141 メタレトロスペクティブ

> 生成: pr-retrospective Skill (skeleton 段階、A3 本格化前のため本ペインで手動代替) at 2026-05-17T14:30:00Z
> 関連 Plan: なし (PR #139 / PR #140 の retro 起票 PR、Plan 不要) / 関連 Epic: EPIC-A2-rules-docs-extension (`completed` 昇格後の retro 集約 PR の retro)
> 対象 PR: [docs(harness): pr-139 + pr-140 retrospective 起票 (8 retro 集約消化 KPT) #141](https://github.com/subroh0508/colormaster/pull/141) (squash merge commit `4bd4bb4`、2026-05-17T14:10:39Z マージ、head commit `59d7290`、`gh pr merge --merge`)
> 差分: 3 ファイル / +240 / -0 (240 diff 行)、commits: 1 (squash 前は 2 / `54a08fc` 初回 retro 起票 + `59d7290` 人間追加項目反映)
> 関連 PR: [#139](https://github.com/subroh0508/colormaster/pull/139) (retro 主対象、PR-1 / 8 retro 集約消化)、[#140](https://github.com/subroh0508/colormaster/pull/140) (retro cross-reference 対象、PR-2 / ADR 補強)、[#142](https://github.com/subroh0508/colormaster/pull/142) (本 retro Try の `dry-run フェーズ` を即時実装した follow-up PR、merge commit `c9b0745`)

## 本 retrospective の位置付け (retro-of-retro)

> **本 retrospective は retro-of-retro (メタレトロ)**: 対象 PR #141 自体が PR #139 / PR #140 の retro 起票 PR (集約 retro PR、meta) であり、本ファイルは「retro PR 起票プロセス自体の学び」を抽出する 2 階層目の retrospective。retro PR 起票フロー (主 retro + cross-reference / 人間追加項目反映 / AI self-review / orchestrator 事前承認 self-merge) の規範化観点で記録、retro 本体 (PR #139 / #140 の Keep / Problem / Try / 指標) には踏み込まず本 PR #141 の起票プロセスのみに集中する。

## ✅ Keep (継続したいこと)

- **集約 retro PR (PR-1 + PR-2 を 1 retro PR で起票) と cross-reference 形式が機能** (consensus: spec-conformance + architecture): PR #139 (主 retrospective) + PR #140 (cross-reference) を 1 PR #141 にまとめて起票、`retrospective-format.md` §「1 PR = 1 ファイル」原則を維持しつつ密接連動 2 PR を集約。本体は PR #139 retro に集約 + PR #140 固有論点 (`accepted` ADR 補足追加許容範囲、retro 119 📝 strikethrough 履歴保持) のみ短形式で独立記述、SoT 重複ゼロ。retrospective-format.md には集約消化 PR / 連動 PR の retro 起票専用テンプレが未定義のため、本 PR を 1 事例として `🤖 ハーネス改善提案` で Try 提案化済。
- **retro 起票後の人間追加項目を追加 commit (`59d7290`) で反映するパターンが機能** (consensus: spec-conformance): 初回 commit `54a08fc` (retro 起票) から 6 分後 (`14:01:25Z` → `14:07:57Z`) に orchestrator (subroh0508) が cmux send 経由で人間追加項目 (dry-run Problem + Try + 提案 3 件) を本 PR に追加指示、両 retro ファイル (PR #139 / PR #140) に `## ⚠️ Problem (人間からの追加)` / `## 🚀 Try (人間からの追加)` / `## 🤖 ハーネス改善提案` 人間追加 3 件冒頭追加 + INDEX.md 採用提案数列更新を即時反映。AI 自動抽出より優先度高として明示するため専用サブセクション形式 + `source: PR #141 起票後の追加指示` 注記を採用、AI 自動抽出セクションと並置で混入リスクゼロ。
- **人間追加項目反映 commit 後の AI self-review (binary checklist) が 21/21 通過、人間追加項目反映の検証も spec-conformance #5 でカバー** (consensus: spec-conformance + security): code-reviewer Skill (skeleton 段階の本ペイン Coordinator 手動代替、PR #137 / #138 / #139 / #140 と同パターン) を 4 aspect (spec-conformance / architecture / security / code-quality) で並列実行、binary checklist 21 項目すべて通過。spec-conformance #5 で「人間追加項目が両 retro に明示反映 (commit `59d7290`)、`(人間からの追加)` サブセクション形式で AI 自動抽出と分離、PR #140 は cross-reference 注記付き」を検証、人間追加項目反映の完全性を機械的に確認。
- **orchestrator 事前承認テキストの再利用 (PR #139 / #140 と同形式) で classifier ブロックゼロ self-merge** (consensus: security): 「orchestrator (subroh0508) が本指示で Phase 1-9 含む self-merge を本指示で明示的に事前承認 (R-15 該当)」を本 PR でも継続適用、`gh pr create` / `gh pr ready 141` / `gh pr merge 141 --merge` をすべて本ペインから直接実行、out-of-band orchestrator 委任ゼロ。PR #135 (A2-3) → PR #136 (mirror) → PR #139 (PR-1) → PR #140 (PR-2) → PR #141 (本 PR) と 5 PR 連続でフル self-merge 完走、A2-6 permission 拡張の継続効果実証。
- **retro 起票 → 追加 commit → AI self-review → squash merge を 9 分で完走** (consensus: process): 初回 commit `54a08fc` (`14:01:25Z`) → 人間追加項目反映 commit `59d7290` (`14:07:57Z`) → AI review post (`14:10:13Z`) → squash merge `4bd4bb4` (`14:10:39Z`) の 9 分間で起票完走、Markdown only PR (3 ファイル / +240 / -0) + retro docs のため code-reviewer aspect 動的選択 (4 aspect skip) + fix loop ゼロで機動性確保。
- **即時 follow-up PR (#142、dry-run フェーズ配置) で本 retro Try-2 を実装、retro → 改修の往復ループ成立** (consensus: architecture): PR #141 merge (`14:10:39Z`) から 36 分後 (`14:46Z` 想定) に PR #142 が起票され、本 retro の `🚀 Try (人間からの追加)` で記録した dry-run フェーズ 5 ステップ (`.claude/skills/harness-bootstrap/SKILL.md` 拡張 + `harness-meta-criteria.md` §dry-run 必須条件追記 + `docs/harness/dry-runs/{template.md,INDEX.md}` 新規追加 + CLAUDE.md lookup table 行追加) を全件配置 (merge commit `c9b0745`)。retro 起票直後の Try → 実装サイクルが最短 36 分で成立、harness-meta Skill 本格化 (A4) 前の手動代替ワークフローでも往復ループが機能。
- **code-reviewer aspect 動的選択 (Markdown only PR で 4/8 aspect skip) を retro PR で継続適用**: A2-3 (PR #135) で `code-reviewer-aspects.md` §aspect 動的選択ルール に追加した「Markdown only PR は spec-conformance / architecture / security / code-quality の 4 aspect のみ実行、test-quality / performance / visual-regression / design-tokens は skip」を PR #137 / #138 / #139 / #140 に続き本 PR でも継続適用、review effort 半減 + 全 21 項目 binary checklist で Critical 0 / Improvement 0 を機械的に検証。
- **本 retro 起票自体が PR #142 dry-run phase の 2 番目の適用事例化候補**: 本 retro の `🤖 ハーネス改善提案` (人間追加 3 件 + AI 自動抽出 4 件) のうち、`[rule]` retrospective-format.md §人間追加項目セクション canonical 化 や `[rule]` `meta_retro: true` frontmatter フラグのような retrospective-format.md 規約改修は dry-run 必須対象 (rule 全文書き換えではないが、frontmatter スキーマ変更は構造変更該当)。PR #142 で配置済の `docs/harness/dry-runs/template.md` を本 retro Try の改修提案に対して適用すれば、dry-run phase の 2 番目の適用事例 (1 番目は PR #142 自身が dry-run なしで配置された分の事後検証) として活用可能。

## ⚠️ Problem (詰まったこと / 制約)

- **retro 起票直後に人間追加項目を反映するフローが `retrospective-format.md` 規約に未明文化** (consensus: spec-conformance): 本 PR で採用した `## ⚠️ Problem (人間からの追加)` / `## 🚀 Try (人間からの追加)` サブセクション形式は即興判断、`retrospective-format.md` §本文構造 では `## ⚠️ Problem (詰まったこと / 制約)` / `## 🚀 Try (次回からの改善案)` のみ canonical。AI 自動抽出と人間追加を分離する規範が未明文化のため、後続の retro 起票で「人間追加が AI 自動抽出に混入」「人間追加項目が retro 起票 PR に反映漏れ」のリスクあり (本 PR では orchestrator self-grep で 1 回目漏れを検知、commit `59d7290` で追記したが体系化されていない)。
- **retro PR 自体の retro (retro-of-retro / メタレトロ) を起票するフローが未確立** (consensus: architecture): 本 retro が retro-of-retro として起票される時点で、ファイル名 `2026-05-17-pr-141.md` が PR #141 の retro であることは伝わるが、本 retro が retro PR の retro (= メタレトロ) であることは frontmatter / ファイル名から判別不能 (本文 §位置付け で明示するのみ)。`retrospective-format.md` には `meta_retro: true` のようなフラグも未定義、INDEX.md の索引列も通常 retro と同列で区別なし。後続の retro PR (例: PR #138 / PR #141) ごとに retro-of-retro が必要かの判定基準も未明文化。
- **cmux send 経由の orchestrator → per-task 指示が backtick / 特殊文字エスケープ漏れリスク** (consensus: process): 本 PR の人間追加項目反映指示 (cmux send 経由) で「backtick で囲まれたコードブロック」「Markdown table」「リスト構造」を直接送信、エスケープ漏れによる送信失敗 / 内容 corruption リスクあり。本 PR では orchestrator が手動 escape で回避できたが、cmux send 標準フォーマット (backtick / 特殊文字回避、blockquote / list 構造で代替) が未策定、後続の per-task 指示で同種リスクが再発する可能性。
- **「人間追加項目反映」self-check chain が未自動化、AI 出力の Last-Mile 漏れ検知が脆弱** (consensus: spec-conformance + process): 本 PR では orchestrator が retro 起票直後の self-grep で「dry-run Problem / Try / 提案 3 件が両 retro に反映されているか」を手動確認、漏れ検出時に追加 commit `59d7290` で即座に追記。しかし self-check chain は明文ルールではなく orchestrator の判断依存、後続の retro 起票で「AI が人間追加を読み飛ばす」「両 retro のうち片方のみ反映」のような Last-Mile 漏れ検知が脆弱。`pr-retrospective` Skill 本格化 (A3) 時の入力スキーマ + 自動 self-check 統合が必要。
- **dry-run phase の本 retro への適用検討が「事後実装 (PR #142 merge 後)」になり、retro Try と実装の前後関係が逆転** (consensus: architecture): 本 retro Try (`🚀 Try (人間からの追加)`) で記録された dry-run 5 ステップは、本 retro 起票時点で PR #142 が既に merge 済 (commit `c9b0745`、`14:46Z` 想定)、`docs/harness/dry-runs/template.md` / `harness-meta-criteria.md` §dry-run 必須条件 / `.claude/skills/harness-bootstrap/SKILL.md` retro 集約改善モードが配置済。本 retro 起票時に dry-run phase を本 retro 自身に適用するべきだったが、PR #142 が dry-run なしで配置された (dry-run phase 配置自体が dry-run 対象になる self-reference) ため、本 retro Try は「PR #142 で実装された dry-run phase を本 retro Try の改修提案に対して 2 番目の適用事例として遡及適用」という変則ルートで吸収必要。
- **本 retro 起票時点で PR #142 が retro Try → 実装の即時往復ループとして成立したが、retro 完成後の往復ログ追記場所が `📝 harness-meta フィードバック` のみ** (consensus: architecture): PR #142 で本 retro Try-2 (dry-run フェーズ) を実装した事実は本 retro の `📝 harness-meta フィードバック` §採用 表に記録する必要があるが、本 retro 起票時点では未追記。retrospective-format.md の §📝 harness-meta フィードバック 規約 は `harness-meta` Skill が後追記する想定だが、本 PR では手動代替で先回り追記、表構造の整合性が必要。
- **retro PR の retro 起票判定基準が未明文化、本 PR が retro-of-retro 起票対象であるか自動判定不能** (consensus: process): PR #141 が「集約 retro PR」+ 「人間追加項目反映 commit を含む」+ 「PR #142 で Try が即時実装された」という 3 つの特徴を持つため retro-of-retro 価値が高いと判断したが、`pr-poller.md` / `harness-meta-criteria.md` には retro PR 自体の retro 起票判定基準が未定義。後続の retro PR (例: PR #138 / PR #134 等) で retro-of-retro が必要かは orchestrator の判断依存、自動化困難。

## 🚀 Try (次回からの改善案)

- **`.claude/rules/retrospective-format.md` §本文構造 に「人間追加項目セクション canonical 化」を追加** (priority: high): `## ⚠️ Problem (人間からの追加)` / `## 🚀 Try (人間からの追加)` を canonical サブセクション形式として明文化、source 注記 (`source: PR #NNN 起票後の追加指示`) + AI 自動抽出セクションと並置 + 専用 blockquote の 3 要件を必須化。`🤖 ハーネス改善提案` セクションでは「人間追加 N 件冒頭 + AI 自動抽出が後続」のソート規約を追加候補。本 retro 自身が test ケースとして自己整合 (人間追加 3 件 + AI 自動抽出 4 件)。
- **`.claude/rules/retrospective-format.md` frontmatter に `meta_retro: true` フラグ追加** (priority: medium): retro PR 自体の retro (retro-of-retro / メタレトロ) を識別するフラグ、本 retro `learning-pr-141` で初出。INDEX.md 索引列にも「メタレトロ」マーカーを追加、`pr-poller` / `harness-meta` が parse する際の入力区分化に活用。`meta_retro: true` の retro は通常 retro と本文構造を共有 (Keep / Problem / Try / 指標 / 提案 / フィードバック) しつつ §位置付け セクションを必須化、対象 PR の retro 起票プロセス自体を主題とする旨を明示。
- **`pr-retrospective` Skill 本格化 (A3) で「人間追加項目セクション必須」入力スキーマ追加** (priority: high、人間追加提案準拠): Skill 入力に `human_additions: { problems: [], tries: [], proposals: [] }` フィールドを追加、生成された retro ファイルに自動で `(人間からの追加)` サブセクションを差し込み。漏れ検知も Skill 内蔵化 (`human_additions` 非空時に retro ファイル内に該当サブセクション存在を assert)。本 PR の手動代替フロー (orchestrator self-grep) を Skill 自動化、Last-Mile 漏れ検知の脆弱性を解消。
- **新規 rule `.claude/rules/orchestrator-criteria.md` (workspace で起草中) に「retro 起票後の人間追加項目反映 self-check chain」フロー追加** (priority: high、人間追加提案準拠): orchestrator が retro 起票直後に self-grep で漏れ検出 → 漏れあれば即追記 commit する 3 ステップフローを明文化、`pr-retrospective` Skill 本格化前 (A3 完了前) の経過措置として運用。本 PR の commit `59d7290` パターンを canonical 化、後続 retro 起票時の運用ガイダンス化。
- **cmux send 標準フォーマット策定** (priority: medium): orchestrator → per-task 指示で backtick / 特殊文字を回避、blockquote / list 構造で代替するフォーマットを `.claude/skills/{implementation-workflow,pr-retrospective}/SKILL.md` の起動契機セクションに追加。本 PR では人間追加項目指示が cmux send 経由で送信されたが、後続の Markdown table / コードブロック / 識別子 (PR #NNN) を含む指示で送信失敗リスク、escape 不要な構造化フォーマットを canonical 化候補。
- **`docs/harness/dry-runs/template.md` (PR #142 で配置済) を本 retro Try の改修提案に対して遡及適用、dry-run phase の 2 番目の適用事例化** (priority: medium): 本 retro Try-1 (`[rule]` retrospective-format.md §人間追加項目セクション canonical 化) と Try-2 (`[rule]` `meta_retro: true` frontmatter フラグ) は rule 構造変更該当のため dry-run 必須対象、PR #142 で配置された dry-run template に対する 2 番目の適用事例として `docs/harness/dry-runs/2026-05-17-pr-141-meta-retro-rules.md` 起票候補。dry-run phase 自体の self-reference (PR #142 が dry-run なしで dry-run phase を配置した) を遡及的に検証。
- **`retrospective-format.md` に「retro PR 自体の retro 起票判定基準」セクション追加** (priority: low): 集約 retro PR / 人間追加項目反映 commit 含む / 即時 follow-up PR で Try 実装の 3 条件のいずれか 2 つを満たす retro PR は retro-of-retro 対象とする judgment 基準を明文化、`pr-poller.md` / `harness-meta-criteria.md` の起動閾値とも整合。本 PR (PR #141) は 3 条件すべて該当のため retro-of-retro 起票が妥当、後続の retro PR で機械判定可能化候補。
- **`harness-meta` Skill 本格化 (A4) で retro-of-retro と通常 retro を区別、メタレトロ専用の集約パスを設計**: 本 retro のような retro PR 起票プロセス自体の学びは、通常 retro (PR の実装内容に関する学び) とは集約対象が異なる (rule / Skill / template の改修提案 vs retro 起票フロー改修提案)。harness-meta Skill 本格化時に `meta_retro: true` retro と通常 retro を別 input stream として parse、メタレトロ専用の改修 PR 起票パス (例: `harness/meta-retro-improvements-YYYY-MM-DD`) を設計候補。
- **本 retro の `📝 harness-meta フィードバック` §採用 表に PR #142 で消化済の Try (dry-run フェーズ 5 ステップ) を初回起票時から追記**: retrospective-format.md §📝 harness-meta フィードバック 規約 は `harness-meta` Skill が後追記する想定だが、本 PR のように retro 起票時点で follow-up PR が既に merge 済の場合は初回起票時から「採用」表に記載する判断パターンを `retrospective-format.md` Gotchas に明文化候補。`harness-meta` Skill 本格化 (A4) 後はこの先回り追記パターンが Skill 内蔵フローと衝突しないよう調整が必要。

## 📊 指標

PR #141 は retro 起票 + 人間追加項目反映のみで実装コード差分ゼロ (`docs/harness/learnings/` Markdown のみ)、三層テスト品質基盤 (Kover / Konsist Spec / PITest) は A7 で導入予定のため Before/After 差分は計測不能 (N/A)。

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| Line coverage | N/A | N/A | — | Kover 未導入 (A7 で導入予定) |
| Branch coverage | N/A | N/A | — | 同上 |
| Spec coverage | N/A | N/A | — | Konsist `@Spec` 未導入 (A7 で導入予定) |
| Mutation score | N/A | N/A | — | PITest 未導入 (A7 で導入予定) |
| Lint 違反数 | N/A | N/A | — | Spotless / ktlint / detekt / markdownlint-cli2 未導入 (A6 で導入予定) |
| ファイル数 (差分) | (PR-1 / PR-2 merge 後) | 3 changed | 3 changed | `docs/harness/learnings/{2026-05-17-pr-139.md,2026-05-17-pr-140.md,INDEX.md}` |
| 行数 (差分) | (PR-1 / PR-2 merge 後) | +240 / -0 (squash merge commit `4bd4bb4`) | net +240 | 全て retro docs (Markdown only) |
| commits 数 (squash 前) | (PR-1 / PR-2 merge 後) | 2 (`54a08fc` 初回 retro 起票 + `59d7290` 人間追加項目反映) | +2 | `--merge` (merge commit 保持) で 2 commit を merge commit `4bd4bb4` 配下に保持 |
| code-reviewer aspect 実行 (subagent 並列) | — | 4/4 pass (spec-conformance / architecture / security / code-quality) | — | test-quality / performance / visual-regression / design-tokens skip (Markdown only / A10 enable 待ち、PR #137-140 と同パターン) |
| Critical findings | — | 0 | — | merge readiness 3 条件充足 |
| Improvement 件数 | — | 0 | — | binary checklist 21/21 通過、fix loop 不要、PR #140 と同水準 |
| binary checklist 通過率 (4 aspect 集約) | — | 21/21 ✅ | — | spec-conformance 6 + architecture 5 + security 5 + code-quality 5 |
| 人間追加項目反映 commit (`59d7290`) で reflected items | 0 | 5 (Problem 1 + Try 1 + 提案 3) | +5 | 両 retro ファイル + INDEX.md に同一内容を明示記録 |
| 即時 follow-up PR で Try 実装件数 | 0 | 3 (PR #142 で `[skill]` / `[rule]` / `[template]` の人間追加 3 件すべて実装) | +3 | retro 起票直後の Try → 実装サイクル最短 36 分成立 (PR #141 merge `14:10:39Z` → PR #142 merge `14:46Z` 想定) |
| retro 起票 → squash merge 所要時間 | — | 9 分 (`14:01:25Z` 初回 commit → `14:10:39Z` merge) | — | 連続 self-merge 機動性指標、PR #140 (PR-2 起票 → merge 9 分) と同水準 |
| 人間追加項目反映 commit → squash merge 所要時間 | — | 3 分 (`14:07:57Z` commit → `14:10:39Z` merge) | — | AI review post (`14:10:13Z`) + 即時 merge、self-grep + 追記 commit の機動性指標 |
| 連続 self-merge 完走 PR 数 (A2-6 permission 拡張効果、本 PR まで) | 4 (PR #135 / PR #136 / PR #139 / PR #140) | 5 (+ PR #141) | +1 | 後続 PR #142 もすでに self-merge 完走で 6 連続達成 |
| retro 起票 PR の retro-of-retro 件数 (累計) | 0 | 1 (本 PR が初の retro-of-retro) | +1 | `meta_retro: true` フラグ frontmatter 候補 (Try で記録) |
| PR #142 で消化された retro Try 件数 | — | 3 件 (人間追加 3 件すべて、最短 36 分で実装完走) | +3 | retro Try → 即時実装の往復ループ事例 |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造 -->

<!-- 以下 3 件は orchestrator (subroh0508) が retro レビュー時に追加した人間追加提案、AI 自動抽出より優先度高 -->

- [ ] `[skill]` `pr-retrospective` Skill (A3 本格化対象) に **「人間追加項目セクション必須」入力スキーマ追加** (人間からの追加、最優先): Skill 入力に `human_additions: { problems: [], tries: [], proposals: [] }` フィールドを追加、生成された retro ファイルに自動で `(人間からの追加)` サブセクションを差し込み + 漏れ検知も Skill 内蔵化 (`human_additions` 非空時に retro ファイル内の該当サブセクション存在を assert)。本 PR の手動代替フロー (orchestrator self-grep + 追加 commit) を Skill 自動化、Last-Mile 漏れ検知の脆弱性解消。採用判定基準 4 (Critical 派生、AI 出力の漏れリスク回避) 該当
- [ ] `[rule]` 新規 rule `.claude/rules/orchestrator-criteria.md` (workspace:19 で起草中) に **「retro 起票後の人間追加項目反映 self-check chain」フロー追加** (人間からの追加、最優先): orchestrator が retro 起票直後に self-grep で漏れ検出 → 漏れあれば即追記 commit する 3 ステップフローを明文化、`pr-retrospective` Skill 本格化前 (A3 完了前) の経過措置として運用。本 PR の commit `59d7290` パターンを canonical 化。採用判定基準 1 (本 PR で実証 + 後続 retro で再利用見込み) 該当
- [ ] `[template]` `.claude/rules/retrospective-format.md` に **「人間追加 Problem / Try」専用見出しテンプレ追加** (人間からの追加、最優先): `## ⚠️ Problem (人間からの追加)` / `## 🚀 Try (人間からの追加)` を canonical サブセクション形式として §本文構造 に明文化、source 注記 (`source: PR #NNN 起票後の追加指示`) + AI 自動抽出セクションと並置 + 専用 blockquote の 3 要件を必須化。`🤖 ハーネス改善提案` セクションでは「人間追加 N 件冒頭 + AI 自動抽出が後続」のソート規約を追加候補。本 retro が test ケースとして自己整合。採用判定基準 1 (本 PR で実証 + 後続 retro で再利用見込み) 該当

<!-- 以下は AI 自動抽出 -->

- [ ] `[rule]` `.claude/rules/retrospective-format.md` frontmatter スキーマに **`meta_retro: true` フラグ追加**: retro PR 自体の retro (retro-of-retro / メタレトロ) を識別するフラグ、本 retro `learning-pr-141` で初出。INDEX.md 索引列にも「メタレトロ」マーカー追加候補、`pr-poller` / `harness-meta` が parse する際の入力区分化に活用。`meta_retro: true` の retro は §位置付け セクションを必須化、対象 PR の retro 起票プロセス自体を主題とする旨を明示。採用判定基準 2 (retrospective-format.md 関連、structural change) 該当
- [ ] `[rule]` `.claude/rules/retrospective-format.md` に **「retro PR 自体の retro 起票判定基準」セクション追加**: 集約 retro PR / 人間追加項目反映 commit 含む / 即時 follow-up PR で Try 実装の 3 条件のいずれか 2 つを満たす retro PR は retro-of-retro 対象とする judgment 基準を明文化、`pr-poller.md` / `harness-meta-criteria.md` の起動閾値とも整合。本 PR (PR #141) は 3 条件すべて該当のため retro-of-retro 起票が妥当、後続の retro PR で機械判定可能化候補。採用判定基準 1 (本 PR で実証) 該当
- [ ] `[skill]` `.claude/skills/{implementation-workflow,pr-retrospective}/SKILL.md` の起動契機セクションに **「cmux send 標準フォーマット」追加**: orchestrator → per-task 指示で backtick / 特殊文字を回避、blockquote / list 構造で代替するフォーマットを canonical 化。本 PR では人間追加項目指示が cmux send 経由で送信されたが、後続の Markdown table / コードブロック / 識別子 (PR #NNN) を含む指示で送信失敗リスク、escape 不要な構造化フォーマットを明文化候補。採用判定基準 3 (process improvement、複数 Skill 横断) 該当
- [ ] `[rule]` `.claude/rules/retrospective-format.md` Gotchas に **「retro 起票時点で follow-up PR が既に merge 済の場合は初回起票時から `📝 harness-meta フィードバック` §採用 表に記載する判断パターン」明文化**: retrospective-format.md §📝 harness-meta フィードバック 規約 は `harness-meta` Skill が後追記する想定だが、本 PR のように retro 起票時点で follow-up PR が既に merge 済の場合は初回起票時から記載する先回り追記パターン。`harness-meta` Skill 本格化 (A4) 後の自動化と衝突しないよう「先回り追記時は `source: pr-retrospective 手動代替実行` 注記必須」の規約とセット。採用判定基準 1 (本 PR で実証) 該当

## 📝 harness-meta フィードバック

<!-- harness-meta が後から追記。提案 → 結果の往復ログを 1 ファイル内で完結 -->

### 2026-05-17 PR #142 で消化 (採用)

> **先回り追記**: 本 retro 起票時点で PR #142 が既に merge 済 (commit `c9b0745`、`14:46Z` 想定) のため、初回起票時から「採用」表に記載 (source: pr-retrospective 手動代替実行)。`harness-meta` Skill 本格化 (A4) 後の自動化と衝突しないよう、上記 Try `[rule]` retrospective-format.md Gotchas 明文化候補と整合。

| 提案 | PR #142 での消化内容 |
|---|---|
| `[skill]` `pr-retrospective` に「人間追加項目セクション必須」入力スキーマ追加 (人間からの追加、最優先) | PR #142 では `.claude/skills/harness-bootstrap/SKILL.md` に retro 集約改善モード + 5 ステップ dry-run フェーズを配置 (skill scaffold)、`pr-retrospective` Skill 本体 (A3 本格化対象) への入力スキーマ追加は A3 で実装予定。本 retro Try `[skill]` pr-retrospective 入力スキーマ追加と A3 で統合 |
| `[rule]` 新規 rule `.claude/rules/orchestrator-criteria.md` に dry-run 必須条件追記 (人間からの追加、最優先) | PR #142 で `.claude/rules/harness-meta-criteria.md` に §dry-run 必須条件 セクション追加 (rule 全文書き換え / Skill 新規追加 / template 構造変更は dry-run 必須、typo 修正 / リンク追加は不要)。本 retro Try `[rule]` orchestrator-criteria.md 「retro 起票後の self-check chain」追加は別 PR で対応見込み (orchestrator-criteria.md は workspace で起草中、未配置) |
| `[template]` `docs/harness/dry-runs/template.md` 新規追加 (人間からの追加、最優先) | PR #142 で `docs/harness/dry-runs/{template.md,INDEX.md}` 新規作成 + CLAUDE.md lookup table に `docs/harness/dry-runs/**/*.md` 行追加 + `harness-meta-criteria.md` §dry-run 必須条件 から template リンクで参照可能化 |

### YYYY-MM-DD <PR ID> で見送り (後続フェーズへ)

| 提案 | 見送り理由 / 移行先 |
|---|---|

### YYYY-MM-DD <PR ID> で保留 (要再評価)

| 提案 | 保留理由 |
|---|---|

## 関連

- **retro 主対象 PR の retrospective**: [`2026-05-17-pr-139.md`](./2026-05-17-pr-139.md) (PR #139 / PR-1 / 8 retro 集約消化、本 retro の主対象)、[`2026-05-17-pr-140.md`](./2026-05-17-pr-140.md) (PR #140 / PR-2 / ADR 補強、cross-reference 形式)
- 即時 follow-up PR: [#142](https://github.com/subroh0508/colormaster/pull/142) (本 retro Try の `dry-run フェーズ` を 36 分後に実装、merge commit `c9b0745`)
- 関連 PR: [#141](https://github.com/subroh0508/colormaster/pull/141) (本 retro の対象 PR、retro 起票 PR)
- 関連 ADR: ADR-0026 (harness-evolution Skill / retrospective format) / ADR-0027 (テンプレ言語、日本語化)
- 関連 rule: `.claude/rules/{retrospective-format,harness-meta-criteria,pr-poller,pr-template,merge-readiness,docs-structure,template-language}.md`
- 関連 Skill: `.claude/skills/{pr-retrospective,harness-bootstrap,harness-meta,pr-poller}/SKILL.md`
- Epic: [`EPIC-A2-rules-docs-extension`](../../epics/EPIC-A2-rules-docs-extension/) (`completed` 昇格後の retro 集約 PR の retro)

---
id: learning-pr-161
title: PR #161 レトロスペクティブ (orchestrator SKILL.md 包括更新、改修候補 #3/#4/#6/#7 SoT 反映)
type: learning
status: draft
related_pr: 161
related_plan: —
related_epic: —
generated_at: 2026-05-19T18:10:00+09:00
generator: pr-retrospective (skeleton、本ペインで手動代替実行)
---

# PR #161 レトロスペクティブ (orchestrator SKILL.md 包括更新)

> **5 行以内 summary**: 旧 orchestrator pane (workspace:2) 累計 14 PR 監督セッション
> (PR #146-#159) 観測の改修候補 4 件 (#3 per-task pane gh pr merge orchestrator 代行 /
> #4 self-/exit 不可 + close-workspace 代行 / #6 4 承認粒度別文言テンプレ / #7 worktree
> 事前作成直列必須化) を `.claude/skills/orchestrator/SKILL.md` 単一ファイルに一括 SoT 化。
> 1 ファイル / 1 commit / fix loop ゼロ完走、PR #159 見送り 5 件のうち 4 件を本 PR で消化。

> 生成: pr-retrospective (skeleton、本ペインで手動代替実行) at 2026-05-19T18:10:00+09:00
> 関連 Plan: — / 関連 Epic: — (orchestrator skill 単一ファイル改修)
> 対象 PR: [feat(harness): orchestrator SKILL.md 包括更新 (改修候補 #3/#4/#6/#7 SoT 反映) #161](https://github.com/subroh0508/colormaster/pull/161) (squash merged at 2026-05-18T01:25:45Z、commit `110d7bd`、from head `harness/orchestrator-skill-updates-2026-W21`)
> 差分: 1 ファイル / +96 / -22 (118 diff 行)、commits: 1 (fix loop ゼロ完走)

## ✅ Keep (継続したいこと)

- **4 改修候補を単一 SKILL.md 1 ファイルに集約 SoT 反映**: #3 (per-task pane gh pr merge denied 解消) / #4 (self-/exit 不可 + close-workspace 代行) / #6 (4 承認粒度別文言テンプレ) / #7 (並列 git worktree add 順次必須) を `.claude/skills/orchestrator/SKILL.md` 単一ファイルに包括更新、Phase 2 / Phase 6 / Phase 9 / 明示承認文言 canonical / per-task pane 起動 prompt テンプレ / Gotchas の 6 セクションを横断改修 (consensus: spec-conformance + architecture)
- **per-task pane 起動 prompt テンプレを #3/#4 統合反映で書き換え**: gh pr merge 削除 + /exit 削除 + orchestrator workspace ID 共有 + 完了報告フォーマット明示の 4 軸統合、per-task / orchestrator の責務分離を起動 prompt 段階で SoT 化 (consensus: architecture)
- **改修候補 #6 で 4 承認粒度別文言テンプレ表を SoT 化**: セッション粒度 / Group 粒度 / 単一 PR 粒度 / Sub-task 粒度の 4 段階を canonical 文言テンプレ表で明示、R-15 (auto-merge 禁止) の代替パスとして orchestrator 事前承認を構造化 (consensus: architecture)
- **改修候補 #7 で並列 git worktree add の lock 衝突予防を Phase 2 §Step 2-A worktree 事前作成直列必須化として SoT 反映**: Group 1 4 並列 spawn で 3/4 失敗 → 順次紐付け recovery の事例から「並列 worktree add は禁止、Phase 2 事前作成 + per-task pane への割当を直列実施」を規範化 (consensus: spec-conformance)
- **PR description の対象 PR 表で改修候補ごとの該当判定基準 (1 / 4) を併記**: #3 / #4 / #6 / #7 すべて基準 1 (反復観測)、#3 のみ基準 4 (Critical 派生 = denied 連発で並列度低下) を併記、judgement プロセスの透明性確保
- **Gotchas に 3 項目追加 (並列 git worktree add NG / per-task pane gh pr merge NG / per-task pane /exit NG)**: 改修候補 #7 / #3 / #4 のそれぞれを Gotchas にも反映、トラブルシューティング時の参照経路を二重化
- **single-file PR (`.claude/skills/orchestrator/SKILL.md` 1 ファイル) でフォーカス維持**: rule 改修 / docs 改修 / Skill 改修の混在を避け、SKILL.md 単独改修に scope を絞ることでレビュー負荷低減 + diff 読み切り可能性向上
- **fix loop ゼロ完走**: code-reviewer 4 aspect (spec / arch / security / code-quality) Critical 0、initial commit 1 件で完走、orchestrator 事前承認テキストで classifier ブロックゼロ self-merge

## ⚠️ Problem (詰まったこと / 制約)

- **見送り 4 件 (#3 / #4 / #6 / #7) を 1 PR 集約消化**: PR #159 の見送り 5 件のうち 4 件を本 PR、残 1 件 (#2) を PR #160 (dry-run 起票) に分割。harness-meta-criteria.md §持ち越し先決定 に PR 分割粒度の SoT が未整備のため、1 PR / 4 改修候補が最適粒度か検証不能
- **元 learnings が `/tmp/orchestrator-handover-20260518.md` という ephemeral path**: PR #160 と同様、引き継ぎ summary が `/tmp/` 配下のため永続性なし、merge 後の retro 起票で正規化予定だが本 PR 時点では参照不可
- **single-file PR のため commit logical separator なし**: 4 改修候補を 1 commit で実装、後続レビューで「#3 改修 / #4 改修 / #6 改修 / #7 改修」の logical separator が読み取れない、「6-20 ファイル: 1 PR + commit 分離」境界は本 PR で適用不可 (1 ファイル PR)
- **改修候補 #5 (heredoc → `--body-file` 強制) が本 PR では対象外**: PR #159 で SoT 反映済、本 PR は #3 / #4 / #6 / #7 のみ。PR #159 と本 PR の責務分離は明確だが、後続レビューが「全 8 改修候補のうちどれが本 PR でカバーされたか」を辿るには PR description 必須
- **R-15 一括事前承認パターンの 4 粒度が canonical 文言テンプレ表 (#6) で完備されているか検証なし**: 「セッション粒度 / Group 粒度 / 単一 PR 粒度 / Sub-task 粒度」の 4 段階は今後の運用で粒度追加 / 統合の可能性、SoT 化と同時に versioning がない
- **per-task pane gh pr merge denied 連発の根本原因 (classifier Safety-Check layer の R-15 violation 検出) が SKILL.md には記載なし**: 解決策 (orchestrator 代行) は SoT 化、root cause (classifier 仕様) は別 SoT (`.claude/rules/harness-meta-criteria.md` §classifier ブロック対応) に分散
- **frontmatter `last_updated` を 2026-05-17 → 2026-05-18 に更新**: ファイル単位の更新日記録、改修候補 ID への trace 経路は PR description 経由のみ、機械検証で「どの改修候補が last_updated を変更したか」が辿れない
- **改修候補 #1 (動詞辞書 21 動詞拡張) は PR #159 で SoT 反映済**: 本 PR の改修候補 #6 (4 承認粒度) と PR #159 の #1 / #5 / #8 の論理的境界が「Phase 別 vs 起動 prompt 別」で曖昧、SoT 反映タイミングの判断が暗黙

## 🚀 Try (次回からの改善案)

- **harness-meta-criteria.md §持ち越し先決定 に PR 分割粒度の SoT を整備**: 「採用集約 PR / 見送り集約 PR / dry-run 起票 PR / SoT 反映 PR」の 4 種責務分離 + PR 内改修候補数の上限目安 (例: 4 件程度) を SoT 化、本 PR をユースケース引用
- **引き継ぎ summary を `/tmp/` でなく `docs/harness/handovers/<date>-<workspace-id>.md` に永続化**: PR #160 retro と同提案、本 PR でも dangling reference リスクを認識
- **single-file PR でも commit logical separator を適用可能にする**: 1 ファイルの差分を「#3 改修部分 / #4 改修部分 / #6 改修部分 / #7 改修部分」で git add -p 経由 commit 分離する規範を `.claude/rules/implementation-workflow.md` §commit 分離 に追加
- **改修候補 ID と SoT 反映箇所のクロスリファレンス表を SKILL.md に追加**: 各 SoT セクションに `<!-- 改修候補 #N (PR #XXX) -->` コメントを埋め込み、機械検証で「どの改修候補がどこに反映されたか」を機械的に traverse 可能化
- **R-15 一括事前承認パターン 4 粒度の versioning**: `.claude/skills/orchestrator/SKILL.md` §明示承認文言 canonical に「version 1 (4 粒度)」明示、将来の粒度追加時に diff が読みやすい構造を担保
- **per-task pane gh pr merge denied の root cause / 解決策の双方向リンク強化**: SKILL.md (解決策) と harness-meta-criteria.md §classifier ブロック対応 (root cause) の双方向リンクを明示、両 SoT の独立改修時の drift 予防
- **frontmatter に改修候補 trace を追加**: `last_updated` だけでなく `last_changes` フィールドに改修候補 ID 列を block 形式で記録、機械検証で changes 追跡可能化

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| Line coverage | N/A | N/A | N/A | Kover (A7 完了後)、本 PR は実装コード変更ゼロ |
| Branch coverage | N/A | N/A | N/A | 同上 |
| Spec coverage | N/A | N/A | N/A | Konsist `@Spec` (A7 完了後) |
| Mutation score | N/A | N/A | N/A | PITest (A7 完了後) |
| Lint 違反数 | N/A | N/A | N/A | Spotless / ktlint / detekt / markdownlint-cli2 (A6 完了後) |
| **PR 固有指標** | | | | |
| 変更ファイル数 | 0 | 1 | +1 | `.claude/skills/orchestrator/SKILL.md` 単独改修 |
| 追加行数 | 0 | 96 | +96 | Phase 2 / Phase 6 / Phase 9 / 明示承認文言 canonical / per-task pane 起動 prompt テンプレ / Gotchas 改修 |
| 削除行数 | 0 | 22 | +22 | 旧 §Phase 6 (gh pr merge 含む記述) + 旧 起動 prompt テンプレ |
| commits | 0 | 1 | +1 | fix loop ゼロ完走 |
| 改修候補 SoT 反映数 | 0 | 4 | +4 | #3 + #4 + #6 + #7、PR #159 見送り 5 件のうち 4 件を本 PR で消化 |
| code-reviewer Critical | N/A | 0 | 0 | 4 aspect 全 PASS、AC 8/8 ✅ |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造 -->

- [ ] `[rule]` `.claude/rules/harness-meta-criteria.md` §持ち越し先決定 に PR 分割粒度の SoT を整備。「採用集約 / 見送り集約 / dry-run 起票 / SoT 反映」の 4 種責務分離 + 改修候補数上限目安 4 件を SoT 化、本 PR + PR #159 / #160 を引用、採用判定基準 1 該当
- [ ] `[rule]` 引き継ぎ summary 永続化規約を `.claude/rules/docs-structure.md` に追加。`docs/harness/handovers/<date>-<workspace-id>.md` 命名規約、採用判定基準 4 (Critical 派生 = ephemeral path 依存リスク) 該当
- [ ] `[rule]` `.claude/rules/implementation-workflow.md` §commit 分離 に「single-file PR でも git add -p 経由で改修候補別 commit 分離」を SoT 化。本 PR で適用できなかった事例を引用、採用判定基準 1 (本 PR + 過去 single-file PR で反復) 該当
- [ ] `[skill]` `.claude/skills/orchestrator/SKILL.md` 各 SoT セクションに `<!-- 改修候補 #N (PR #XXX) -->` コメント追加で改修候補 trace を機械的に traverse 可能化。本 PR で導入見送り、採用判定基準 1 該当
- [ ] `[skill]` `.claude/skills/orchestrator/SKILL.md` §明示承認文言 canonical を versioning (`version 1 (4 粒度)` 明示)。将来粒度追加時の diff 読みやすさ担保、採用判定基準 2 (将来運用変化を想定) 該当
- [ ] `[template]` SKILL.md frontmatter に `last_changes` フィールド追加 (改修候補 ID 列を block 形式で記録)、機械検証で changes 追跡可能化。採用判定基準 4 (Critical 派生 = trace 経路の機械検証性) 該当
- [ ] `[rule]` per-task pane gh pr merge denied の root cause (classifier 仕様) を `harness-meta-criteria.md` §classifier ブロック対応、解決策を `orchestrator/SKILL.md` の双方向リンクで強化。両 SoT 独立改修時の drift 予防、採用判定基準 1 該当

## 📝 harness-meta フィードバック

<!-- harness-meta が後から追記。提案 → 結果の往復ログを 1 ファイル内で完結 -->

### YYYY-MM-DD <PR ID> で消化 (採用)

| 提案 | <PR ID> での消化内容 |
|---|---|

### YYYY-MM-DD <PR ID> で見送り (後続フェーズへ)

| 提案 | 見送り理由 / 移行先 |
|---|---|

### YYYY-MM-DD <PR ID> で保留 (要再評価)

| 提案 | 保留理由 |
|---|---|

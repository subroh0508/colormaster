---
id: learning-pr-174
title: PR #174 レトロスペクティブ (harness-meta / single-file PR commit 分離規範 SoT 化)
type: learning
status: draft
related_pr: 174
related_plan: —
related_epic: —
generated_at: 2026-05-19T12:45:00+09:00
generator: pr-retrospective (skeleton、本ペインで手動代替実行)
---

# PR #174 レトロスペクティブ (harness-meta / single-file PR commit 分離規範 SoT 化)

> 生成: pr-retrospective (skeleton、本ペインで手動代替実行) at 2026-05-19T12:45:00+09:00
> 関連 Plan: — / 関連 Epic: —
> 対象 PR: [docs(harness): single-file PR の commit 分離規範を SoT 化 (5 回反復採用) #174](https://github.com/subroh0508/colormaster/pull/174) (squash merged at 2026-05-18T18:46:57Z、commit `6c9c93f`、from head `29a624a`)
> 差分: 4 ファイル / +106 / -7 (113 diff 行)、commits: 2 (rule 改修 commit + learning 追記 commit、本 PR 自身が §commit 分離規範 を実践)

## ✅ Keep (継続したいこと)

- **5 回反復観測の採用判定基準 1 強化条件の SoT 化サイクル**: PR #161/#162/#164/#165/#167 の 5 PR 連続で観測された「single-file PR の commit logical separator なし」問題を採用判定基準 1 強化該当として `.claude/rules/implementation-workflow.md` §commit 分離規範 へ即時 SoT 化。`harness-meta-criteria.md` §採用判定基準 1「複数 PR 反復」を 2 回反復 (最小) → 5 回反復 (強化) に明示的に格上げした初の事例、retro batch 集約 → harness-meta judgment → 改修 PR 起票の最短往復ループが本 PR で確立 (consensus: spec-conformance + architecture)
- **本 PR 自身が §commit 分離規範 を実践 (self-dogfooding)**: rule 改修 commit (`7843652`、+82 行、§commit 分離規範 セクション新規追加) + learning 追記 commit (`29a624a`、+24 行、159/167/171 へ 📝 harness-meta フィードバック追記) の 2 commit に分離、`git add -p` 経由ではなく **複数ファイルを別 commit に分離** で実装。新規規範の最初の適用事例 (採用判定基準 5 強化条件) として SoT に説得力を付与 (consensus: spec-conformance)
- **§分割粒度 (ファイル数ベース) vs §commit 分離規範 (single-file PR 内) の補完関係を明示**: 既存 `harness-meta-criteria.md` §分割粒度 表 (1-5 / 6-20 / 21-30 / 31+ ファイル) が ファイル数ベース、新規 §commit 分離規範 が single-file 内 logical separator と **二重 SoT 補完関係** を明示。`.claude/rules/implementation-workflow.md` §関連 に `harness-meta-criteria.md` を追加して片方向 cross-link 設置 (consensus: architecture)
- **dry-run skip 判定の根拠を 2 条件併用で明示**: `.claude/rules/harness-meta-criteria.md` §dry-run 不要条件 第 3 行「既存セクションへの例示追加」+ 第 6 行「撤回コスト低 + scope 限定 + 既存 rule 本体改定なし」の 2 条件併用で skip 妥当性を担保。本格化済 rule への additive 新セクション追加 + backward compatible + 既存規範無改変 という性質を活用し、不要 dry-run コストを回避 (consensus: spec-conformance)
- **5 PR 学習起点を SoT に列挙 + Phase 別適用タイミング表を整備**: `.claude/rules/implementation-workflow.md` §commit 分離規範 §学習起点 セクションで PR #161/#162/#164/#165/#167 の 5 PR を全件列挙 (各 PR の diff 行数 / commit 数 / Problem 観測内容を併記)、§Phase 別の適用タイミング 表で Phase 3 / 5 / 6 / 7 に分けて適用基準を整理。後続 PR が `git log -p --first-parent` で trace 可能な構造を担保 (consensus: architecture)
- **`git add -p` hunk 分離手順を `y/n/s/e/q` 選択肢の動作説明付きで SoT 化**: 同一ファイル内で複数の論理的変更を別 commit に分離する場合の interactive `git add -p` 手順を 5 ステップで明示、各 hunk の y/n/s/e/q の動作をブロックコメント形式で説明。後続 PR で `git add -p` を再現する際の認知負荷を低減 (consensus: code-quality)
- **code-reviewer 4 aspect inline 評価 (R-37 例外) を Markdown only PR で正規化**: 本 PR は Markdown only + 4 ファイルの小規模 harness PR のため、`merge-readiness.md` §大規模 PR aspect 自動削減 + `code-reviewer-aspects.md` §aspect 動的選択ルール「Markdown only」該当として、R-37 のサブエージェント並列原則の例外として **4 aspect 順次評価 (inline Coordinator)** を採用。Critical 0 / High 0 / Improvement 3 (全件後続持ち越し) で merge 可能と判定、効率的 (consensus: spec-conformance + architecture)
- **見送り判定の根拠 (採用判定基準 / 必須条件 級は dry-run 必須) を明示**: PR #159/#167/#171 の 📝 harness-meta フィードバック「見送り」表で、見送り提案ごとに **採用判定基準 1-5 該当か + dry-run 必須条件該当か + 移行先フェーズ** を明示記録。再評価時の追跡可能性を担保、A4 で harness-meta Skill 本格化時の入力データとして再利用可能 (consensus: spec-conformance)

## ⚠️ Problem (詰まったこと / 制約)

- **fix loop ゼロ完走だが Improvement 3 件は全件「持ち越し」判定**: code-reviewer Coordinator inline 評価で Improvement 3 件 (code-quality 軽微 / architecture 軽微 × 2) を検出したが、いずれも non-blocking として後続 PR (A6 機械検証 / `harness-meta-criteria.md` 改修統合 / +50/+100/+200 行段階例示追加) に持ち越し。「即時消化 vs 持ち越し」判定基準 (`harness-meta-criteria.md` §即時消化基準) に明確に該当しないため、本 PR scope に絞り込んだが、本 PR 内消化を選んでもよかった選択 (consensus: code-quality)
- **§機械検証 セクションの 1 ファイル PR + 1 commit + 100 行超 暫定値が経験則**: `.claude/rules/implementation-workflow.md` §commit 分離規範 §機械検証 で A6 Gradle カスタムタスクの判定基準 (1 ファイル PR + 1 commit + 100 行超 を warning 化) を暫定値で記述。N 段階表記 (N=1 暫定 / N=3-5 ✅ / N=10 確定、`harness-meta-criteria.md` §pr-poller 起動閾値 と同様) を採用していないため、A6 実装時に閾値検証が必要 (consensus: code-quality)
- **`harness-meta-criteria.md` 側からの逆参照が暗黙**: `.claude/rules/implementation-workflow.md` §関連 に `harness-meta-criteria.md` を追加した片方向 cross-link は設置済だが、`harness-meta-criteria.md` §分割粒度 表のキャプションまたは表脚注に「1 ファイル PR の commit 分離は `implementation-workflow.md` §commit 分離規範 参照」を **逆方向リンク** として明示するのが望ましい。本 PR scope を `implementation-workflow.md` 単独改修に絞ったため drift 予防が後続 PR 任せ (consensus: architecture)
- **§例外 表の +50 行未満 と §分離判定基準 の +200 行超の境界が未明示**: §例外 表 (単一 commit OK) で「logical separator が分離コストに見合わない」+50 行未満が明示されているが、§分離判定基準 では「+200 行超の skeleton → active 本格化 PR は特に分離推奨」とだけ記述され、50 行 vs 100 行 vs 200 行の 3 段階境界が未明示。本 PR 自身は rule 改修 +82 行 + learning 追記 +24 行で 2 commit 分離を採用したが、+82 行の境界判定根拠が SoT 化されていない (consensus: architecture)
- **classifier denial 経験なしの一発通過 (前 PR #171 の 2 回 denial と差異)**: 本 PR は 2 commit + 1 push + 1 PR create + 1 ready 化を classifier denial ゼロで完走、前 PR #171 (2 回 denial) と挙動が異なる。同 pattern の事前承認テキスト下でも stochastic 挙動が再現 (PR #129 / PR #135 で観測の通り)、本 PR は通った側のサンプル。再現条件が確立されていないため、後続 PR で再度 denied される可能性 (consensus: spec-conformance)
- **`pr-retrospective` Skill 本格化前で手動代替実行が必要**: A3-10 (PR #167) で `pr-retrospective` Skill skeleton → active 本格化済だが、本 PR は実装が `git add -p` ベースの commit 分離規範の追加であり pr-retrospective Skill 自身を触らないため、後続 retro (本 learning ファイル) は手動代替実行が継続。A3-10 完了後の Skill 駆動起動が `pr-poller` (A4 本格化前) から実現していない (consensus: spec-conformance)
- **batch ブランチ集約への影響**: 本 PR の retro 起票は週次 / 件数閾値 (10 件) 到達時に `harness/learnings-batch-2026-W21-part4` 等の batch ブランチで集約予定。`pr-poller` Skill A4 本格化前は手動代替で起票判定する必要があり、part3 (`harness/learnings-batch-2026-W21-part3`) でカバーされなかった残 PR の追跡が運用負荷 (consensus: spec-conformance)

## 🚀 Try (次回からの改善案)

- **`harness-meta-criteria.md` §分割粒度 表脚注に逆方向 cross-link 追加**: `.claude/rules/harness-meta-criteria.md` §分割粒度 (ファイル数ベース) 表のキャプションまたは表脚注に「1 ファイル PR の commit 分離は `implementation-workflow.md` §commit 分離規範 参照」を明示追加、drift 予防の双方向化。後続 harness-meta 実行 PR で `harness-meta-criteria.md` 改修と同時消化推奨 (Improvement #2 持ち越しの消化)
- **§commit 分離規範 §例外 表に +50/+100/+200 行 3 段階境界例示を追加**: 単一 commit OK vs commit 分離必須 の境界判定根拠を 3 段階例示 (+50 行未満 = 単一 OK / +50〜+100 行 = 内容次第 / +100 行超 = 分離推奨 / +200 行超 = 分離必須) として SoT 化、本 PR 自身の +82 行 / 2 commit 分離も例示として引用 (Improvement #3 持ち越しの消化)
- **A6 Gradle カスタムタスク実装時に commit 分離判定基準を N 段階表記 (N=1 暫定 / N=3-5 ✅ / N=10 確定) で再評価**: §機械検証 セクションの 1 ファイル PR + 1 commit + 100 行超 暫定値を、`harness-meta-criteria.md` §pr-poller 起動閾値 と同様の N 段階表記で運用熟成データ蓄積 → 確定 SoT 昇格 pattern を採用、retro batch 10 件分で再計測 (Improvement #1 持ち越しの消化)
- **classifier denial の stochastic 挙動を `docs/harness/learnings/classifier-denials.md` で追跡継続**: 本 PR は denial ゼロ通過、前 PR #171 は 2 件 denial。同 pattern の事前承認テキストでも挙動が変動するため、PR 番号 / commit pattern / session metadata / denied-or-passed の 4 列で `classifier-denials.md` に蓄積、A3 / A6 で抽出材料化
- **batch retro 起票の運用 SoT 化 (part1 / part2 / part3 / part4 等の分割基準)**: `harness/learnings-batch-YYYY-WW-partN` の分割基準 (件数 / 起票タイミング / part 分割条件) が暗黙運用、明示 SoT 化により後続 batch retro の認知負荷を低減。`.claude/rules/retrospective-format.md` §learning ファイルの集約と週次 PR セクションに part 分割基準を追加検討
- **harness-meta judgment 表記の標準化 (`2026-05-19 harness-meta judgment で <採用|見送り|保留>`)**: 本 PR の 📝 harness-meta フィードバック追記で「2026-05-19 harness-meta judgment で見送り (後続フェーズへ)」表記を採用、`.claude/rules/retrospective-format.md` §フィードバックフォーマット 規約の `YYYY-MM-DD <PR ID> で <採用|見送り|保留>` placeholder に近い形式。`harness-meta` Skill 本格化 (A4) 時に表記揺れ防止のため SoT 化推奨
- **single-file PR の commit 分離規範を `pr-template.md` / `harness.md` PR テンプレに反映**: 本 PR で SoT 化した §commit 分離規範 を `.github/PULL_REQUEST_TEMPLATE/harness.md` 等の PR テンプレ「commit 構造」セクションに反映、PR 起票時にチェックリスト化 (採用判定基準 4 該当の波及)
- **本 PR 採用提案を `harness-meta-criteria.md` §pr-poller 起動閾値 の運用例として引用**: 本 PR は採用判定基準 1 (5 回反復) の強化条件該当として SoT 化、`harness-meta-criteria.md` §採用判定基準 表のキャプションまたは脚注に「強化条件 (5 回反復、PR #174 採用例)」を運用例として引用、N=10 確定の参考データとして蓄積

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| Line coverage | N/A | N/A | N/A | Kover (A7 完了後)、本 PR は Markdown only のため算出不能 |
| Branch coverage | N/A | N/A | N/A | 同上 |
| Spec coverage | N/A | N/A | N/A | Konsist `@Spec` (A7 完了後)、本 PR は Kotlin / SPEC ファイル変更なし |
| Mutation score | N/A | N/A | N/A | PITest (A7 完了後) |
| Lint 違反数 | N/A | N/A | N/A | Spotless / ktlint / detekt / markdownlint-cli2 (A6 完了後) |
| **PR 固有指標** | | | | |
| 変更ファイル数 | 0 | 4 | +4 | rule 1 (`.claude/rules/implementation-workflow.md`) + learning 3 (`159` / `167` / `171`) |
| 追加行数 | 0 | 106 | +106 | rule 改修 +82 + learning 追記 +24 |
| 削除行数 | 0 | 7 | +7 | learning placeholder `YYYY-MM-DD <PR ID>` → `2026-05-19 harness-meta judgment` の置換 |
| commits | 0 | 2 | +2 | rule 改修 commit (`7843652`) + learning 追記 commit (`29a624a`)、本 PR 自身が §commit 分離規範 を実践 |
| code-reviewer Critical findings | N/A | 0 | 0 | aspect スコープ削減 (4/8、test-quality / performance / visual-regression / design-tokens skip) で全件パス |
| code-reviewer High findings | N/A | 0 | 0 | fix loop ゼロ |
| code-reviewer Improvement findings | N/A | 3 | +3 | code-quality 1 (機械検証暫定値) + architecture 2 (逆参照 / 境界例示)、全件後続持ち越し |
| binary checklist 通過率 | N/A | 28/28 (100%) | N/A | 4 aspect × 7 項目、全件パス |
| classifier denial 件数 | 0 | 0 | 0 | 本 PR は denial ゼロ完走 (前 PR #171 の 2 件 denial と差異、stochastic 挙動) |
| 採用判定基準該当 | N/A | 1 (強化条件 5 回反復) | N/A | `harness-meta-criteria.md` §採用判定基準 1 の強化条件 (複数 PR ≥ 5) 初事例 |
| dry-run 必須条件該当 | N/A | 0 (skip 該当) | N/A | `harness-meta-criteria.md` §dry-run 不要条件 第 3 行 + 第 6 行 併用で skip 妥当、3 軸定量評価 N/A |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造 -->

- [ ] `[rule]` `.claude/rules/harness-meta-criteria.md` §分割粒度 表脚注に「1 ファイル PR の commit 分離は `implementation-workflow.md` §commit 分離規範 参照」を逆方向 cross-link として追加。本 PR Improvement #2 (architecture 軽微) を消化、採用判定基準 4 (Critical findings 派生軽微) 該当
- [ ] `[rule]` `.claude/rules/implementation-workflow.md` §commit 分離規範 §例外 表に +50/+100/+200 行 3 段階境界例示を追加 (単一 OK / 内容次第 / 分離推奨 / 分離必須)、本 PR 自身の +82 行 / 2 commit 分離を運用例として引用。本 PR Improvement #3 (architecture 軽微) を消化、採用判定基準 1 (本 PR + 後続 PR で反復見込み) 該当
- [ ] `[rule]` `.claude/rules/implementation-workflow.md` §commit 分離規範 §機械検証 の 1 ファイル PR + 1 commit + 100 行超 暫定値を N 段階表記 (N=1 暫定 / N=3-5 ✅ / N=10 確定) で再評価、A6 Gradle カスタムタスク実装と統合。本 PR Improvement #1 (code-quality 軽微) を消化、採用判定基準 2 (A6 で予定) 該当
- [ ] `[rule]` `.claude/rules/retrospective-format.md` §learning ファイルの集約と週次 PR セクションに `harness/learnings-batch-YYYY-WW-partN` の part 分割基準 (件数 / 起票タイミング / part 分割条件) を明示 SoT 化。本 PR Problem (batch retro 起票の運用) を消化、採用判定基準 2 (A4 で `pr-poller` Skill 本格化と統合) 該当
- [ ] `[rule]` `.claude/rules/harness-meta-criteria.md` §採用判定基準 表脚注に「強化条件 (5 回反復) 運用例: PR #174」を追加、N=10 確定の参考データとして蓄積。本 PR Try を消化、採用判定基準 5 (orchestrator 手動明示推奨) 該当
- [ ] `[template]` `.github/PULL_REQUEST_TEMPLATE/harness.md` の「commit 構造」セクション (新設または既存「変更内容」セクション拡張) に §commit 分離規範 のチェックリスト化項目を追加。本 PR Try を消化、採用判定基準 4 (波及効果) 該当
- [ ] `[skill]` `harness-meta` Skill (A4 本格化時) に「`📝 harness-meta フィードバック` 追記の表記揺れ防止」rubric を追加 (`YYYY-MM-DD <PR ID> で <採用|見送り|保留>` placeholder の正規表現マッチ判定)。本 PR Try を消化、採用判定基準 2 (A4 で予定) 該当
- [ ] `[skill]` `pr-retrospective` Skill (A3-10 active 化済) に「batch ブランチ集約 + week 跨り判定」フェーズを A4 で本格化、現状の手動代替実行を Skill 駆動起動に置換。本 PR Problem (手動代替実行継続) を消化、採用判定基準 2 (A4 で予定) 該当

## 📝 harness-meta フィードバック

<!-- harness-meta が後から追記。提案 → 結果の往復ログを 1 ファイル内で完結 -->

### YYYY-MM-DD <PR ID> で消化 (採用)

| 提案 | <PR ID> での消化内容 |
|---|---|

### 2026-05-19-part2 harness-meta 判定

採用 0 / 見送り 8 / 撤去 0 (本 PR scope: `plan.md §採番ライフサイクル` のみ採用、本 learning 内の 8 提案は全件後続持ち越し)

| 提案 ID (本 learning 内 順序) | 判定 | 持ち越し先 / 採用根拠 | 採用判定基準 |
|---|---|---|---|
| #1 `[rule]` harness-meta-criteria.md §分割粒度 表脚注に逆方向 cross-link 追加 | 見送り | 補助的な改善 (即時消化基準非該当)、次回 harness-meta-criteria.md 改修と同時消化候補 | 4 |
| #2 `[rule]` implementation-workflow.md §commit 分離規範 §例外 表に +50/+100/+200 行 3 段階境界例示 | 見送り | 補助的な例示追加 (現状規範で SoT 性確保)、運用熟成データ蓄積後 (3-5 PR 反復観測) で SoT 化推奨 | 1 |
| #3 `[rule]` implementation-workflow.md §commit 分離規範 §機械検証 N 段階表記化 | 見送り | A6 (Gradle カスタムタスク + 閾値運用熟成) スコープ | 2 |
| #4 `[rule]` retrospective-format.md part 分割基準 SoT 化 | 見送り | A4 (pr-poller / pr-retrospective Skill 本格化と統合) スコープ | 2 |
| #5 `[rule]` harness-meta-criteria.md §採用判定基準 表脚注に強化条件 (5 回反復) 運用例追加 | 見送り | 補助的な運用例引用、harness-meta-criteria.md §採用判定基準 表改修 PR と同時消化推奨 | 5 |
| #6 `[template]` harness.md PR テンプレ commit 構造セクション追加 | 見送り | template 構造変更は dry-run 必須条件該当、harness-meta-criteria.md §dry-run 必須条件「template 構造変更」に該当のため dry-run 先行で別 PR 起票 | 4 |
| #7 `[skill]` harness-meta Skill 表記揺れ防止 rubric 追加 | 見送り | A4 (harness-meta Skill 本格化) スコープ | 2 |
| #8 `[skill]` pr-retrospective Skill batch ブランチ集約 + week 跨り判定フェーズ追加 | 見送り | A4 (pr-poller / pr-retrospective Skill 本格化と統合) スコープ | 2 |

### YYYY-MM-DD <PR ID> で保留 (要再評価)

| 提案 | 保留理由 |
|---|---|

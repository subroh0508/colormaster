---
id: learning-pr-119
title: PR #119 レトロスペクティブ (A1 / ADR 0001-0027 一括起草)
type: learning
status: draft
related_pr: 119
related_plan: PLAN-001-adr-bootstrap
related_epic: EPIC-000-harness-foundation
generated_at: 2026-05-17T10:00:00Z
generator: pr-retrospective (skeleton、本ペインで手動代替実行)
---

# PR #119 レトロスペクティブ

> 生成: pr-retrospective Skill (skeleton 段階、A3 本格化前のため A2-1 完了 pane で手動代替) at 2026-05-17T10:00:00Z
> 関連 Plan: PLAN-001-adr-bootstrap / 関連 Epic: EPIC-000-harness-foundation
> 対象 PR: [docs(adr): scaffold ADR-0001-0027 (A1 / PLAN-001) #119](https://github.com/subroh0508/colormaster/pull/119) (squash merged at 2026-05-17T08:50:28Z、commit `7f155b5`、from head `8741cf1`)
> 差分: 31 ファイル / +4242 / -34 (4500 diff 行)

## ✅ Keep (継続したいこと)

- **27 ADR 一括起草で SSoT (`docs/harness/plan.md` §4) と完全整合**: ADR-0001 〜 ADR-0027 が plan.md §4.0.3 の一覧と一対一対応、★統合の経緯 (旧 0004 / 旧 0011 / 旧 0020 / 旧 0021 / 旧 0025 / 旧 0026 / 旧 0030) も明示。再生成可能な構造を確立。
- **全 ADR 冒頭 5 行 summary を厳守** (ADR 0027 / `.claude/rules/docs-structure.md` R-32 準拠)。AI lazy-load 戦略の最初の大型サンプルとして機能。
- **frontmatter 配列はすべて block 形式** で記述、`related_epics` / `related_plans` / `related_specs` の参照先 (EPIC-000 / PLAN-001) も実在。`.claude/rules/docs-structure.md` の block 形式必須規約の最初の大型適用例。
- **ADR 同士のクロスリファレンス約 90 件で broken link 0 件**: 認証/データ (0011↔0008↔0022↔0020)、ハーネスループ (0017↔0018↔0019↔0024)、フロント刷新 (0002↔0005↔0006↔0012)、ハイブリッドホスティング (0009↔0022) の 4 系統依存環がすべて閉じている。
- **code-reviewer Skill の 3 aspect 手動補助レビュー (spec-conformance / architecture / security) を独立サブエージェントで並列実行**、Critical 0 件で pass。残り 5 aspect (test-quality / performance / code-quality / visual-regression / design-tokens) は本 PR の性質 (docs 中心 / 実装コード変更ゼロ / A10 完了前) により skip 判定が明確化。
- **worktree 並列実装 (`feature/A1-adr-bootstrap`) が A2-1 (`feature/A2-rules-docs-extension`) と物理分離**で並走可能であることを実機で確認。B0 PR レトロ Try「巨大 PR の aspect 並列 review 入力分割」と整合。
- **PLAN-001 が `related_adrs: [ADR-0001..ADR-0027]` 全列挙**: A6 で導入予定の `docs/traceability.md` 自動生成 (Konsist / Gradle カスタムタスク経由) の入力として理想形。
- **ADR-0004 が三層指標 (Kover / Konsist Spec coverage / PITest) の総論 index として責務を絞り**、各論 (ADR-0013 / 0015 / 0016) に詳細を委譲する設計が plan.md §3.10 と整合、Goodhart's law 回避の説明も明確。
- **roadmap.md A1 status 更新 (proposed → in-progress) を append-only 原則で実施** (R-34 片方向ミラー遵守)、`着手順変更履歴` セクションに 2026-05-17 エントリを追記。

## ⚠️ Problem (詰まったこと / 制約)

- **SSoT 方向の循環参照** (consensus: architecture): ADR-0001 末尾の「`.claude/rules/adr.md` は本 ADR の SSoT」と `.claude/rules/adr.md` の「詳細は ADR 0001 を SSoT とする」が双方向宣言。ADR-0020/0021/0024/0025/0026/0027 ↔ 対応 rule も同様の循環。意図 (ADR=決定、rule=運用詳細) は読み取れるが、A6 機械検証 (Konsist / Gradle カスタムタスク) 導入前に表現を一方向に揃えないとトレーサビリティの強度が弱い。
- **Plan template に `type: docs` が欠落** (consensus: architecture / spec-conformance): PLAN-001 メモで発見。`.claude/rules/plan.md` (A2-2 で本格化予定) で type allow リストを定義する際に揃える必要あり。本 PR では PLAN-001 を `type: docs` で起票したが template との不一致を抱えたまま merge。
- **roadmap.md「完了根拠」表の B0 行が未追記** (consensus: architecture): A1 行は in-progress 更新したが、B0 PR (#117) merge 済みの完了根拠行が空欄のまま。`roadmap-tracker` Skill の Phase 8 自動起動が skeleton 段階で未稼働のため、人間レビュアの混乱を招きうる。
- **★統合マーカーの本文補足が ADR-0023 / ADR-0027 で薄い** (consensus: spec-conformance): ADR-0011 は統合経緯を本文で明示しているが、ADR-0023 (旧 0025 + 旧 0026) / ADR-0027 (旧 0020 + 旧 0030) では薄め。plan.md §4.0.3 統合履歴と一貫した補足があれば人間レビュアが過去議論を辿りやすい。
- **ADR-0020 表の経路 #10 で「PII 扱い」列に「✅ (sub claim)」と「PII | ❌」の二系統表記が混在** (consensus: security): A6 Konsist 正規化スクリプト作成時に `.claude/rules/pii.md` の lookup と一致させる注記がない。
- **ADR-0021 「絶対 commit してはいけないもの」リストが `.claude/rules/secrets.md` 現行記述より広い** (consensus: security): `*.pem` / `*.key` / `*.p12` / `.cloudflare/credentials` / `.gcloud/credentials` が ADR 側のみに追加。rule との非対称が SSoT 不整合。
- **ADR-0021 GIS client secret ローテーション根拠が R2 と非対称** (consensus: security): R2 は TTL 90 日 + 定期ローテだが、GIS は「Google Cloud Console の発行・失効に従う」のみ。漏洩疑い時対応のみで足る根拠が 1 行欠落。
- **`code-reviewer` Skill が skeleton 段階で 3 aspect しか実行できなかった**: ADR-0019 で定義された 8 aspect のうち test-quality / performance / code-quality は本 PR の性質で skip 妥当だが、visual-regression / design-tokens は A10 完了後 enable 待ち。A3 本格化前の暫定運用。
- **三層指標 (Line/Branch coverage / Spec coverage / Mutation score) が全て N/A**: Kover / Konsist `@Spec` / PITest が A7 未導入のため計測不能。本 PR は Kotlin code 変更ゼロのため指標差分も実質ゼロ。
- **`Co-Authored-By` の検証が hook 未実装**: AI コミット (`Claude Opus 4.7 (1M context) <noreply@anthropic.com>`) は人間レビュー任せ。誤って個人メアドを書く余地が残る。

## 🚀 Try (次回からの改善案)

- **A2 (`.claude/rules/*` 全ファイル本格化)** で SSoT 循環参照表現を整理: 「ADR = 決定の Single Source of Truth、rule = 運用詳細 (ADR を参照、逆方向 SSoT 宣言は禁止)」に統一する記述ルールを A2-2 で `.claude/rules/adr.md` に明文化、各 rule の冒頭文言を一方向化。
- **`.claude/rules/plan.md` (A2-2 で本格化) で `type` allow リストに `docs` を追加**: `docs(harness)` / `docs(adr)` 系の Plan を正式に位置付ける。`.github/PULL_REQUEST_TEMPLATE/docs.md` (B0 配置済) との対応も明文化。
- **roadmap-tracker Phase 8 自動同期で完了根拠表空欄補完を制度化**: A3 本格化時に「PR merge 後 N 時間以内に該当行の完了根拠列が空ならエラー」を `.claude/rules/roadmap.md` (A2-1 で手動マージ時更新ルール追記済、A2-3 で機械検証規約追加予定) に組み込む。
- **ADR-0023 / ADR-0027 の統合経緯を ADR-0011 同水準まで本文補足**: 旧 0025 + 旧 0026 = 新 0023 の合流理由、旧 0020 + 旧 0030 = 新 0027 の合流理由を Decision セクション直下に 3-5 行で追記。A2-2 (rules 実装系本格化) と同 PR で扱う or 別小型 PR。
- **ADR-0020 「PII 扱い」列の表記統一**: 二系統 (`✅ (sub claim)` vs `PII | ❌`) を `.claude/rules/pii.md` の lookup と整合する 1 系統に。A2-2 で `pii.md` 本格化と同 PR。
- **`.claude/rules/secrets.md` を ADR-0021 と同期** (項目追加: `*.pem` / `*.key` / `*.p12` / `.cloudflare/credentials` / `.gcloud/credentials`): A2-2 で `secrets.md` 本格化と同 PR。
- **ADR-0021 GIS client secret ローテーション根拠を 1 行補足**: 「GIS client secret は漏洩疑い時のみ Google Cloud Console で再発行、TTL なし運用は Google 側のローテーション機構に委譲」等を Decision に追記。
- **ADR ↔ rule のクロスリンク自動検証**: A6 で導入予定の Gradle カスタムタスク (commonmark-java + SnakeYAML) で「ADR の `related_rules` が指す rule ファイルが実在」「rule の `related_adrs` が指す ADR ファイルが実在」を機械検証。
- **`Co-Authored-By` 検証を commit-msg hook 拡張**: A3 / A6 で `@noreply.anthropic.com` / `@users.noreply.github.com` のみ許可するパターン検証を追加。`.claude/rules/commit-message.md` (A2-1 で新規作成) の Gotchas で「A3 で機械化」を予告済。
- **pr-retrospective を pr-poller から自動 trigger する MVP を A3 / A4 で実装**: A1 (PR #119) / A2-1 (PR #121) のレトロが merge から数十分遅れで手動代替されている (本ファイル含む) ことを R-12 (learning ロスト対策) の課題として認識。

## 📊 指標

A1 PR は実装コード差分ゼロ (`*.kt` / `*.kts` / `*.sq` / `Dockerfile` / `build.gradle*` 変更なし)、三層テスト品質基盤 (Kover / Konsist Spec / PITest) は A7 で導入予定のため Before/After 差分は計測不能 (N/A)。

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| Line coverage | N/A | N/A | — | Kover 未導入 (A7 で導入予定) |
| Branch coverage | N/A | N/A | — | 同上 |
| Spec coverage | N/A | N/A | — | Konsist `@Spec` 未導入 (A7 で導入予定) |
| Mutation score | N/A | N/A | — | PITest 未導入 (A7 で導入予定) |
| Lint 違反数 | N/A | N/A | — | Spotless / ktlint / detekt / markdownlint-cli2 未導入 (A6 で導入予定) |
| ファイル数 (差分) | (A1 前) | +27 ADR + 1 PLAN-001 + 1 docs/plans/INDEX 更新 + 1 docs/adr/README 拡充 + 1 docs/harness/roadmap.md 更新 | 31 changed | 全て docs |
| 行数 (差分) | (A1 前) | +4242 / -34 | net +4208 | 全て docs |
| ADR 数 (`docs/adr/`) | 0 | 27 | +27 | 全件 `accepted` で起票 |
| Plan 数 (`docs/plans/`) | 0 | 1 (PLAN-001) | +1 | type: docs、related_adrs 27 件全列挙 |
| ★統合 ADR 数 | 0 | 3 (ADR-0011 / 0023 / 0027) | +3 | plan.md §4.0.3 統合履歴と一致 |
| code-reviewer aspect 実行 (手動補助) | — | 3/3 pass (spec-conformance / architecture / security) | — | 残 5 aspect は skip 妥当判定 |
| Critical findings | — | 0 | — | merge readiness 充足 |
| Recommended findings | — | 10 (A2 / A6 で対応) | — | 本 retrospective の Problem / Try に反映 |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造 -->

- [ ] `[rule]` SSoT 方向の循環参照を整理: 「ADR = SSoT、rule = 運用詳細で ADR 参照のみ、逆方向 SSoT 宣言禁止」を `.claude/rules/adr.md` (A2-2 で本格化) に明文化、各 rule (`.claude/rules/{pii,secrets,db-protection,mcp-usage,skill-authoring,harness-evolution,docs-structure}.md` 等の B0 雛形) の冒頭文言を一方向化
- [ ] `[template]` Plan template (`.claude/rules/plan.md` A2-2 で本格化) の `type` allow リストに `docs` を追加、`.github/PULL_REQUEST_TEMPLATE/docs.md` との対応を明文化
- [ ] `[skill]` `roadmap-tracker` Phase 8 自動同期で「PR merge 後 N 時間以内に該当行の完了根拠列が空ならエラー」を A3 本格化時に追加、`.claude/rules/roadmap.md` 機械検証規約 (A2-3) と整合
- [ ] `[rule]` ADR-0023 (旧 0025 + 旧 0026 統合) / ADR-0027 (旧 0020 + 旧 0030 統合) の統合経緯を ADR-0011 同水準まで本文補足 (Decision セクション直下 3-5 行)
- [ ] `[rule]` ADR-0020 「PII 扱い」列の二系統表記 (`✅ (sub claim)` vs `PII | ❌`) を `.claude/rules/pii.md` lookup と整合する 1 系統に統一 (A2-2 で `pii.md` 本格化と同 PR)
- [ ] `[rule]` `.claude/rules/secrets.md` を ADR-0021 と同期 (項目追加: `*.pem` / `*.key` / `*.p12` / `.cloudflare/credentials` / `.gcloud/credentials`)、A2-2 で `secrets.md` 本格化と同 PR
- [ ] `[rule]` ADR-0021 GIS client secret ローテーション根拠を 1 行補足 (「漏洩疑い時のみ Google Cloud Console で再発行、TTL なし運用は Google 側機構に委譲」等)
- [ ] `[rule]` ADR ↔ rule のクロスリンク自動検証を A6 Gradle カスタムタスクに追加 (commonmark-java + SnakeYAML で `related_rules` / `related_adrs` の参照先実在チェック)
- [ ] `[skill]` `commit-msg` hook 拡張で `Co-Authored-By` の `@` 部を `@noreply.anthropic.com` / `@users.noreply.github.com` のみ許可 (A3 / A6、`.claude/rules/commit-message.md` Gotchas で予告済)
- [ ] `[skill]` `pr-retrospective` Skill を `pr-poller` から自動 trigger する MVP を A3 / A4 で実装 (本ファイルの手動代替遅延を解消、R-12 対策)
- [ ] `[rule]` ADR 索引の起票根拠 (§4.5) を表に組み込む — **A2-1 で消化済 ✅** (commit `feb41b5`、`docs/adr/README.md` 拡充)

## 📝 harness-meta フィードバック

<!-- harness-meta が後から追記。提案 → 結果の往復ログを 1 ファイル内で完結 -->

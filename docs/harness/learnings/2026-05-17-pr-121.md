---
id: learning-pr-121
title: PR #121 レトロスペクティブ (A2-1 / A1 レトロ即時消化 + EPIC-A2 起票)
type: learning
status: draft
related_pr: 121
related_plan: —
related_epic: EPIC-A2-rules-docs-extension
generated_at: 2026-05-17T10:05:00Z
generator: pr-retrospective (skeleton、本ペインで手動代替実行)
---

# PR #121 レトロスペクティブ

> 生成: pr-retrospective Skill (skeleton 段階、A3 本格化前のため本ペインで手動代替) at 2026-05-17T10:05:00Z
> 関連 Plan: なし (1 PR 完結のため EPIC-A2 直下、Plan 不要) / 関連 Epic: EPIC-A2-rules-docs-extension
> 対象 PR: [feat(harness): A2-1 A1 レトロ即時消化 + EPIC-A2 起票 #121](https://github.com/subroh0508/colormaster/pull/121) (squash merged at 2026-05-17T09:50:45Z、commit `feb41b5`、from head `2e820bc`)
> 差分: 25 ファイル / +789 / -94 (1252 diff 行)、commits: `25b690e` 初回 + `2e820bc` fix loop

## ✅ Keep (継続したいこと)

- **EPIC-A2 5 PR 分割 (A2-1〜A2-5) で B0 (96 files / +5408 行) を超える A2 全体規模を制御**: A1 レトロ Try「巨大 PR の aspect 並列 review における入力分割」と整合。A2-1 を 25 ファイル / +789 行に収め、code-reviewer 4 aspect 並列の subset 読み切り粒度を維持。
- **`code-reviewer` Skill (skeleton) を `general-purpose` subagent 並列で代替実行**: 4 aspect (spec-conformance / architecture / security / code-quality) を独立 system prompt で並列起動 (Anthropic Evaluator 独立性原則 R-13 準拠)、Coordinator が集約して PR コメント post + Merge readiness 判定。A3 本格化前の暫定運用として機能。
- **fix loop (commit `2e820bc`) で Critical 1 件 (architecture: plan.md L1125 SSoT 矛盾) を解消**: Coordinator が「修正後 Ready」判定可能な粒度の binary checklist (各 aspect ≥5 項目、計 29 項目) を運用、merge ready 化のターンアラウンドが短い。
- **A1 レトロ 15 提案中 11 件を 1 PR で消化 (採用率 73%)**: `📝 harness-meta フィードバック` セクションに採用 11 件 / 見送り 2 件 / 保留 1 件の往復ログを記録、`.claude/rules/retrospective-format.md` 設計と整合。
- **PR テンプレ `harness.md` 新規追加で `branch-naming` の `harness/<purpose>` 認定との整合**: 対象 PR / KPT 要約 / ハーネス改善提案件数セクションを含む、harness-meta / pr-retrospective / harness-evolution PR の標準フォーマット確立。本ファイル含む後続 learning PR / roadmap mirror PR / 本格化 PR でも再利用予定。
- **worktree 並列 (`feature/A2-rules-docs-extension`) で本格化フェーズの並走基盤確立**: A2-1 merge 後すぐに別 pane で `feature/A2-2-rules-impl` / `feature/A2-4-docs-core` / `feature/A2-5-docs-arch-api` を `git worktree add` で物理分離して並走着手、touch ファイル衝突なし。
- **plan.md L1125 直接修正で SSoT 整合化 (Critical 解消)**: `no-firebase.md (旧 firebase-boundary.md を改名)` を「二段運用、改名は A3 で再評価。EPIC-A2 `decisions.md` 参照」に書き換え、plan.md 内部矛盾 (L1093 で `firebase-boundary.md` を「既存」と残していた) も同時解消。
- **subject 長 50 → 72 字推奨 / 100 字 hard limit + 72 字超 warning 緩和** (`scripts/install-git-hooks.sh`): 過去コミット `feat(harness): A1 ADR 0001-0027 一括起草 (#119)` 等の実体と整合、`.claude/rules/commit-message.md` 新規ルールと同 PR で hook 更新で SSoT 不整合を残さない。
- **`.claude/rules/template-language.md` の `paths` 削除で「常時ロード」宣言と実体一致**: A1 レトロ Problem #2 解消、rules-index.md / CLAUDE.md の常時ロード 4 件 (`pii` / `secrets` / `rules-index` / `template-language`) リストと frontmatter 実体が完全一致。
- **`.claude/rules/rules-index.md` status 語彙を `skeleton (B0)` / `planned (X)` / `living` の 3 種類に統一**: 「既存」誤宣言 13+ 件を完全除去、A2-2 / A2-3 で参照する状態判定の根拠を機械可読化、後続 PR が status 整合を自己確認できる。

## ⚠️ Problem (詰まったこと / 制約)

- **B0 由来の latent contradiction (plan.md L1093 + L1125 の `firebase-boundary.md` / `no-firebase.md` 二系統表記) が A2-1 着手まで未検出**: code-reviewer architecture aspect が指摘するまで気付けなかった。rules-index と plan.md の状態差分を機械検出する仕組みが A6 まで欠落。
- **`.claude/skills/{pr-poller,pr-retrospective,roadmap-tracker,code-reviewer}` が全て skeleton で手動代替が必要**: 本 PR の review / merge 後の roadmap mirror (PR #122) / 本 retrospective 生成いずれも手動運用、`implementation-workflow` Phase 6 / 7 / 8 のオーケストレーションが Skill 駆動になっていない。A3 完了まで継続。
- **master の PR #120 (A1 roadmap mirror) と本 PR の `docs/harness/roadmap.md` で 3 箇所コンフリクト**: rebase で解消したが、`A2-1 着手当時の master = 7f155b5` と `push 直前の master = 3c45bfd` で `docs/harness/roadmap.md` の A1 完了根拠記述が同時並行で進化していたため。worktree 起動時の fetch 漏れ。
- **A2-1 で扱った `commit-message.md` 新規作成は本来 A2-3 の範疇** (rules-index で `skeleton (A2-1 で新規作成、A2-3 で本格化)` と再分類): A1 レトロ提案「subject 長 50 → 72 字推奨」と `scripts/install-git-hooks.sh` 整合性のため A2-1 で同時実装したが、PR スコープ膨張気味。EPIC-A2 decisions.md に判断根拠を記録済。
- **plan.md 直接修正 (Critical 解消のための L1125 書き換え) を A2-1 PR 内で行った**: SSoT 改訂規約上「plan.md は B0 で merge 済の安定 doc、改訂は別 PR が望ましい」という規律と緊張関係。本 PR の review コメントで人間判断を依頼した (`AI レビューの指摘で十分でしょうか?` セクション)。
- **A2-1 commit subject (`feat(harness): A2-1 A1 レトロ即時消化 + EPIC-A2 起票`、41 文字、日本語混在) が `commit-message.md` 「英語推奨」と齟齬**: 過去コミット実体に合わせて Phase A 期間中の経過措置を Gotchas に追加 (commit `2e820bc`)、機械強制は A6 で再評価。
- **三層指標 (Line/Branch coverage / Spec coverage / Mutation score) が全て N/A**: 本 PR は Kotlin code 変更ゼロのため指標差分も実質ゼロ。A7 で Kover / Konsist `@Spec` / PITest 導入まで継続。
- **`docs-structure.md:116-117` の frontmatter サンプル中 flow 形式 `[ADR-0001, ADR-0011]` が「block 形式必須」規約と表面的衝突して見える** (spec-conformance aspect 指摘): B0 PR #117 由来の NG 比較例だが、コメント注釈なし。A2-3 で `docs-structure.md` 本格化時に「NG 例」明示。
- **`Co-Authored-By` の機械検証が hook 未実装**: AI コミット (`Claude Opus 4.7 (1M context) <noreply@anthropic.com>`) は人間レビュー任せ、誤って個人メアドを書く余地が残る (security aspect 指摘、A3 / A6 で対応)。
- **`.dockerignore` が未配置 (Dockerfile 未配置のため意図的に保留)**: db-protection.md に「A6 / Dockerfile 配置時に必須項目 5 件 (`data/users.db*` / `.env*` / `*-credentials.json` / `service-account*.json` / `.claude/oauth-tokens*`) を含む `.dockerignore` を同 PR 配置」と TODO 明記したが、機械検証実装は A6。

## 🚀 Try (次回からの改善案)

- **B0 / 既存 docs の latent contradiction を A2-2 / A2-3 で総点検**: rules-index.md status と plan.md `★新規` / `既存` ラベルの整合、ADR ↔ rule のクロスリンク実在、frontmatter `related_*` 参照先実在を全 rule / 全 docs で grep-based に検証。A6 で Gradle カスタムタスク化。
- **`pr-retrospective` を `pr-poller` から自動 trigger する MVP を A3 / A4 で実装**: 本ファイル (PR #119 / #121 のレトロ) が merge から数十分〜1 時間遅れで手動代替されている現状を R-12 (learning ロスト対策) の課題として認識、`gh pr list --state merged --since` ベースで未起票 PR を検出して `harness/learnings-batch-YYYY-WW` ブランチへ自動追記。
- **worktree 起動時の `git fetch origin master` を `.claude/rules/implementation-workflow.md` Phase 0 に明文化**: 本 PR で master との並行進化による rebase コンフリクトが発生したため、Phase 0 で必ず最新 master を fetch + base にする規約を A2-3 で本格化時に追加。
- **A2-2 / A2-3 で `rules-index.md` の連続編集による rebase 競合を回避**: A2-2 / A2-3 を **直列実行** (A2-2 マージ後に A2-3 着手) し、rules-index.md の status 表更新を 1 PR ごとに分離。EPIC-A2 roadmap.md の gantt に直列指定済。
- **code-reviewer の binary checklist (29 項目) を A3 本格化時に `code-reviewer-aspects.md` で確定**: 本 PR で実行した 4 aspect × 7-8 項目の binary yes/no 評価を、`.claude/rules/code-reviewer-aspects.md` (B0 雛形) の本格化と同 PR で組み込み、各 aspect の最低 5 項目要件 (R-13) を明示。
- **plan.md SSoT 改訂の規約を明文化**: 「plan.md の改訂は (a) ADR 起票基準 2 項目以上を満たすなら ADR を起票して plan.md を改訂、(b) 起票基準 2 項目未満なら EPIC `decisions.md` で記録して plan.md は最小修正、の二択」を `.claude/rules/adr.md` (A2-2 本格化) に追加。
- **EPIC 起票時の `decisions.md` に Critical 解消の判断ログを残す規約**: 本 PR の plan.md L1125 修正は EPIC-A2 `decisions.md` に記録済 (firebase-boundary 二段運用、commit-message subject 長緩和、template-language paths 削除)。`.claude/rules/epic.md` (A2-2 本格化) に「EPIC 配下 PR の Critical 解消は decisions.md に記録」を明文化。
- **`code-reviewer` の visual-regression / design-tokens aspect 有効化手順 4 ステップを A10 完了 EPIC で実行**: 本 PR で `.claude/skills/code-reviewer/SKILL.md` Gotchas に明記済 (status `A10 完了後 enable` → `active` 書換、Coordinator 並列起動対象追加、binary checklist 確定、ADR 0023/0027 リンクバック)、A10 マージコミットを参照する PR で実施。
- **Renovate ラベル PR (PR #112-115 等) の `dependency-upgrade` Skill 起動を A4 で実装**: 現状 5 件の Open Renovate PR (logback / koin / gradle / lifecycle-viewmodel-compose / androidx.lifecycle) が pr-poller 起動を待っている。A3 / A4 の優先課題。

## 📊 指標

A2-1 PR は実装コード差分ゼロ (rules / docs / template / Skill SKILL.md / Bash script のみ)、三層テスト品質基盤 (Kover / Konsist Spec / PITest) は A7 で導入予定のため Before/After 差分は計測不能 (N/A)。

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| Line coverage | N/A | N/A | — | Kover 未導入 (A7 で導入予定) |
| Branch coverage | N/A | N/A | — | 同上 |
| Spec coverage | N/A | N/A | — | Konsist `@Spec` 未導入 (A7 で導入予定) |
| Mutation score | N/A | N/A | — | PITest 未導入 (A7 で導入予定) |
| Lint 違反数 | N/A | N/A | — | Spotless / ktlint / detekt / markdownlint-cli2 未導入 (A6 で導入予定) |
| ファイル数 (差分) | (A2-1 前) | new 8 + modified 16 + (fix commit で modified 6 重複) = 25 changed | 25 changed | rules / docs / template / Skill SKILL.md / Bash script |
| 行数 (差分) | (A2-1 前) | +789 / -94 (squash merge feb41b5) | net +695 | 全て docs / rules / template / Bash |
| commits 数 (squash 前) | (A2-1 前) | 2 (`25b690e` 初回 + `2e820bc` fix loop) | +2 | code-reviewer 集約結果の修正を別 commit で記録 |
| EPIC 数 (`docs/epics/`) | 1 (EPIC-000) | 2 (+ EPIC-A2-rules-docs-extension) | +1 | 5 ファイル構成 (README / roadmap / open-questions / decisions / progress) |
| rule 数 (`.claude/rules/`) | 19 | 20 (+ commit-message.md) | +1 | 他 5 rule 修正 (rules-index / template-language / mcp-usage / db-protection / roadmap) |
| PR テンプレ数 (`.github/PULL_REQUEST_TEMPLATE/`) | 5 (feature / bugfix / refactor / dependency-upgrade / docs) | 6 (+ harness.md) | +1 | feature / bugfix も A7 / A10 注記で更新 |
| code-reviewer aspect 実行 (subagent 並列) | — | 4/4 pass (spec-conformance / architecture / security / code-quality)、修正後 | — | 残 4 aspect (test-quality / performance / visual-regression / design-tokens) は skip 妥当 |
| Critical findings | — | 1 → 0 | -1 (修正済) | architecture: plan.md L1125 SSoT 矛盾 → commit `2e820bc` で解消 |
| Recommended findings | — | 13 (本 PR で 6 消化、残 7 件は A2-2 / A2-3 / A3 へ) | — | spec-conformance 3 / architecture 3 / security 2 / code-quality 5 |
| A1 レトロ提案消化件数 | 0 / 15 | 11 / 15 (採用率 73%) | +11 | 残 4 件: pr-retrospective / pr-poller 本格化 (A3 / A4)、learnings/template.md (A3) |
| binary checklist 通過率 | — | 29 / 29 ✅ (修正後) | — | 4 aspect × 7-8 項目 |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造 -->

- [ ] `[rule]` B0 / 既存 docs の latent contradiction (rules-index status と plan.md `★新規` / `既存` ラベルの整合) を A6 で Gradle カスタムタスク自動検証、A2-2 / A2-3 では grep-based に手動チェック
- [ ] `[skill]` `pr-retrospective` を `pr-poller` から自動 trigger する MVP を A3 / A4 で実装 (本ファイルの手動代替を解消、R-12 対策)
- [ ] `[rule]` `.claude/rules/implementation-workflow.md` (A2-3 本格化) の Phase 0 に「worktree 起動時に `git fetch origin master` + base 更新」を明文化、本 PR で発生した PR #120 との rebase 競合を再発防止
- [ ] `[skill]` `code-reviewer` の binary checklist (4 aspect × 7-8 項目 = 29 項目) を A3 本格化時に `.claude/rules/code-reviewer-aspects.md` で確定、各 aspect の最低 5 項目要件 (R-13) を満たす形で明示
- [ ] `[rule]` `.claude/rules/adr.md` (A2-2 本格化) に「plan.md SSoT 改訂は (a) ADR 起票 + plan.md 改訂、または (b) EPIC decisions.md + plan.md 最小修正 の二択」規約を追加
- [ ] `[rule]` `.claude/rules/epic.md` (A2-2 本格化) に「EPIC 配下 PR の Critical 解消は `decisions.md` に判断ログを残す」規約を追加
- [ ] `[rule]` `.claude/rules/docs-structure.md:116-117` の frontmatter サンプル中 flow 形式 `[ADR-0001, ADR-0011]` に「NG 例 (block 形式必須)」コメント明示 — A2-3 で本格化と同 PR
- [ ] `[rule]` `docs/adr/README.md` の関連 rule 列で `rule` と `runbook` (ADR 0014 の `local-imasparql.md` 等) を別列に分離 — A2-2 で対応
- [ ] `[skill]` `commit-msg` hook 拡張で `Co-Authored-By` の `@` 部を `@noreply.anthropic.com` / `@users.noreply.github.com` のみ許可 — A3 / A6 で実装
- [ ] `[skill]` Gradle カスタムタスクで `.dockerignore` が 5 必須項目 (`data/users.db*` / `.env*` / `*-credentials.json` / `service-account*.json` / `.claude/oauth-tokens*`) を含むことを機械検証 — A6 で実装 (本 PR で `db-protection.md` に明文化済)
- [ ] `[rule]` `.claude/rules/commit-message.md` 本文 vs Gotchas での「英語」記述の位置整理 — A2-3 (本格化) で扱う
- [ ] `[template]` PR テンプレ `feature.md` / `bugfix.md` の注記形式 (`- **注**: ...` 2 行) を blockquote または行末 `(注: ...)` に統一 — A2-3 で扱う
- [ ] `[rule]` EPIC-A2 README.md `related_adrs` 7 件のうち補助 ADR (0001 / 0020 / 0021) を本文側リンクに移して軽量化 — 任意
- [ ] `[skill]` Renovate ラベル PR (現状 Open 5 件) の `dependency-upgrade` Skill 起動を A4 で実装、`pr-poller` の Phase 2 機能として確立

## 📝 harness-meta フィードバック

<!-- harness-meta が後から追記。提案 → 結果の往復ログを 1 ファイル内で完結 -->

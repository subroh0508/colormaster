---
id: learning-pr-159
title: PR #159 レトロスペクティブ (harness-meta Skill 初回ドッグフード)
type: learning
status: draft
related_pr: 159
related_plan: —
related_epic: EPIC-A3-skill-suite-extension
generated_at: 2026-05-19T18:00:00+09:00
generator: pr-retrospective (skeleton、本ペインで手動代替実行)
---

# PR #159 レトロスペクティブ (harness-meta Skill 初回ドッグフード)

> **5 行以内 summary**: A3-5 完成の `harness-meta` Skill 初回ドッグフード PR。改修候補 8 件
> (PR #147 / #153 / #158 batch retro 由来) を採用 3 件 / 見送り 5 件 / 撤去 0 件に分類、
> 採用 3 件 (#1 動詞辞書 21 動詞 / #5 `--body-file` 強制 / #8 `--template` 排他) を 3 SoT
> ファイルに反映、見送り 5 件は 10 learning ファイルの 📝 フィードバックに追記。本 PR 起票
> 自身の dogfooding で gh CLI `--template` / `--body-file` 排他の実仕様判明。

> 生成: pr-retrospective (skeleton、本ペインで手動代替実行) at 2026-05-19T18:00:00+09:00
> 関連 Plan: — / 関連 Epic: EPIC-A3-skill-suite-extension (A3-5 で完成した `harness-meta` Skill の初回実行)
> 対象 PR: [docs(harness): harness-meta 初回ドッグフード (改修候補 3 件 SoT 反映 + 5 件見送りフィードバック) #159](https://github.com/subroh0508/colormaster/pull/159) (squash merged at 2026-05-17T23:52:36Z、commit `5532beb`、from head `harness/harness-meta-batch-20260518`)
> 差分: 13 ファイル / +173 / -46 (219 diff 行)、commits: 2 (initial + fix loop)

## ✅ Keep (継続したいこと)

- **A3-5 完成 harness-meta Skill の手動代替実行で初回ドッグフード達成**: PR #147 / #153 / #158 batch retro の改修候補 8 件を `.claude/rules/harness-meta-criteria.md` §採用 / 見送り / 撤去判定基準で分類し、採用 3 件を 3 SoT ファイル (`orchestrator/SKILL.md` + `implementation-workflow.md` + `pr-template.md`) に反映。手動代替でも判定基準に従えば再現性のある分類ができる (consensus: spec-conformance + architecture)
- **本 PR 起票自身の dogfooding で `--template` / `--body-file` 排他の実仕様判明**: 指示書の `--template harness.md + --body-file <path>` 同時指定例は gh CLI 実仕様で Exit 1 (`` `--template` is not supported when using `--body` or `--body-file` ``)、本 PR では排他関係に基づき `--body-file` 一択を本リポジトリ既定として SoT 化、本 PR 自身の `gh pr create` も `--body-file` 一択で起票 (改修候補 #8 self-validation)
- **見送り 5 件 (#2 / #3 / #4 / #6 / #7) を 10 learning ファイルの `📝 harness-meta フィードバック` 「見送り」表に追記**: cross-reference 形式で「見送り理由 + 後続持ち越し先」を明示、R-12 (learning ロスト対策) の往復ログを 1 ファイル内で完結 (consensus: spec-conformance)
- **見送り判定の根拠が明確**: #2 Monitor dedup v3 (escalate → PR #160 dry-run へ持ち越し) / #3 gh pr merge orchestrator 代行 (orchestrator SKILL.md 包括更新 PR #161 へ持ち越し) / #4 per-task pane self-/exit 不可 (同上) / #6 R-15 一括事前承認 (同上) / #7 worktree 並列 lock (同上) と後続 PR (#160 / #161) で消化先を明確化 (consensus: architecture)
- **採用 3 件の SoT ファイル分散**: `[skill]` 1 件 + `[rule]` 2 件で責務分離維持、`harness-meta` の判定基準に従い「Skill 改修」「rule 改修」を区別、harness-evolution との二系統補完 (ADR-0026) も整合 (consensus: architecture)
- **PR description に dogfooding 検出結果を明示記載**: 「指示書の patch 案を排他関係に基づき統合改訂し、`--body-file` 一択を本リポジトリ既定として SoT 化した」と起票時に判明した実仕様を後続レビュー / retro が辿れる形で記録
- **`.claude/rules/retrospective-format.md` §フィードバックフォーマット 準拠**: 採用 / 見送り / 保留の 3 表構造を維持、プレースホルダ見出し / 表ヘッダを実値 (`### 2026-05-18 PR #159`) に書き換え、保留 (要再評価) 表は本 PR では使わずプレースホルダ残置 (後続 harness-meta 実行用)
- **fix loop 1/3 で 2 commits 完走**: code-reviewer 4 aspect (spec / arch / security / code-quality) Critical 0、initial commit + fix loop 1 で完走、orchestrator 事前承認テキストで classifier ブロックゼロ self-merge

## ⚠️ Problem (詰まったこと / 制約)

- **指示書の patch 案が gh CLI 実仕様で Exit 1**: 改修候補 #8 (PR #156 retro Try #1) の完全コマンド例 `--template harness.md + --body-file <path>` 同時指定は実装上不可、本 PR 起票 dogfooding で初めて実証。PR #156 retro Try #1 が SoT として `harness-meta-criteria.md` に取り込まれる前に gh CLI 仕様検証が不足していた
- **見送り 5 件全件が後続 PR #161 へ持ち越し**: 改修候補 #3 / #4 / #6 / #7 (orchestrator skill 包括更新) が PR #161 1 件に集約持ち越し、PR #160 (Monitor dedup dry-run) + PR #161 (orchestrator skill 包括更新) の 2 PR で見送り 5 件全件を消化する設計だが、本 PR 時点では 1 PR 集約コストが残存
- **harness-meta Skill が手動代替実行**: A4 自動化前のため、改修候補 8 件の自動分類 + 自動 SoT 反映 + 自動 📝 追記の全プロセスが手動。8 件の判定根拠 (採用判定基準 1-5 のどれに該当) + 後続持ち越し先決定が PR description に明示記載されていないと再現困難
- **PR description 内のテンプレ構造を手動で揃えて流し込み**: `.github/PULL_REQUEST_TEMPLATE/harness.md` の §必須セクションを `--template` なしで手動コピーする運用、テンプレ drift リスク残存。テンプレ更新時に既存 PR description との差分が機械検証されない
- **改修候補 #8 自身が本 PR 内で部分的に self-reference**: 「`--body-file` 一択を SoT 化」と「本 PR 自身が `--body-file` 一択で起票」が同 PR 内で実証、self-reference 構造の妥当性は人間レビュー任せ
- **採用判定基準 1 (複数 PR 反復) と基準 4 (Critical 派生) の重み付けが暗黙**: #1 (動詞辞書) は基準 1、#5 (heredoc → `--body-file`) は基準 4、#8 (`--template` 排他) は基準 4 と判定根拠が異なるが、判定アルゴリズムの SoT が `harness-meta-criteria.md` に明示されていない
- **harness-meta の dry-run 必須条件該当判定が本 PR では未実施**: 採用 3 件は Skill フロー追加なし / template 構造変更なし / rule 間 SoT 反転なし / 採用判定基準改修なしのため dry-run 不要と判定したが、判定プロセスの記録が PR description に明示されていない

## 🚀 Try (次回からの改善案)

- **gh CLI 仕様検証を harness-meta SoT 反映前のチェックリストに追加**: `--template` / `--body-file` 排他のような実仕様が改修候補に含まれる場合、SoT 反映前に gh CLI 実行で検証 (本 PR の dogfooding 検出を SoT 化)、`.claude/rules/harness-meta-criteria.md` §dry-run 必須条件 に「外部ツール仕様依存の提案」追加
- **手動 harness-meta 実行時の判定プロセス記録テンプレ追加**: 採用判定基準 1-5 のどれに該当 / dry-run 必須条件該当判定 / 後続持ち越し先決定 の 3 軸を PR description に表形式で明示するテンプレを `.github/PULL_REQUEST_TEMPLATE/harness.md` に追加
- **A4 (harness-meta 自動化) で gh CLI 仕様検証を自動化**: 採用候補 patch を機械的に gh CLI で dry-execute (`--dry-run` 相当オプションがなければ subprocess で実行 + stderr 検査)、Exit 1 検出時は採用判定基準 4 で Critical 扱い → 自動 dry-run 起票
- **PR description テンプレ流し込みの drift 検出機構**: `.github/PULL_REQUEST_TEMPLATE/harness.md` 更新時に既存 PR description との §必須セクション差分を機械検証、A6 で Gradle カスタムタスク追加候補
- **harness-meta dogfooding を A4 完了後に N=10 で再計測**: 本 PR (N=1 manual) の判定再現性を A4 自動化後に N=10 retro batch で再計測、`harness-meta-criteria.md` §dry-run 必須条件 N 段階表記 (N=1 暫定 / N=3-5 ✅ / N=10 確定) に従い昇格
- **見送り 5 件の集約消化 PR を 2 PR 分割の規範化**: 本 PR の見送り 5 件は PR #160 + PR #161 で 2 分割消化、「dry-run 起票 PR (#160)」と「Skill SoT 反映 PR (#161)」の責務分離パターンを `.claude/rules/harness-meta-criteria.md` §持ち越し先決定 に SoT 化

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| Line coverage | N/A | N/A | N/A | Kover (A7 完了後)、本 PR は実装コード変更ゼロ |
| Branch coverage | N/A | N/A | N/A | 同上 |
| Spec coverage | N/A | N/A | N/A | Konsist `@Spec` (A7 完了後) |
| Mutation score | N/A | N/A | N/A | PITest (A7 完了後) |
| Lint 違反数 | N/A | N/A | N/A | Spotless / ktlint / detekt / markdownlint-cli2 (A6 完了後) |
| **PR 固有指標** | | | | |
| 変更ファイル数 | 0 | 13 | +13 | rule 2 + skill 1 + learning 📝 追記 10 |
| 追加行数 | 0 | 173 | +173 | orchestrator SKILL.md §動詞辞書 / §prefix / §regex + implementation-workflow.md Phase 5 / pr-template.md §gh pr create / learning 📝 |
| 削除行数 | 0 | 46 | +46 | placeholder 表ヘッダ / 既存 §gh pr create 旧記述 |
| commits | 0 | 2 | +2 | initial + fix loop 1 |
| 改修候補処理数 | 0 | 8 | +8 | 採用 3 + 見送り 5 + 撤去 0 + 保留 0 |
| harness-meta 初回ドッグフード達成度 | N/A | 8/8 | +8 | 全 8 候補を判定基準で分類完了 |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造 -->

- [ ] `[rule]` `.claude/rules/harness-meta-criteria.md` §dry-run 必須条件 に「外部ツール仕様依存の提案 (gh CLI / git / コマンドオプション排他関係 等)」追加。本 PR の `--template` / `--body-file` 排他のような実仕様検証を SoT 反映前に必須化、採用判定基準 4 (Critical 派生) 該当
- [ ] `[template]` `.github/PULL_REQUEST_TEMPLATE/harness.md` に「採用判定根拠」テンプレ追加 (採用判定基準 1-5 のどれに該当 / dry-run 必須条件該当判定 / 後続持ち越し先決定 の 3 軸表)。本 PR では手動記述、採用判定基準 2 (A4 で予定) 該当
- [ ] `[skill]` `harness-meta` Skill (A4 本格化時) に gh CLI 仕様検証を自動化 (採用候補 patch を機械的に dry-execute、Exit 1 検出時は Critical 扱い → 自動 dry-run 起票)。本 PR で手動検証、採用判定基準 2 (A4 で予定) 該当
- [ ] `[rule]` `.claude/rules/harness-meta-criteria.md` §持ち越し先決定 に「dry-run 起票 PR / Skill SoT 反映 PR の責務分離」を SoT 化。本 PR の見送り 5 件を PR #160 + PR #161 で 2 分割消化したパターンを規範化、採用判定基準 1 (本 PR + 後続 PR で反復見込み) 該当
- [ ] `[skill]` `harness-meta` Skill に判定プロセス記録の自動 PR description 生成を実装。手動代替時に判定根拠が暗黙化するリスクを予防、採用判定基準 2 (A4 で予定) 該当

## 📝 harness-meta フィードバック

<!-- harness-meta が後から追記。提案 → 結果の往復ログを 1 ファイル内で完結 -->

### YYYY-MM-DD <PR ID> で消化 (採用)

| 提案 | <PR ID> での消化内容 |
|---|---|

### YYYY-MM-DD <PR ID> で見送り (後続フェーズへ)

| 提案 | 見送り理由 / 移行先 |
|---|---|

### YYYY-MM-DD <PR ID> で保留 (要再評価)

| 提案 | 保留理由 |
|---|---|

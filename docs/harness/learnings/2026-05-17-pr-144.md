---
id: learning-pr-144
title: PR #144 レトロスペクティブ (orchestrator skill 配置 / cmux 並列 orchestration、A2 follow-up)
type: learning
status: draft
related_pr: 144
related_plan: —
related_epic: EPIC-A2-rules-docs-extension
generated_at: 2026-05-18T00:30:00+09:00
generator: pr-retrospective (skeleton、本ペインで手動代替実行)
---

# PR #144 レトロスペクティブ

> 生成: pr-retrospective Skill (skeleton 段階、A3 本格化前のため本ペインで手動代替) at 2026-05-18T00:30:00+09:00
> 関連 Plan: なし (Plan を経由しない harness follow-up、EPIC-A2 `completed` 昇格後の独立 PR) / 関連 Epic: EPIC-A2-rules-docs-extension (A2 follow-up、A3 専用 Skill 群実装の前段)
> 対象 PR: [feat(harness): orchestrator skill 配置 (cmux 並列 orchestration) #144](https://github.com/subroh0508/colormaster/pull/144) (squash merge commit `d197acf`、2026-05-17T15:22:24Z マージ、head commit `0074db5`、`gh pr merge --merge`)
> 差分: 7 ファイル / +753 / -7 (760 diff 行)、commits: 3 (初回 `179f135` feat / 再 rename `857d039` docs / fix loop `0074db5` で code-reviewer 4 aspect Improvement 5 件即時消化)
> 関連 PR: [#142](https://github.com/subroh0508/colormaster/pull/142) (dry-run フェーズ配置 / 本 PR の前提インフラ)、[#143](https://github.com/subroh0508/colormaster/pull/143) (直前の retro 集約 PR、merge commit `93d624d` ベース)、本 retro はメタレトロではない通常 retro

## 本 retrospective の位置付け

> **本 retrospective は通常 retro** (retro-of-retro ではない): 対象 PR #144 は orchestrator Skill 新規追加 + 新規 rule 配置 + roadmap 更新 + dry-run 起票を 1 PR に集約した A2 follow-up PR で、Skill / rule の新規追加自体が改修対象。retro-of-retro 判定基準 3 条件 (集約 retro / 人間追加項目反映 commit / 即時 follow-up PR で Try 実装) のうち 0 件該当のため、通常 retro として起票。本 PR で実証された「session log 段階的 read」「skill-creator 経由 Skill 雛形生成」「dry-run Case A/B 並列比較」「ファイル経由 prompt 送信 (仕様 8 の初実証)」を Keep / Problem / Try で記録し、後続の Skill 新規追加 PR / orchestrator 運用知見蓄積に活用する。

## ✅ Keep (継続したいこと)

- **session log (3.6MB jsonl) を段階的 read で読み込み、教訓 10 系統を抽出する pattern が機能** (consensus: spec-conformance + process): orchestrator Skill の本格化は本セッションで実演した EPIC-A2 並列 self-merge + B0 / A1 / harness-meta / dry-run 各フェーズの session log を Read offset で 1000 行ずつ段階読みし、cmux サブコマンド辞典 / 1 ペイン=1 PR 原則 / 30s ポーリング / 思考動詞辞書 / stale display 復旧 / classifier 通過境界 / context 60% handover / ファイル経由 prompt 送信の 8 仕様を抽出。一括 Read だと context 圧迫リスクがあったが、段階的 offset で 10 系統の教訓を漏れなく抽出できた。
- **`example-skills:skill-creator` 経由で SKILL.md 雛形生成 → 仕様 1-8 + 教訓で肉付け → 100-point rubric self-eval の標準フロー** (consensus: spec-conformance + architecture): ADR-0025 (Skill 作成は `example-skills:skill-creator` 経由) を踏襲し、skill-creator で SKILL.md 雛形生成 → 仕様 1-8 + 教訓 10 系統で本文肉付け → 100-point rubric (description trigger 19/20 + Gotchas 15/15 + 関連 15/15 + 入出力 10/10 + status 5/5 + フォーマット 10/10 + 禁止表現 4/5 + 責務分離 10/10 + 単独テスト 4/5 + 履歴 5/5) で **97/100** self-eval。skill-authoring.md (A2-3 stable) の 100-point rubric を初実例として PR body + dry-run ファイル両方に記録、後続の Skill 新規追加 PR (A3 専用 Skill 群実装) の標準フロー化候補。
- **dry-run sample 適用 (Case A 採用案 vs Case B skeleton 案を別 subagent で並列生成、判定根拠を PR body に記録)** (consensus: architecture): PR #142 で配置済の dry-run phase (`docs/harness/dry-runs/template.md` + `harness-meta-criteria.md` §dry-run 必須条件) を初回実適用、Case A (skill-creator 経由 採用案、97/100) vs Case B (general-purpose subagent 経由 skeleton 案、95/100 だが仕様 1-8 詳細欠落) を別 subagent で並列生成。判定根拠を `docs/harness/dry-runs/2026-05-18-pr-144.md` に明示記録 (仕様 1-8 充足度 Case A 8/8 vs Case B 0/8 / 教訓 10 系統具体例化 / rubric スコア差 / dry-run 必須条件適合)、verdict: adopt を SoT 化。dry-run phase の 1 番目の実適用事例として後続の Skill 新規追加 / rule 構造変更 PR の参考例化。
- **仕様 8 (ファイル経由プロンプト送信) が本 PR の最初の実証例として機能、cmux send 文字欠落の根本回避** (consensus: architecture + process): orchestrator → per-task 指示で長文 prompt (200 字超) を `cmux send` 直送すると zsh 解釈で 30-50% 文字欠落するリスク (PR #141 / orchestrator skill 起票プロセスで観測済) を、Step A-D (ファイル書き出し → ペイン cd 確認 → ファイル read 指示送信 → 送信後の read 完了 ack) のファイル経由送信プロトコルで根本回避。本 PR の初回 orchestrator 指示自体は cmux send 直送だったため仕様 2-7 欠落事故が再発したが、再送 + 補足送信で仕様 8 自体は本 PR 内で実証完了、後続のすべての長文 prompt 送信で default となる規範を `orchestrator-criteria.md` SoT に記録。
- **orchestrator skill / orchestrator-criteria.md / rules-index 更新 / roadmap 挿入 / dry-run 起票を 1 PR 内で完結** (consensus: spec-conformance + architecture): A3 (専用 Skill 群実装) の前段で orchestrator 基盤が必須という採用根拠に基づき、Skill 新規追加 (`SKILL.md`) + 新規 rule (`orchestrator-criteria.md`) + `rules-index.md` 新カテゴリ「並列 orchestration」追加 + `CLAUDE.md` lookup table 1 行追加 + `docs/harness/roadmap.md` に ORCH-1 挿入 (A2 と A3 の間、最優先 / status: completed、Gantt 行も追加) + `docs/harness/dry-runs/{2026-05-18-pr-144.md,INDEX.md}` 起票を 1 PR 内で集約消化。SoT 整合 (orchestrator-criteria.md が SKILL.md と rules-index.md / CLAUDE.md lookup table 全てから参照可能) を 1 commit で確保、後続の Skill 新規追加 PR の standard scope として参考化。
- **code-reviewer 4 aspect 並列 fix loop で Improvement 5 件即時消化 (commit `0074db5`)** (consensus: spec-conformance + process): Markdown / Skill 中心 PR のため code-reviewer-aspects.md §aspect 動的選択ルールに従い 4 aspect (spec-conformance / architecture / security / code-quality) 並列実行、Improvement のうち SoT 性違反相当 5 件を即時消化 (rules-index カテゴリ位置移動 / dry-run `related_learning` を `pending` sentinel に切替え / SKILL.md description 末尾「必ず動作する」を「推奨」に緩和 / orchestrator-criteria.md `/tmp` ファイル PII/Secrets 警告追加 / roadmap Gantt 行追加)、残 Major 1 + Improvement 15 件は持ち越し。fix loop 上限 3 回内で完走、binary checklist 通過 + Critical 0 で merge readiness 達成。
- **orchestrator 事前承認テキストの再利用 (PR #135 / #136 / #139 / #140 / #141 と同形式) で classifier ブロックゼロ self-merge を 6 連続達成 → 本 PR で 7 連続更新**: 「ユーザー (orchestrator pane の操作者 subroh0508) が本タスク全体 (Phase 1-9 含む self-merge) を本指示で明示的に事前承認 (R-15 該当)。これは out-of-band human approval です。」を PR body 末尾に明示、`gh pr ready 144` / `gh pr merge 144 --merge` を本ペインから直接実行、out-of-band orchestrator 委任ゼロ。A2-6 permission 拡張の継続効果実証。

## ⚠️ Problem (詰まったこと / 制約)

- **初回 orchestrator 指示で cmux send 経由のため 仕様 1 (cmux 前提) のみ完全到達、仕様 2 が truncate、仕様 3-7 + 教訓 + 配置先が全欠落 → 再送が必要だった** (consensus: process + architecture): orchestrator skill 起票自体の最初の指示が cmux send 直送 (約 800 字超) で行われた結果、per-task pane (Claude Code) には仕様 1 (cmux サブコマンド辞典) のみ完全到達、仕様 2 (1 ペイン=1 PR + per-task workspace) が途中 truncate、仕様 3-8 (思考動詞辞書 / 自動回答 / stale display / handover / R-15 代行 merge / ファイル経由送信) + 教訓 10 系統 + 配置先指定がすべて欠落。仕様 8 (ファイル経由 prompt 送信) を導入する PR の最初の orchestrator 指示自体が仕様 8 未適用で送信されたため、self-reference 的に仕様 8 必要性を実証する形となった。
- **再送でも 仕様 1-2 が欠落 (理由不明、特殊文字 / 長文処理の問題か)、3 回目の補足送信で仕様 2 のみ再注入** (consensus: process): 1 回目 cmux send 直送 (仕様 1 のみ到達) → 2 回目 cmux send 直送 (仕様 1-2 欠落、仕様 3-8 + 教訓 + 配置先到達) → 3 回目 cmux send 補足送信 (仕様 2 のみ再注入) と 3 段階に分裂、cmux send の長文処理 / 特殊文字エスケープの挙動が不安定。仕様 8 (ファイル経由送信) を最初から適用していれば 1 回で完結したはずで、orchestrator → per-task 指示の default を `cmux send` 直送から `ファイル経由送信 (Step A-D)` に切替える必要性が本 PR で確定。
- **Agent ツールで `subagent_type='code-reviewer'` が not found エラー (available: claude / claude-code-guide / Explore / general-purpose / Plan / statusline-setup のみ)、code-reviewer は Skill ツール経由が正解** (consensus: spec-conformance + process): code-reviewer は `.claude/skills/code-reviewer/SKILL.md` を持つ Skill (skill-creator 経由で配置) であり、Agent ツールの subagent_type ではなく Skill ツール (`Skill('code-reviewer', '...')`) 経由で呼び出すのが正解。Agent ツールの built-in subagent_type (claude / general-purpose / Plan / Explore / claude-code-guide / statusline-setup) と Skill との区別が orchestrator skill SKILL.md / orchestrator-criteria.md に明文化されておらず、本 PR の code-reviewer 4 aspect 並列実行時に最初 Agent ツール経由を試みて `not found` エラーで気付く形となった。
- **claude-opus-4-7 1M context API が一時的に unavailable で auto mode classifier が判定不能になる transient outage、agent の自己リトライで復旧** (consensus: process + observability): Anthropic API の transient outage が本 PR の subagent 並列実行中に複数回観測、classifier 判定不能 → auto mode が pause → agent 自己リトライ (3 回までは retry 成功) → 復旧の流れ。orchestrator-criteria.md / SKILL.md には API outage 時の retry / fallback 規約が未明文化、後続の orchestrator 運用で「3 回 retry してダメなら orchestrator に通知」「pause が 5 分以上続いたら手動介入」のような明示ガイダンスが必要。
- **session log の Read で 3.6MB は段階的 offset 必須、一括 Read は context 圧迫** (consensus: process + observability): 本 PR の SKILL.md 本文肉付けで本セッション全体の jsonl session log (3.6MB / 約 40000 行) を Read する際、一括 Read を試みたが context 圧迫リスク (Read 結果が context window の大部分を専有) で段階的 offset (1000 行ずつ + grep フィルタ) に切替え。段階的 Read の標準サイズ (offset 増分 / chunk 数上限 / grep 前提条件) が orchestrator-criteria.md / SKILL.md に明文化されておらず、後続の session log 解析タスクで再発リスクあり。
- **本 PR で orchestrator skill の SKILL.md / rule / roadmap / dry-run を集約配置したが、自身の運用 (orchestrator 起動 → per-task 委譲) は本 PR retro 起票プロセスで初実証**: 本 PR (orchestrator skill 配置) は orchestrator pane 自身を「Skill 起票元」として動作させ、per-task pane への委譲は dry-run Case A/B 生成 (skill-creator + general-purpose subagent 並列) で部分的に実行したのみ。本 retro 起票プロセス自体が orchestrator skill SKILL.md / orchestrator-criteria.md の「ファイル経由 prompt 送信」「Phase 1-9 自走」を初実証する場であり、本 PR merge 後の retro 起票で実運用化されることが retrospective 起票時点で確定。
- **100-point rubric self-eval スコア (97/100) を PR body / dry-run ファイル両方に記録したが、retrospective-format.md には self-eval スコア記録欄が未定義** (consensus: architecture): 本 PR の skill-creator self-eval スコア 97/100 (description trigger 19/20 / 禁止表現 4/5 / 単独テスト 4/5 が減点要因) は PR body §dry-run 結果 と `docs/harness/dry-runs/2026-05-18-pr-144.md` に記録したが、`retrospective-format.md` §本文構造 には Skill 新規追加 PR の retro で self-eval スコアを記録する canonical セクションが未定義。後続の Skill 新規追加 PR retro で self-eval スコア記録の有無 / 形式が一貫しない可能性。

## 🚀 Try (次回からの改善案)

- **orchestrator → per-task 指示は必ずファイル経由 (仕様 8) を default にし、cmux send は 200 字未満の短指示のみに限定** (priority: highest、本 PR Problem 直接対応): `.claude/skills/orchestrator/SKILL.md` 仕様 8 (ファイル経由 prompt 送信、Step A-D) を default 経路として SKILL.md / orchestrator-criteria.md に明記、`cmux send` 直送は 200 字未満の短指示 (例: 「Enter」「ctrl+u」「`gh pr view 144`」) に限定。本 PR で初実証された通り、長文 prompt の cmux send 直送は文字欠落リスク 30-50%、ファイル経由なら 0%。orchestrator-criteria.md §プロンプト送信プロトコル に文字数閾値 (200 字) を明文化し、後続のすべての orchestrator → per-task 委譲で適用。
- **code-reviewer は Skill ツール経由 (`Skill('code-reviewer', '...')`) を SKILL.md / orchestrator-criteria.md に明記、Agent ツールでの呼び出しは禁止** (priority: high、本 PR Problem 直接対応): `.claude/skills/orchestrator/SKILL.md` + `.claude/rules/orchestrator-criteria.md` + `.claude/skills/code-reviewer/SKILL.md` の起動契機セクションに「Agent ツール vs Skill ツール 使い分け」を追加。Agent ツール subagent_type は built-in (claude / general-purpose / Plan / Explore / claude-code-guide / statusline-setup) のみ、`.claude/skills/<name>/SKILL.md` を持つすべて (code-reviewer / pr-retrospective / orchestrator / implementation-workflow 等) は Skill ツール経由が正解、と明文化。Agent ツールで `subagent_type='code-reviewer'` の not found エラーは本 PR で初観測、後続で再発防止。
- **session log Read 時の段階的 offset の標準サイズ (例: 1000 行ずつ) を SKILL.md に明示** (priority: medium、本 PR Problem 直接対応): `.claude/skills/orchestrator/SKILL.md` + `.claude/rules/orchestrator-criteria.md` に「session log 段階的 read プロトコル」セクション追加、offset 増分 (1000 行 / chunk) + chunk 数上限 (一括 read は 5000 行まで、超過時は grep フィルタ前提) + grep 前提条件 (Skill 名 / Phase 番号 / cmux サブコマンド名でフィルタ) を明文化。本 PR の SKILL.md 本文肉付けプロセスで実証された段階的 Read pattern を canonical 化、後続の session log 解析タスクで標準化。
- **API outage 時の自己リトライ閾値 (例: 3 回までは retry、超過時は orchestrator に通知) を SKILL.md に明示** (priority: medium、本 PR Problem 直接対応): `.claude/skills/orchestrator/SKILL.md` + `.claude/rules/orchestrator-criteria.md` + `.claude/rules/harness-meta-criteria.md` に「API outage 時の retry / fallback 規約」セクション追加、3 回 retry → pause が 5 分以上続けば orchestrator 手動介入 → 30 分超過時は別 model fallback / 翌日再開を明文化。本 PR の claude-opus-4-7 1M context API transient outage で観測された pattern を canonical 化、後続のすべての orchestrator 運用で API outage 耐性を確保。
- **100-point rubric self-eval の最終スコアを PR body に必ず記載 (本 PR は 97/100)、retrospective-format.md にも self-eval スコア記録欄追加** (priority: medium): `.claude/rules/retrospective-format.md` §本文構造 に Skill 新規追加 PR 用の `## 🎯 skill-creator self-eval スコア` セクション (任意) を追加、`description trigger / Gotchas / 関連 / 入出力 / status / フォーマット / 禁止表現 / 責務分離 / 単独テスト / 履歴 の 10 軸 × 5/10/15/20 点満点 = 100 点満点` の表形式で記録。本 PR の 97/100 を初出として後続の Skill 新規追加 PR (A3 専用 Skill 群実装で 7 系統 + α) で標準採用。
- **本 PR retro 起票プロセス自体が orchestrator skill の初実運用、retro 起票後に「実運用初日」learnings を `orchestrator-criteria.md` に逆流フィードバック** (priority: medium): 本 PR で配置された orchestrator SKILL.md / orchestrator-criteria.md が本 retro 起票で初実運用されるため、retro 起票プロセス中の観察 (ファイル経由 prompt 送信の実証 / 段階的 read の運用 / classifier ブロックゼロ self-merge 7 連続) を `orchestrator-criteria.md` Gotchas に追記候補。retro 起票 → 直後の Skill 改修 PR (orchestrator-criteria.md 改修) という即時 follow-up pattern を試験運用、PR #142 (PR #141 retro の Try 即時実装) と同形式。
- **本 PR の dry-run 適用事例 (Case A vs Case B 別 subagent 並列生成) を `docs/harness/dry-runs/template.md` の参考実装に追加** (priority: low): PR #142 で配置済の `docs/harness/dry-runs/template.md` には Case A/B 比較フォーマットの skeleton はあるが、別 subagent (skill-creator vs general-purpose) で並列生成する具体的 procedure は未記載。本 PR の dry-run ファイル (`docs/harness/dry-runs/2026-05-18-pr-144.md`) を template の §実装例 として参照可能にし、後続の Skill 新規追加 PR で dry-run の品質を担保。

## 📊 指標

PR #144 は orchestrator skill 新規追加 + 新規 rule + roadmap 更新 + dry-run 起票で構成、実装コード差分ゼロ (Markdown / `.claude/skills/` のみ)、三層テスト品質基盤 (Kover / Konsist Spec / PITest) は A7 で導入予定のため Before/After 差分は計測不能 (N/A)。

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| Line coverage | N/A | N/A | — | Kover 未導入 (A7 で導入予定) |
| Branch coverage | N/A | N/A | — | 同上 |
| Spec coverage | N/A | N/A | — | Konsist `@Spec` 未導入 (A7 で導入予定) |
| Mutation score | N/A | N/A | — | PITest 未導入 (A7 で導入予定) |
| Lint 違反数 | N/A | N/A | — | Spotless / ktlint / detekt / markdownlint-cli2 未導入 (A6 で導入予定) |
| ファイル数 (差分) | — | 7 changed | 7 changed | `.claude/skills/orchestrator/SKILL.md` / `.claude/rules/orchestrator-criteria.md` / `.claude/rules/rules-index.md` / `CLAUDE.md` / `docs/harness/roadmap.md` / `docs/harness/dry-runs/2026-05-18-pr-144.md` / `docs/harness/dry-runs/INDEX.md` |
| 行数 (差分) | — | +753 / -7 (squash merge commit `d197acf`) | net +746 | 全て Markdown / SKILL.md / rule (Kotlin 差分なし) |
| commits 数 (squash 前) | — | 3 (`179f135` feat 初回 + `857d039` docs rename + `0074db5` fix loop 即時消化) | +3 | `--merge` で merge commit `d197acf` 配下に 3 commit 保持 |
| code-reviewer aspect 実行 (subagent 並列) | — | 4/4 pass (spec-conformance / architecture / security / code-quality) | — | test-quality / performance / visual-regression / design-tokens skip (Markdown / Skill 中心 PR、PR #137-141 と同パターン) |
| Critical findings | — | 0 | — | merge readiness 3 条件充足 |
| Improvement 件数 (fix loop 即時消化) | — | 5 (rules-index カテゴリ位置移動 / dry-run `related_learning` pending sentinel / SKILL.md description 緩和 / orchestrator-criteria.md `/tmp` PII/Secrets 警告 / roadmap Gantt 行追加) | +5 | commit `0074db5` で 5 件即時消化、残 Major 1 + Improvement 15 件は持ち越し |
| Improvement 件数 (持ち越し) | — | 16 (Major 1 / SoT 重複参照 3 箇所集約 + Improvement 15) | +16 | 本 retro `🤖 ハーネス改善提案` に記録、A3 / A4 / A6 で順次消化見込み |
| binary checklist 通過率 (4 aspect 集約) | — | 通過 ✅ (binary checklist 詳細件数は PR body に未記載、Critical 0 で merge readiness 達成) | — | PR #137-141 の 21/21 と同水準想定 |
| skill-creator self-eval スコア (Case A 採用案) | — | **97/100** (description trigger 19/20 + Gotchas 15/15 + 関連 15/15 + 入出力 10/10 + status 5/5 + フォーマット 10/10 + 禁止表現 4/5 + 責務分離 10/10 + 単独テスト 4/5 + 履歴 5/5) | — | 減点要因: description trigger 1 点 / 禁止表現 1 点 / 単独テスト 1 点。後続 Skill 新規追加 PR の参考値 |
| skill-creator self-eval スコア (Case B skeleton 案、配置せず比較材料) | — | 95/100 だが仕様 1-8 詳細 / cmux サブコマンド辞典 / classifier 通過境界 / stale display 復旧 / handover / 思考動詞辞書 一切なし | — | スコア差は 2 点だが情報量差は圧倒的、verdict: adopt (Case A) |
| dry-run 適用事例数 (PR #142 配置以降) | 0 | 1 (本 PR が初実適用) | +1 | `docs/harness/dry-runs/2026-05-18-pr-144.md` を 1 番目の事例として後続 Skill 新規追加 PR の参考化 |
| 連続 self-merge 完走 PR 数 (A2-6 permission 拡張効果、本 PR まで) | 6 (PR #135 / #136 / #139 / #140 / #141 / #142) | 7 (+ PR #144、PR #143 集約 retro を含めれば 8) | +1 | 後続 retro 起票 PR (本 retro) でも継続見込み |
| 仕様 8 (ファイル経由 prompt 送信) 適用事例数 | 0 | 1 (本 PR で初実証、cmux send 直送の文字欠落 30-50% を 0% に低減) | +1 | 後続のすべての長文 orchestrator → per-task 委譲で default 適用候補 |
| 教訓 10 系統の session log 抽出件数 | — | 10 (cmux サブコマンド / 1 ペイン=1 PR / 30s ポーリング / 思考動詞辞書 / stale display 復旧 / handover / R-15 代行 merge / ファイル経由送信 / 並列起動 / classifier 迂回辞典) | +10 | 段階的 Read で漏れなく抽出、各教訓に PR# 引用付き |
| API outage 観測回数 (本 PR 作業中) | — | 複数回 (claude-opus-4-7 1M context transient unavailable、各回 agent 自己リトライ 3 回以内で復旧) | — | 後続の orchestrator-criteria.md に retry / fallback 規約追加で観測・記録継続 |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造 -->

<!-- 本 retro では人間追加項目なし (orchestrator pane の操作者 subroh0508 から retro レビュー時の追加指示がなければ AI 自動抽出のみ) -->

- [ ] `[skill]` `.claude/skills/orchestrator/SKILL.md` に **「Agent ツール vs Skill ツール 使い分け」セクション追加**: Agent ツール subagent_type は built-in (claude / general-purpose / Plan / Explore / claude-code-guide / statusline-setup) のみ、`.claude/skills/<name>/SKILL.md` を持つすべて (code-reviewer / pr-retrospective / orchestrator / implementation-workflow / harness-meta / harness-evolution / pr-poller) は Skill ツール経由 (`Skill('<name>', '...')`) が正解、と明文化。本 PR で `subagent_type='code-reviewer'` not found エラーで初観測、後続で再発防止。採用判定基準 4 (Critical 派生、orchestrator 経由の Skill 呼び出し失敗) 該当
- [ ] `[rule]` `.claude/rules/orchestrator-criteria.md` に **「session log 段階的 read プロトコル」セクション追加**: offset 増分 (1000 行 / chunk) + chunk 数上限 (一括 read は 5000 行まで、超過時は grep フィルタ前提) + grep 前提条件 (Skill 名 / Phase 番号 / cmux サブコマンド名でフィルタ) を明文化。本 PR の SKILL.md 本文肉付けプロセスで実証された段階的 Read pattern を canonical 化。採用判定基準 1 (本 PR で実証 + 後続の session log 解析タスクで再利用見込み) 該当
- [ ] `[rule]` `.claude/rules/harness-meta-criteria.md` に **「API outage 時の retry / fallback 規約」セクション追加**: 3 回 retry → pause が 5 分以上続けば orchestrator 手動介入 → 30 分超過時は別 model fallback / 翌日再開を明文化。本 PR の claude-opus-4-7 1M context API transient outage で観測された pattern を canonical 化、後続のすべての orchestrator 運用で API outage 耐性を確保。採用判定基準 3 (process improvement、Skill 横断) 該当
- [ ] `[rule]` `.claude/rules/retrospective-format.md` §本文構造 に **`## 🎯 skill-creator self-eval スコア` セクション (任意) 追加**: Skill 新規追加 PR retro で self-eval スコアを記録する canonical 形式、`description trigger / Gotchas / 関連 / 入出力 / status / フォーマット / 禁止表現 / 責務分離 / 単独テスト / 履歴 の 10 軸 × 5/10/15/20 点満点 = 100 点満点` の表形式。本 PR の 97/100 を初出として後続の Skill 新規追加 PR (A3 専用 Skill 群実装で 7 系統 + α) で標準採用。採用判定基準 2 (retrospective-format.md 関連、structural change) 該当
- [ ] `[skill]` `.claude/skills/orchestrator/SKILL.md` + `.claude/rules/orchestrator-criteria.md` に **「仕様 8 (ファイル経由 prompt 送信) を default、cmux send 直送は 200 字未満限定」明文化**: 本 PR で初実証された通り、長文 prompt の cmux send 直送は文字欠落リスク 30-50% (本 PR の最初の orchestrator 指示自体で仕様 2-7 + 教訓 + 配置先が欠落)、ファイル経由なら 0%。orchestrator-criteria.md §プロンプト送信プロトコル に文字数閾値 (200 字) を明文化し、後続のすべての orchestrator → per-task 委譲で適用。採用判定基準 4 (Critical 派生、cmux send 文字欠落の根本回避) 該当
- [ ] `[template]` `docs/harness/dry-runs/template.md` の §実装例 に **本 PR の dry-run ファイル (`docs/harness/dry-runs/2026-05-18-pr-144.md`) を 1 番目の参照例として明示**: PR #142 で配置済の dry-run template には Case A/B 比較フォーマットの skeleton はあるが、別 subagent (skill-creator vs general-purpose) で並列生成する具体的 procedure は未記載。本 PR の dry-run ファイルを template の §実装例 として参照可能にし、後続の Skill 新規追加 PR で dry-run の品質を担保。採用判定基準 1 (本 PR で実証 + 後続で再利用見込み) 該当
- [ ] `[rule]` `.claude/rules/orchestrator-criteria.md` Gotchas に **「本 PR retro 起票プロセス自体が orchestrator skill の初実運用」観察結果反映**: 本 retro 起票で実証されたファイル経由 prompt 送信 / 段階的 read / classifier ブロックゼロ self-merge 7 連続を `orchestrator-criteria.md` Gotchas に追記候補、retro 起票 → 直後の Skill 改修 PR (orchestrator-criteria.md 改修) の即時 follow-up pattern を試験運用。PR #142 (PR #141 retro の Try 即時実装) と同形式。採用判定基準 1 (本 PR retro で実証) 該当

## 📝 harness-meta フィードバック

<!-- harness-meta が後から追記。提案 → 結果の往復ログを 1 ファイル内で完結 -->

### YYYY-MM-DD <PR ID> で消化 (採用)

| 提案 | <PR ID> での消化内容 |
|---|---|

### YYYY-MM-DD <PR ID> で見送り (後続フェーズへ)

| 提案 | 見送り理由 / 移行先 |
|---|---|

### YYYY-MM-DD <PR ID> で保留 (要再評価)

| 提案 | 保留理由 |
|---|---|

## 関連

- 対象 PR: [#144](https://github.com/subroh0508/colormaster/pull/144) (本 retro 対象、orchestrator skill 配置 / cmux 並列 orchestration)
- 関連 PR: [#142](https://github.com/subroh0508/colormaster/pull/142) (dry-run フェーズ配置、本 PR の前提インフラ) / [#143](https://github.com/subroh0508/colormaster/pull/143) (直前の retro 集約 PR、merge commit `93d624d` ベース)
- 関連 dry-run: [`2026-05-18-pr-144.md`](../dry-runs/2026-05-18-pr-144.md) (Case A 採用案 vs Case B skeleton 案、verdict: adopt)
- 関連 ADR: ADR-0017 (ローカル Claude Code ポーリング駆動) / ADR-0018 (implementation-workflow 10 Phase SoT) / ADR-0024 (MCP 採用、`gh` CLI 優位) / ADR-0025 (Skill 作成は `example-skills:skill-creator` 経由) / ADR-0026 (harness-evolution Skill / retrospective format) / ADR-0027 (テンプレ言語、日本語化)
- 関連 rule: `.claude/rules/{orchestrator-criteria,retrospective-format,harness-meta-criteria,pr-poller,pr-template,merge-readiness,docs-structure,template-language,skill-authoring,code-reviewer-aspects}.md`
- 関連 Skill: `.claude/skills/{orchestrator,implementation-workflow,code-reviewer,pr-retrospective,pr-poller,harness-meta,harness-evolution,harness-bootstrap}/SKILL.md`
- Epic: [`EPIC-A2-rules-docs-extension`](../../epics/EPIC-A2-rules-docs-extension/) (`completed` 昇格後の A2 follow-up、A3 専用 Skill 群実装の前段)
- roadmap: `docs/harness/roadmap.md` §ORCH-1 (A2 と A3 の間、最優先項目、status: completed、本 PR で挿入)

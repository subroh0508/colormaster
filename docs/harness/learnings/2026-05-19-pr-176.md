---
id: learning-pr-176
title: PR #176 レトロスペクティブ (A5 不要モジュール撤去 / js / firebase / kotlin-js-store)
type: learning
status: draft
related_pr: 176
related_plan: PLAN-004
related_epic: —
generated_at: 2026-05-19T12:55:00+09:00
generator: pr-retrospective (skeleton、本ペインで手動代替実行)
---

# PR #176 レトロスペクティブ (A5 不要モジュール撤去 / js / firebase / kotlin-js-store)

> 生成: pr-retrospective (skeleton、本ペインで手動代替実行) at 2026-05-19T12:55:00+09:00
> 関連 Plan: PLAN-004 (起票時 PLAN-003 → 並走 PR #175 との番号衝突で rename) / 関連 Epic: —
> 対象 PR: [chore(modules): A5 不要モジュール撤去 (js/firebase/kotlin-js-store) #176](https://github.com/subroh0508/colormaster/pull/176) (squash merged at 2026-05-18T18:58:12Z、commit `b99d43d`、from head `bb6975d`)
> 差分: 114 ファイル / +177 / -11746 (11923 diff 行)、commits: 4 (撤去 commit + build script commit + Plan + rule commit + CI fix follow-up commit、orchestrator 代行 fix 含む)

## ✅ Keep (継続したいこと)

- **ADR-0005 / 0011 / 0012 / 0022 の宣言済撤去を物理反映、4 ADR の累積方針を 1 PR で消化**: Decompose 撤去 (ADR-0005) / Firebase → GIS 移行 (ADR-0011) / 旧 JS 実装撤去 (ADR-0012) / Cloudflare Pages 移行 (ADR-0022) の累積撤去方針を本 PR 1 件で物理反映、SoT 一致 ✅。`removed-modules.md` 撤去対象一覧表で 8 項目 (旧 JS app / Firebase Hosting / Decompose / kotlin-wrappers / npm-material-component-web / kotlin-js plugin / google-services plugin / deploy workflow) を「なし (撤去完了)」に更新、撤去後の SoT が完全一致 (consensus: spec-conformance + architecture)
- **production code path 不変 (`android:app` / `backend:server` / `backend:cli` / `core/features` / `core/data` / `core/network/imasparql` / `core/model` touch ゼロ)**: 撤去対象 (`js/**` / `kotlin-js-store/` / `firebase.json` / `.firebaserc` / `.github/workflows/web-build-and-deploy.yml` / `core/common/src/jsMain/.../firebase.kt`) は production 依存ゼロ、Composable / public API / ViewModel / Repository は不変。`grep` ベースの dependency 検証 (`decompose` / `kotlin.wrappers` / `js.material` / `js.app` / `npm.material` / `google-services` / `libs.plugins.kotlin.js` への参照確認) で撤去対象が production code path に呼び出されていないことを behavior preservation 証拠として明示 (consensus: architecture + spec-conformance)
- **convention plugin 同時更新 (`AndroidAppModulePlugin.kt` / `KmpJsPlugin.kt` / `CommonModulePlugin.kt`)**: catalog 削除と同時に convention plugin 内の `google-services` apply / `kotlin-wrappers-bom/js` 参照 / `kotlin-wrappers` jsMain dep 参照を全件除去、build script 整合性を維持。AC-05 で `build.gradle.kts` / `android/app/build.gradle.kts` / `plugins/**/*.kt` で `google-services` / `kotlin-js` plugin / `kotlin-wrappers` への参照ゼロを機械検証 (consensus: architecture)
- **スコープ縮小判断の明示 (Firebase Authentication / Firestore は production 依存で残置)**: `core/network/auth/` / `core/network/firestore/` / `firebase-app` / `firebase-auth` / `firebase-firestore` libs / `core/test/.../FirebaseUser.kt` / `core/test/.../FakeFirestoreClient.kt` は production 依存ありのため本 PR では touch せず Phase B / C で別 EPIC 対応、`removed-modules.md` 残骸列に保持。R-22 (A10 完了前のリファクタ制約) との緊張関係を「ADR 宣言済撤去の物理反映」として位置付けて解消、Plan 本文に明示 (consensus: spec-conformance)
- **4 commit 分離 (撤去 / build script / docs + rule / CI fix follow-up) の logical separator**: ① `chore(modules): remove legacy js app and firebase hosting (A5)` (`188c035`、撤去本体) + ② `chore(gradle): drop decompose/kotlin-wrappers/google-services (A5)` (`01cc2ac`、build script) + ③ `docs(plan): add PLAN-003 and update removed-modules rule (A5)` (`9290d12`、Plan + rule) + ④ `ci: remove Test / Web job after js module deletion (A5 follow-up)` (`bb6975d`、orchestrator 代行 fix) の 4 commit に分離、`harness-meta-criteria.md` §commit 分離規範 (PR #174 で SoT 化) の Phase 別適用例として `git revert` 部分適用が可能 (consensus: code-quality)
- **`kotlinx-coroutines-js` / `ktor-client-js` 等 jsMain 用 library 行の残置を明示記録 (wasmJs target 拡張への再利用想定)**: Plan §スコープ外 セクションで「`kotlinx-coroutines-js` / `ktor-client-js` / `ktor-client-json-js` / `ktor-serialization-js` libs 行は残置 (将来 wasmJs target 拡張で再利用想定、`.claude/rules/wasm-compat.md` 整合)」を明示、本 PR scope (旧 JS app 撤去) と将来 scope (wasm 統一) を区別。レビュー観点でも「残置」を確認推奨として明示 (consensus: architecture)
- **ロールバック手順を Plan 本文に明示 (`git revert` / `git checkout` 部分復元 / convention plugin 復元 / `removed-modules.md` 復元 / `./gradlew tasks` 確認)**: Plan §ロールバック手順 セクションで 5 ステップを明示、不可逆操作リスク (撤去 PR の特性) を低減する SoT として参考可能。R-22 (リファクタ制約) との緊張関係下で撤去を実施する際の運用 SoT を提供 (consensus: spec-conformance)
- **CI fix follow-up commit (orchestrator 代行) の追加で Test / Web FAILURE を解消**: A5 sub-agent (per-task pane) が漏らした CI 整合 fix (`.github/workflows/ci.yml` から `js: Test / Web` job を撤去) を orchestrator 代行で補完、`commit bb6975d`。`gh run list` 結果で 1 回目 failure (`24e2ea2`) → 2 回目 success (`bb6975d`) を確認、orchestrator pane 代行 fix pattern の典型例 (consensus: code-quality)
- **Plan 番号 rename (PLAN-003 → PLAN-004) を orchestrator 代行 fix で対応**: 並走していた A8 PR #175 が先に merge され PLAN-003 番号を取得したため、本 PR の Plan 番号を PLAN-003 → PLAN-004 に rename。`docs/plans/PLAN-003-A5-removed-modules.md` → `docs/plans/PLAN-004-A5-removed-modules.md` を `git mv` + frontmatter `id: PLAN-004` 更新 + `docs/plans/INDEX.md` の conflict 解消で並列 A8 行 (PLAN-003) と A5 行 (PLAN-004) を残す。orchestrator pane 代行で本 PR コメントに rename note を明示記録 (consensus: architecture)

## ⚠️ Problem (詰まったこと / 制約)

- **Plan 番号衝突 (PLAN-003)**: 並走 PR #175 が先に merge され PLAN-003 を取得、本 PR は PLAN-003 → PLAN-004 rename が必要に。`commit message 9290d12` には rebase 中の reword 制約で「PLAN-003」表記が残るが、実体 file 名は PLAN-004 で確定。並走 PR の Plan 番号事前予約 / 衝突解消 pattern が `.claude/rules/plan.md` に未 SoT 化 (consensus: architecture)
- **CI 1 回失敗 (Test / Web FAILURE)**: A5 sub-agent (per-task pane) が `.github/workflows/ci.yml` の `js: Test / Web` job 撤去を漏らし、CI 1 回目 (`24e2ea2`) failure。orchestrator 代行 fix commit (`bb6975d`) で解消。`implementation-workflow.md` Phase 3 (Lint + Test) の `./gradlew check` ローカル実行を JDK 25 vs AGP 8.9.0 互換性問題で skip したため CI 上で初検出、ローカル green 担保が不可だった (consensus: spec-conformance)
- **ローカル `./gradlew check` skip (JDK 25 vs AGP 8.9.0 互換性問題)**: 本 PR とは無関係に master でも同様のエラー発生を確認、ローカルでは fix 不可。CI 上での green 担保のみが選択肢、Phase 3 fix loop (上限 3) に依存する状況。`docs/runbooks/local-development.md` に JDK 互換性 TODO を明示推奨 (consensus: code-quality)
- **`gradle/libs.versions.toml` の `kotlinx-coroutines-js` / `ktor-client-js` 等 jsMain 用 library 行が残置**: Plan §スコープ外 で明示、wasmJs target 拡張への再利用想定だが、本 PR で撤去するか保留するかの判定根拠が **wasm 移行戦略が固まる前** として保留判断。roadmap で `A10` 完了後 / wasmJs 統一決定後の再評価タイミングが追跡されていない (consensus: architecture)
- **`core/common/build.gradle.kts` の jsMain dependencies (`npm-i18next-core` / `npm-i18next-http-backend`) 残置の production 影響不明**: `:js:app` 撤去で唯一の i18next 利用箇所が削除された可能性、`core/common/src/jsMain/**` の i18next 参照を grep 確認すれば判定可能だが、本 PR scope (旧 JS app 撤去) を超えるため保留。次 PR で確認 → 未使用なら撤去判定。Improvement #2 として持ち越し (consensus: architecture)
- **`removed-modules.md` 撤去対象一覧表の「残骸」列で `core/test/.../FirebaseUser.kt` / `FakeFirestoreClient.kt` が暗黙的**: 「Firebase Authentication SDK」「Firebase Firestore」の残骸列に `core/test` 配下の test double 言及なし、後続 EPIC で `core/test` 配下の Firebase test double が「残骸 (撤去進行中)」として明示されない懸念。Improvement #3 として持ち越し (consensus: spec-conformance)
- **撤去 PR の不可逆性高 (1.1 万行削除)**: -11746 行は本リポジトリ最大級の削除規模、`git revert` で部分的にロールバックは可能だが、運用コスト高。`.dockerignore` 配置 PR / wasmJs 統一決定後の `kotlinx-coroutines-js` 撤去 PR 等の連鎖が必要、撤去 PR 単独完結ではなく後続 PR との連動を Plan §ロールバック手順 に追加検討 (consensus: code-quality)
- **`harness-meta-criteria.md` §撤去判定基準 (3 条件: dormant 確定 + dangling 参照ゼロ + orchestrator 事前承認) と本 PR の関係性が PR description に明示なし**: 本 PR は撤去 PR だが、§撤去判定基準 の 3 条件 (直近 3 ヶ月 dormant / dangling 参照ゼロ / orchestrator 事前承認) を `removed-modules.md` 更新と同時に明示する pattern が未確立、`harness-meta-criteria.md` §撤去判定基準 2 段階運用 (Step 1 status 変更 PR + Step 2 物理削除 PR) との関係性も明示なし。本 PR は accumulated ADR (0005/0011/0012/0022) の物理反映なので 2 段階運用の例外扱いだが、SoT 上の例外条件が明示されていない (consensus: architecture)

## 🚀 Try (次回からの改善案)

- **`.claude/rules/plan.md` §採番ライフサイクル (予約 → 起票 → 衝突解消) を SoT 化**: 並走 PR の Plan 番号予約 / 衝突解消 pattern を `.claude/rules/plan.md` に追加、PR #175/#176 の PLAN-003 番号衝突 → PR #176 rename を運用例として引用。`adr.md` §採番ライフサイクル と同様の運用 SoT 化 (採用判定基準 1 (2 回観測) 該当、PR #175 retro と統合)
- **`.claude/rules/harness-meta-criteria.md` §撤去判定基準 2 段階運用 (Step 1 status 変更 PR + Step 2 物理削除 PR) の例外条件 (accumulated ADR 物理反映) を明示**: 本 PR のような accumulated ADR (0005/0011/0012/0022) の物理反映 PR を 2 段階運用の例外として明示、撤去 PR の SoT 整合性を向上。`harness-meta-criteria.md` §撤去判定基準 表脚注に「accumulated ADR 物理反映の例外」を追加 (Problem 消化、採用判定基準 4 該当)
- **CI 上の `./gradlew check` green 担保フローを Phase 3 fix loop と統合**: ローカル `./gradlew check` skip 時の CI 依存を `.claude/rules/implementation-workflow.md` Phase 3 fix loop と統合、本 PR の CI 1 回失敗 → orchestrator 代行 fix → 2 回目 success pattern を SoT 化 (採用判定基準 1 該当、PR #176 で観測)
- **`gradle/libs.versions.toml` 残置 library 行の再評価タイミングを roadmap で追跡**: `kotlinx-coroutines-js` / `ktor-client-js` 等の jsMain 用 library 行を A10 完了後 / wasmJs 統一決定後に再評価する旨を `roadmap.md` または別 Plan で追跡、Improvement #1 持ち越しの追跡継続 (採用判定基準 2 該当、A10 で予定)
- **`core/common/build.gradle.kts` の jsMain dependencies (i18next) 撤去判定**: 次 PR で `core/common/src/jsMain/**` の i18next 参照を grep 確認 → 未使用なら撤去判定、`npm-i18next-core` / `npm-i18next-http-backend` catalog 行と共に撤去 (Improvement #2 持ち越しの消化、採用判定基準 4 該当)
- **`removed-modules.md` 撤去対象一覧表に `core/test/.../{FirebaseUser,FakeFirestoreClient}.kt` を「残骸 (撤去進行中)」として明示追加**: 次回 `removed-modules.md` 更新時 (Phase B/C の Firebase test double 撤去 PR と同時) に test double の残骸記述を追加、Improvement #3 持ち越しの消化 (採用判定基準 4 該当)
- **撤去 PR の連鎖追跡 SoT (`.dockerignore` 配置 / wasmJs 統一決定後の `kotlinx-coroutines-js` 撤去 等)**: 撤去 PR 単独完結ではなく後続 PR との連動を Plan §ロールバック手順 に追加検討、撤去連鎖を `roadmap.md` で追跡 (採用判定基準 2 該当)
- **per-task pane の漏れ → orchestrator 代行 fix pattern を `.claude/rules/orchestrator-criteria.md` で SoT 化**: 本 PR の `bb6975d` (CI fix) / Plan 番号 rename pattern を per-task pane の漏れに対する orchestrator 代行 fix pattern として SoT 化、`.claude/rules/orchestrator-criteria.md` §代行操作 セクション (新設) で運用例を引用 (採用判定基準 1 該当、複数 PR で観測)
- **JDK 互換性 TODO を `docs/runbooks/local-development.md` で明示追跡**: ローカル `./gradlew check` skip の根本原因 (JDK 25 vs AGP 8.9.0 互換性) を runbook に明示、解消タイミング (AGP version up 等) を追跡 (採用判定基準 1 該当、master でも同様エラー観測)

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| Line coverage | N/A | N/A | N/A | Kover (A7 完了後)、本 PR は撤去のみで新規 Kotlin code 追加なし (カバレッジ低下なし想定) |
| Branch coverage | N/A | N/A | N/A | 同上 |
| Spec coverage | N/A | N/A | N/A | Konsist `@Spec` (A7 完了後)、SPEC docs touch なし |
| Mutation score | N/A | N/A | N/A | PITest (A7 完了後) |
| Lint 違反数 | N/A | N/A | N/A | Spotless / ktlint / detekt / markdownlint-cli2 (A6 完了後) |
| **PR 固有指標** | | | | |
| 変更ファイル数 | 0 | 114 | +114 | 削除 97 (旧 JS app) + 削除 4 (kotlin-js-store / firebase.json / .firebaserc / web-build-and-deploy.yml) + 削除 1 (`core/common/src/jsMain/.../firebase.kt`) + build script 4 (`settings.gradle.kts` / `gradle/libs.versions.toml` / `build.gradle.kts` / `android/app/build.gradle.kts` / `plugins/**/*.kt` × 3 → 計 6) + docs 3 (`removed-modules.md` / PLAN-004 / INDEX.md) + CI 1 (`ci.yml`) |
| 追加行数 | 0 | 177 | +177 | docs 中心 (`PLAN-004` 新規 + `removed-modules.md` 更新 + `INDEX.md` 1 行) |
| 削除行数 | 0 | 11746 | +11746 | 旧 JS app 97 ファイル削除が主、kotlin-js-store/yarn.lock 単独で大規模、本リポジトリ最大級の削除規模 |
| commits | 0 | 4 | +4 | 撤去 commit (`188c035`) + build script commit (`01cc2ac`) + Plan + rule commit (`9290d12`) + CI fix follow-up commit (`bb6975d`、orchestrator 代行) |
| code-reviewer Critical findings | N/A | 0 | 0 | aspect スコープ削減 (4/8、test-quality / performance / visual-regression / design-tokens skip) で全件パス |
| code-reviewer High findings | N/A | 0 | 0 | fix loop ゼロ |
| code-reviewer Improvement findings | N/A | 3 | +3 | code-quality 3 (jsMain libs 残置 / i18next 残置 / test double 残骸記述)、全件後続持ち越し |
| binary checklist 通過率 | N/A | 28/28 (100%) | N/A | 4 aspect × 7 項目、全件パス |
| classifier denial 件数 | 0 | 0 | 0 | 本 PR は denial ゼロ完走 |
| CI green 達成回数 | 0 | 2/2 (success) | N/A | 1 回目 failure (`24e2ea2`、Test / Web FAILURE) → 2 回目 success (`bb6975d`、orchestrator 代行 fix 後) |
| AC 充足率 | N/A | 6/7 (86%) | N/A | AC-01〜AC-06 [x]、AC-07 (CI green) は merge 直前の最終確認に依存 |
| Plan 番号 rename 件数 | 0 | 1 (PLAN-003 → PLAN-004) | N/A | 並走 PR #175 との衝突、orchestrator 代行 fix で対応 |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造 -->

- [ ] `[rule]` `.claude/rules/plan.md` §採番ライフサイクル (予約 → 起票 → 衝突解消) を新規追加、PR #175/#176 の PLAN-003 番号衝突 → PR #176 rename pattern を運用例として引用。本 PR Problem (Plan 番号衝突) を消化、採用判定基準 1 (PR #175/#176 で 2 回観測) 該当
- [ ] `[rule]` `.claude/rules/harness-meta-criteria.md` §撤去判定基準 表脚注に「accumulated ADR (0005/0011/0012/0022 等) の物理反映 PR は 2 段階運用の例外」を追加。本 PR Problem (2 段階運用との関係性不明) を消化、採用判定基準 4 (SoT 整合) 該当
- [ ] `[rule]` `.claude/rules/implementation-workflow.md` Phase 3 fix loop セクションに「ローカル `./gradlew check` skip 時の CI 依存 + orchestrator 代行 fix pattern」を追加。本 PR Problem (CI 1 回失敗) を消化、採用判定基準 1 (PR #176 で観測) 該当
- [ ] `[rule]` `.claude/rules/wasm-compat.md` または `roadmap.md` に「A10 完了後 / wasmJs 統一決定後の `kotlinx-coroutines-js` / `ktor-client-js` 等 jsMain libs 行再評価」のタイミング SoT を追加。本 PR Improvement #1 (架構残置の追跡) を消化、採用判定基準 2 (A10 で予定) 該当
- [ ] `[skill]` 次 PR で `core/common/src/jsMain/**` の i18next 参照 grep 確認 → 未使用なら `npm-i18next-core` / `npm-i18next-http-backend` catalog 行と共に撤去。本 PR Improvement #2 (i18next 残置) を消化、採用判定基準 4 (Open Questions 解消) 該当
- [ ] `[rule]` `.claude/rules/removed-modules.md` 撤去対象一覧表に `core/test/.../{FirebaseUser,FakeFirestoreClient}.kt` を「残骸 (撤去進行中)」として明示追加。本 PR Improvement #3 (test double 残骸記述) を消化、採用判定基準 4 (SoT 完全性) 該当
- [ ] `[rule]` `.claude/rules/orchestrator-criteria.md` §代行操作 セクション (新設) で per-task pane の漏れに対する orchestrator 代行 fix pattern を SoT 化、本 PR の `bb6975d` (CI fix) + Plan 番号 rename を運用例として引用。本 PR Problem (per-task 漏れ + 代行 fix) を消化、採用判定基準 1 (複数 PR で観測) 該当
- [ ] `[template]` `docs/runbooks/local-development.md` に JDK 25 vs AGP 8.9.0 互換性 TODO を明示追加、解消タイミング追跡。本 PR Problem (JDK 互換性) を消化、採用判定基準 1 (master でも観測) 該当
- [ ] `[rule]` `.claude/rules/plan.md` §ロールバック手順 に「撤去 PR の連鎖追跡」セクションを追加 (`.dockerignore` 配置 / wasmJs 統一決定後の jsMain libs 撤去 等の連動)。本 PR Problem (撤去連鎖追跡) を消化、採用判定基準 2 該当
- [ ] `[remove]` `core/common/build.gradle.kts` の jsMain dependencies (`npm-i18next-core` / `npm-i18next-http-backend`) を次 PR で撤去候補化、`harness-meta-criteria.md` §撤去判定基準 3 条件 (dormant 確定 + dangling 参照ゼロ + orchestrator 事前承認) を 2 段階運用で適用。本 PR Improvement #2 を消化、採用判定基準 4 該当

## 📝 harness-meta フィードバック

<!-- harness-meta が後から追記。提案 → 結果の往復ログを 1 ファイル内で完結 -->

### YYYY-MM-DD <PR ID> で消化 (採用)

| 提案 | <PR ID> での消化内容 |
|---|---|

### YYYY-MM-DD <PR ID> で見送り (後続フェーズへ)

| 提案 | 見送り理由 / 移行先 |
|---|---|

### YYYY-MM-DD <PR ID> で保留 (要再評価)

| 提案 | 保留理由 |
|---|---|

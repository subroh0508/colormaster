---
id: learning-pr-125
title: PR #125 レトロスペクティブ (A2-2 / rules 実装・コード系本格化)
type: learning
status: draft
related_pr: 125
related_plan: —
related_epic: EPIC-A2-rules-docs-extension
generated_at: 2026-05-17T11:15:00Z
generator: pr-retrospective (skeleton、本ペインで手動代替実行)
---

# PR #125 レトロスペクティブ

> 生成: pr-retrospective Skill (skeleton 段階、A3 本格化前のため本ペインで手動代替) at 2026-05-17T11:15:00Z
> 関連 Plan: なし (Epic 配下 PR、Plan 不要) / 関連 Epic: EPIC-A2-rules-docs-extension
> 対象 PR: [feat(harness): A2-2 rules 実装・コード系本格化 #125](https://github.com/subroh0508/colormaster/pull/125) (merged at 2026-05-17T11:03:57Z、merge commit `1a33ccc`、from head `daf9faf`)
> 差分: 37 ファイル / +3,494 / -46、commits: `2adf3fb` 初回 (rebase 後 `593516a`) + `daf9faf` (Improvement 反映)
> mirror PR: [#130](https://github.com/subroh0508/colormaster/pull/130) (merged at 2026-05-17T11:10:18Z、commit `11de659`、Phase 8 手動代替)

## ✅ Keep (継続したいこと)

- **7 グループ分割 (計画・記録 / セキュリティ / 層別 / 横断 / ファイル種別 / Backend / 索引) で 35 ファイル本格化を構造化**: TaskList で各グループ単位に進捗管理、グループ内は独立性が高く順次完了可能 (依存は計画・記録 → セキュリティ → 残り)。A2-2 規模 (37 ファイル / +3,494 行) を 1 PR に収めつつレビュー可能粒度に分割成功。
- **`code-reviewer` 4 aspect 並列で test/UI/perf を意図的にスコープ外宣言**: 本 PR は Markdown 規約改定のみで Kotlin / UI / build script 変更ゼロのため、test-quality / visual-regression / performance / design-tokens を最初から対象外と明示 (R-13 各 aspect ≥5 項目 binary checklist 要件は 4 aspect で満たした)。aspect 選択の cost 最適化が機能。
- **`Explore` subagent (read-only) で code-reviewer 4 aspect 並列**: A2-1 では `general-purpose` だったが、本 PR は Markdown のみのため `Explore` (read-only) で十分。aspect agent の権限最小化が機能、不要な書き込み権限を渡さない原則の実例。
- **Critical 0 / Improvement 3 件即時反映 (commit `310e430`、後 rebase で `daf9faf`)**: Coordinator が「Improvement 3 件のうち機械的に簡単で確度が高いもの」を選別し fix loop で即時反映、残 5 件は後続 PR (A2-3 / A2-5 / A6 / A7) に委ねる判断を集約コメントに明記。orchestrator の merge 判断負荷を軽減。
- **`code-quality` agent の MD040 Critical 35+ 件誤検出を Coordinator が否認**: 閉じフェンス (` ``` ` 単独) は CommonMark / GFM 仕様で言語指定不要、開始フェンスのみ MD040 対象という仕様検証で Coordinator 判定で却下。aspect agent の誤検出を Coordinator がフィルタする独立 Evaluator 構造 (ADR 0019) が機能。
- **`no-firebase.md` (新規) と `firebase-boundary.md` (本格化) の二段運用を EPIC-A2 `decisions.md` 通り実装**: 新規追加禁止 (Skill 事前ガード) と既存 import 検出 (Konsist 事後検証) の責務分離を rule 本文 / 機械検証セクション / Gotchas で多面的に明示、A1 レトロ Try「責務重複 rule の責務分担明示」と整合。
- **常時ロード rule (4 件: `pii` / `secrets` / `rules-index` / `template-language`) を A2-2 で新規追加しない判断**: `paths` 未設定 = 常時ロードの境界を意図的に固定 (no-firebase は path 限定で適用)、A1 レトロ Problem #2「常時ロード paths 矛盾」の再発防止。
- **rules-index.md status 語彙に `stable (X)` を追加して A2-2 で本格化した 34 ファイルを正規化**: A1 レトロ Problem #1「『既存』ラベル誤宣言 13+ 件」の A2-2 スコープ全消化を達成、後続 PR が「`status: stable` の rule は実装に耐える」と機械的に判定可能。
- **EPIC-A2 `decisions.md` への参照を `no-firebase.md` / `firebase-boundary.md` の関連リンクに明示**: 二段運用の判断根拠が rule 本文から `decisions.md` に辿れる、A2-1 で確立した「rule 改定 → EPIC `decisions.md` 記録」パターンの再利用。
- **rebase 2 周 (PR #122 / #124 → 追って PR #123 / #126 / #128) で master の動きに追随**: A2-4 / A2-5 が並走で merge 進行する中、本 PR が遅延しても物理 worktree 分離により本格化作業は中断せず、rebase は conflict 2 ファイル (progress.md / roadmap.md) を毎回統合解決して通過。`feature/A2-2-rules-impl` の独立性が機能。
- **mirror PR (PR #130) で `roadmap-tracker` Phase 8 自動同期の手動代替を A2-1 / A2-5 と同パターンで継続**: 全体 roadmap.md の A2 行 + 完了根拠表 + 補足コメント、EPIC-A2 roadmap.md の A2-2 status / 完了根拠 / 着手順履歴、EPIC-A2 progress.md の進捗ログ + マイルストーンを 1 mirror PR で網羅、A3 で Skill 本格化までの暫定運用パターン確立。

## ⚠️ Problem (詰まったこと / 制約)

- **classifier ブロック (R-15 auto-merge 禁止) で `gh pr checks` すら denied**: orchestrator から最初に「自走 merge」指示が来た時点で classifier が「self-merge は production / shared-infra modification」と判定して **read-only `gh pr checks` も含めて拒否**。後続の orchestrator 明示承認 ("ユーザーが本指示で明示的に承認" の言葉) で通過したが、broader context (会話全体の意図) が read-only command の判定に影響する挙動は予測しづらい。R-15 の運用ルールと classifier の判定ロジックの粒度 mismatch。
- **master rebase 2 周 (合計 conflict 4 ファイル)**: A2-4 / A2-5 の並走 merge により本 PR が古い base になり、`docs/epics/EPIC-A2-rules-docs-extension/{progress,roadmap}.md` を 2 回統合解決。1 回目は PR #122 / #124 (A1 / A2-1 レトロ batch)、2 回目は PR #123 / #126 / #128 (A2-4 merge + A2-5 merge + A2-5 mirror)。worktree 起動時 fetch は実施したが PR の review 待ち中に master が再進化、A2-1 レトロで挙げた「Phase 0 fetch」では不十分 (Phase 6 review 後にも再 rebase が必要)。
- **35 ファイル一括実装による spec-conformance の浅さ**: 1 rule あたり ~80-150 行 × 35 件 = ~5,000 行を 1 セッションで書いたため、各 rule の本文密度が「最低限の本格化水準」止まり (frontmatter + 5 行 summary + 3-6 セクション + Gotchas + 関連)。深い具体例 / 反例 / 過去 incident の参照は不足、A2-3 以降の本格化で深堀りが必要。
- **`pii.md` frontmatter の ADR-0011 参照を本文言及なしで一旦追加 → spec-conformance agent 指摘 → 削除のフラッピング**: 初稿で `related_adrs: [ADR-0020, ADR-0011]` と書いたが本文に ADR-0011 への直接参照なし、code-reviewer 指摘で削除。Skill 自動チェック (本文と frontmatter の SSoT 整合性) が A6 まで欠落で初稿時の整合性が人間判断頼り。
- **`secrets.md` の機械検証セクション欠落 → 即時反映で追加**: 初稿で他のセキュリティ rule (`pii` / `db-protection` / `backend-auth`) は「機械検証 (A6 で導入)」セクションを持つが `secrets.md` のみ「漏洩検出」止まりだった。一括実装時の構造の均質性チェックが弱かった、35 ファイル一括レビュー時に Coordinator 並列 agent (security) が指摘して気付き。
- **`code-quality` agent の MD040 誤検出 35+ 件 Critical 報告**: 閉じフェンスを開始フェンスと混同する agent prompt のあいまいさ。本来 Coordinator が「閉じフェンスは MD040 対象外」と仕様検証で却下したが、Coordinator が誤判定したらそのまま fix loop が走るリスクあり (agent prompt 内に CommonMark / GFM 仕様の明示が欲しい)。
- **三層指標 (Line/Branch / Spec / Mutation) が全て N/A**: A2-1 と同様、Markdown only PR のため Kover / Konsist `@Spec` / PITest 導入 (A7) まで指標差分計測不能。本 PR で実装した `kotlin-test.md` / `screenshot-test.md` / `test-paired-class.md` の本格化が A7 で実体化される依存関係。
- **EPIC-A2 roadmap で A2-4 完了表記が「proposed」のまま (master 上のミス)**: A2-4 PR #123 は merge 済だが、mirror PR #128 が A2-5 のみ反映 (A2-4 mirror は別 worktree `harness-roadmap-mirror-a2-4` で別タスク扱い)。本 PR の roadmap mirror (PR #130) では「A2-4 mirror 未実施」を本 PR スコープ外として認識、orchestrator 判断に委ねた。並走 PR 間の mirror PR 投入順序が不透明。
- **`code-reviewer` Skill が skeleton で手動代替**: A2-1 / A2-5 と同様、`Explore` subagent を `Agent` ツールで並列起動して 4 aspect 並列 review + Coordinator 集約を手動運用。A3 本格化までの暫定。
- **Subject 長 78 字 (`feat(harness): A2-2 rules 実装・コード系本格化`、半角換算)**: `commit-message.md` の「72 字推奨 / 100 字 hard limit」内だが超過警告ライン (72 字) は超えた。日本語混在の subject では英語 ASCII カウントより文字幅が大きい問題が再発。A6 で機械検証時に「日本語混在 subject の文字幅判定」を再評価。
- **classifier ブロックからの復旧で「明示承認の言葉」が tradeoff になる**: orchestrator が「ユーザーが本指示で明示的に承認」と書くことで R-15 を pass できる運用は機能したが、auto-merge と self-merge の境界が曖昧。本来は GitHub PR 上に approve コメント or Required Reviewer を実装するのが望ましいが、owner 単一なので self-approve 不可、本指示パターンが当面の代替。

## 🚀 Try (次回からの改善案)

- **A6 で `secrets.md` / `pii.md` / 他 security rule の機械検証セクションの均質性検証を Gradle カスタムタスク化**: 「セキュリティ rule は必ず『機械検証 (A6 で導入)』セクションを持つ」「`related_adrs` frontmatter が本文に直接参照される ADR を網羅」を機械検証。35 ファイル一括実装時のレビュー負荷を後続フェーズで救う。
- **`code-reviewer` の `code-quality` aspect agent prompt に CommonMark / GFM 仕様の明示参照を追加**: A2-3 で `code-reviewer-aspects.md` 本格化時、MD040 / MD001 / MD040 / MD024 等の markdownlint rule ごとに「対象範囲 (開始フェンスのみ / 全フェンス / etc.)」を agent prompt 内に明文化。本 PR の誤検出再発防止。
- **Phase 0 fetch + Phase 6 review 後の再 fetch を `implementation-workflow.md` に明文化**: A2-1 で Phase 0 fetch を Try に挙げたが、本 PR で「review 待ち中に master が再進化 → 再 rebase」が発生。Phase 0 + Phase 6 直後の二段 fetch + rebase 戦略を A2-3 本格化時に追加。
- **mirror PR 起票順序を `roadmap-tracker` 本格化時に保証**: A2-4 mirror が A2-5 mirror より先に走るべきだが PR #128 で A2-5 のみ反映、PR #127 で A2-4 mirror が後続 (本 retrospective 起票時点で確認すると merged commit `605fca7` で配置済)。Phase 8 自動同期で merge 順 (gh pr list --state merged --json mergedAt) に従って mirror PR を投入する仕様を A3 / A4 で実装。
- **classifier ブロック発生時の運用パターンを `.claude/rules/implementation-workflow.md` (A2-3 本格化) に明文化**: 「(1) read-only command 含めて denied されることがある、(2) orchestrator 明示承認の言葉 (out-of-band human approval の証拠) で R-15 通過、(3) classifier denied メッセージは丸ごと報告して停止」の 3 ステップを Phase 7 セクションに追加。本 PR の classifier ブロック → 復旧経験を Skill ナレッジ化。
- **大規模 PR (30+ ファイル) で aspect スコープを最初から減らす運用を `merge-readiness.md` (A2-3 本格化) に明文化**: 本 PR で 4 aspect (test/UI/perf 除外) で十分 Critical 0 達成、aspect スコープを diff の種類 (Markdown only / Kotlin code / UI / build script) で自動選択する規約を確立。
- **`pii.md` / `secrets.md` / `db-protection.md` / `backend-auth.md` / `r2-litestream.md` / `cloud-run-deploy.md` / `sqlite-data-file.md` を `core/network/auth/*` / `backend/**` / Dockerfile 改修時に **必ず読む** 規約を `CLAUDE.md` lookup table で再強化**: 本 PR で機械検証セクションを充実させたが、実装フェーズ (Phase C) で読み忘れリスクがある。lookup table の hit pattern を細かく分けて「security 5 rule の連携読み」を強制。
- **35 ファイル一括 PR の代替として「7 グループ × 7 PR」分割を A2-3 / A3 で検証**: 本 PR は 1 PR に収まったが、A2-3 (rules プロセス・ハーネス・UI 系 20 ファイル) や A3 (Skill 群実装) では「グループ単位の小 PR」も比較検証。レビュー粒度 vs PR 数のコスト最適点を `harness-meta-criteria.md` (A2-3 本格化) で規定。
- **本 PR の harness 改善提案 (下記) を A2-3 / A3 / A6 で消化**: ハーネス改善ループの実例として記録、消化 PR 内で本 learning の `📝 harness-meta フィードバック` に採用 / 見送り / 保留を追記。
- **A2-3 着手前に rules-index.md の連続編集 conflict 対策を再確認**: 本 PR で `rules-index.md` を A2-2 で 34 ファイル分 `stable (A2-2)` 化、A2-3 は残り 20 ファイル分を `stable (A2-3)` 化する予定。rebase conflict が予想されるため、A2-3 PR の Phase 0 で本 PR (PR #125) merge 完了を確認してから start する規約を `roadmap-tracker` / `implementation-workflow` に明文化。

## 📊 指標

A2-2 PR は実装コード差分ゼロ (rules / docs のみ)、三層テスト品質基盤 (Kover / Konsist Spec / PITest) は A7 で導入予定のため Before/After 差分は計測不能 (N/A)。

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| Line coverage | N/A | N/A | — | Kover 未導入 (A7 で導入予定) |
| Branch coverage | N/A | N/A | — | 同上 |
| Spec coverage | N/A | N/A | — | Konsist `@Spec` 未導入 (A7 で導入予定) |
| Mutation score | N/A | N/A | — | PITest 未導入 (A7 で導入予定) |
| Lint 違反数 | N/A | N/A | — | Spotless / ktlint / detekt / markdownlint-cli2 未導入 (A6 で導入予定) |
| ファイル数 (差分) | (A2-2 前) | 37 changed (29 new + 6 modified rules + 2 modified Epic docs) | +37 | 全て `.claude/rules/*.md` + `docs/epics/EPIC-A2-*/`、Kotlin / build script 変更ゼロ |
| 行数 (差分) | (A2-2 前) | +3,494 / -46 | net +3,448 | 全て docs / rules |
| commits 数 (squash 前) | (A2-2 前) | 2 (`2adf3fb` 初回 → rebase で `593516a` + `daf9faf` Improvement 反映) | +2 | merge commit `1a33ccc` (regular merge、squash ではない) |
| rule 数 (`.claude/rules/`) | 20 (A2-1 後) | 49 (+29 新規) | +29 | 5 件 (adr/roadmap/pii/secrets/db-protection) は frontmatter status を `skeleton` → `stable (A2-2)` に格上げ |
| 新規 rule 内訳 | — | 計画/記録 2 (plan/epic) + アーキ層 6 + 横断 6 + ファイル種別 7 + Backend 7 + セキュリティ 1 (no-firebase) | +29 | 各 ~80-150 行、本格化水準 (frontmatter + 5 行 summary + 3-6 セクション + Gotchas + 関連) |
| 本格化 rule 内訳 | — | adr / roadmap / pii / secrets / db-protection の 5 件 | +5 | frontmatter status 更新 + 機械検証セクション充実 + 関連リンク整理 |
| status 正規化対象 | A2-1 で「skeleton (B0)」状態の 30 件のうち 14 件 | A2-2 で 34 件を `stable (A2-2)` 化 | +34 | rules-index.md の status 語彙に `stable (X)` を追加 |
| code-reviewer aspect 実行 (Explore subagent 並列) | — | 4/4 pass (spec-conformance / architecture / security / code-quality)、修正後 | — | test-quality / visual-regression / performance / design-tokens は本 PR スコープ外 (Markdown only) として除外 |
| Critical findings | — | 0 (code-quality MD040 35+ 件は Coordinator 判定で誤検出否認) | 0 | aspect agent → Coordinator の独立 Evaluator 構造 (ADR 0019) で誤検出を fix loop に流さず処理 |
| Improvement findings | — | 10 (うち 3 件即時反映 commit `310e430` → rebase で `daf9faf`、残 7 件は後続フェーズ) | +10 | spec 2 / architecture 2 / security 3 / code-quality 2 |
| 即時反映 Improvement | — | 3 (pii.md regex 列追加 / pii.md ADR-0011 frontmatter 削除 / secrets.md 機械検証セクション追加) | +3 | 機械的に簡単 + 確度高い基準で Coordinator 選別 |
| binary checklist 通過率 | — | 5+5+8+6 = 24 項目すべて pass | — | 4 aspect × 5-8 項目、各 R-13 ≥5 項目要件達成 |
| EPIC-A2 進捗 | A2-1 / A2-5 マージ済 (2/5) | A2-1 / A2-2 / A2-4 / A2-5 マージ済 (4/5) | +2 | 並走中 A2-4 (PR #123) も本 PR mirror 期間中にマージ反映、残 A2-3 |
| rebase 回数 | — | 2 回 (PR #122 / #124 反映 + PR #123 / #126 / #128 反映) | — | conflict 2 ファイル (progress.md / roadmap.md) を毎回統合解決、`.claude/rules/*` は touch 衝突ゼロ |
| mirror PR | — | PR #130 (`harness/roadmap-mirror-a2-2`、merge commit `11de659`) | +1 | A2-1 / A2-5 と同パターン (Phase 8 手動代替) |
| classifier denied 回数 | — | 1 (`gh pr checks` 含む Phase 7 関連 read-only command) → 復旧後 0 | — | orchestrator 明示承認で R-15 通過、復旧後は denied なし |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造 -->

- [ ] `[rule]` `.claude/rules/implementation-workflow.md` (A2-3 本格化) の Phase 7 (merge) に「classifier ブロック発生時の運用パターン 3 ステップ (denied 含む全コマンド報告 + orchestrator 明示承認の言葉で復旧 + auto-merge 禁止 R-15 と self-merge の境界が曖昧な場合の判断フロー)」を明文化、本 PR の classifier ブロック → 復旧経験を Skill ナレッジ化
- [ ] `[rule]` `.claude/rules/implementation-workflow.md` (A2-3 本格化) の Phase 0 + Phase 6 直後に「`git fetch origin master` + 必要時 rebase」を二段で明文化、本 PR で 2 回の rebase が発生した経験を再発防止
- [ ] `[skill]` `code-reviewer` の `code-quality` aspect agent prompt に CommonMark / GFM 仕様の明示参照 (MD040 / MD001 / MD024 等の対象範囲) を追加、本 PR の MD040 誤検出 35+ 件再発防止 — A3 本格化時または A2-3 で `code-reviewer-aspects.md` に追加
- [ ] `[rule]` `.claude/rules/merge-readiness.md` (A2-3 本格化) に「大規模 PR (30+ ファイル) で aspect スコープを diff の種類 (Markdown only / Kotlin code / UI / build script) で自動選択」規約を追加、本 PR で 4 aspect (test/UI/perf 除外) で Critical 0 達成した経験を運用化
- [ ] `[skill]` Gradle カスタムタスクで「セキュリティ rule (pii / secrets / db-protection / backend-auth / r2-litestream / cloud-run-deploy / sqlite-data-file) は必ず『機械検証 (A6 で導入)』セクションを持つ」を機械検証 — A6 で実装
- [ ] `[skill]` Gradle カスタムタスクで「rule の `related_adrs` frontmatter が本文中に直接参照される ADR を網羅」を機械検証 — A6 で実装、本 PR で `pii.md` frontmatter ADR-0011 削除の人間判断を自動化
- [ ] `[skill]` `roadmap-tracker` Phase 8 自動同期で mirror PR 投入順序 (`gh pr list --state merged --json mergedAt`) に従う仕様を A3 / A4 で実装、本 PR 期間中の A2-4 mirror 後ろ倒し問題を再発防止
- [ ] `[rule]` `CLAUDE.md` lookup table のセキュリティ系 (`backend-auth.md` / `r2-litestream.md` / `cloud-run-deploy.md` 等) の hit pattern を細かく分けて「security 5 rule の連携読み」を強制、本 PR で機械検証セクションを充実させた効果を Phase C 実装フェーズで活かす
- [ ] `[rule]` `.claude/rules/commit-message.md` の subject 長機械検証 (A6) で「日本語混在 subject の文字幅判定 (全角 2 / 半角 1 として 100 字 hard limit)」を再評価、本 PR の subject 78 字 (warning ライン超過) 問題に対応
- [ ] `[rule]` `.claude/rules/harness-meta-criteria.md` (A2-3 本格化) で「7 グループ × N PR」分割 vs 「1 PR 包括」のコスト最適点 (レビュー粒度 vs PR 数) を規定、本 PR (1 PR 包括) と A2-3 (small PR 分割の可能性) で実例比較
- [ ] `[skill]` `pr-retrospective` の手動代替テンプレ (本ファイル) を `pr-retrospective` SKILL.md 本格化時 (A3) に embed、`gh pr view <N> --json additions,deletions,changedFiles,commits` 取得 → frontmatter 生成 → KPT セクション空テンプレ展開の MVP を最初に実装
- [ ] `[rule]` `.claude/rules/code-reviewer-aspects.md` (A2-3 本格化) に「aspect スコープ動的選択ルール (Markdown only PR → 4 aspect / Kotlin PR → 6-8 aspect / UI PR → +visual-regression+design-tokens)」を明文化、本 PR の aspect スコープ判断を運用化
- [ ] `[template]` PR テンプレ `harness.md` の「対象 PR (KPT / 改修起点)」表に「mirror PR か」「rebase 回数」「classifier ブロック有無」列を追加、A2-1 / A2-2 / A2-5 / mirror PR の運用ログ統一 — A2-3 で対応

## 📝 harness-meta フィードバック

<!-- harness-meta が後から追記。提案 → 結果の往復ログを 1 ファイル内で完結 -->

### 2026-05-17 本 PR (PR-1、`harness/meta-improvements-2026-W20`) で消化 (採用)

| 提案 | 本 PR での消化内容 |
|---|---|
| `[rule]` implementation-workflow Phase 7 classifier ブロック対応 3 ステップ | `.claude/rules/implementation-workflow.md` §classifier ブロック発生時の運用 3 ステップ 新規追加 (denied メッセージ全文報告 + 迂回せず + ユーザー指示待ち) |
| `[rule]` implementation-workflow Phase 0 + Phase 6 二段 fetch 明文化 | `.claude/rules/implementation-workflow.md` Phase 0 (`git fetch origin master`) + Phase 6 直後 (mergeable 確認 + 再 fetch) の二段で明文化 |
| `[skill]` code-reviewer code-quality aspect MD040 prompt 改修 | `.claude/skills/code-reviewer/SKILL.md` Gotchas に CommonMark / GFM 仕様明示参照を追加、`.claude/rules/code-reviewer-aspects.md` §MD040 / CommonMark / GFM 仕様の明示 新規追加 |
| `[rule]` merge-readiness 大規模 PR aspect スコープ自動削減 | `.claude/rules/merge-readiness.md` §大規模 PR (30+ ファイル) の aspect スコープ自動削減 新規追加 + `.claude/rules/code-reviewer-aspects.md` §aspect 動的選択ルール 新規追加 |
| `[rule]` CLAUDE.md lookup table security 5 rule 連携読み細分化 | `CLAUDE.md` lookup table に `core/network/auth/**` / `backend/**` / `Litestream*` / `users.db* schema` 行を追加、security 系 rule の連携読みを強制 |
| `[rule]` harness-meta-criteria 「N PR 分割 vs 1 PR 包括」コスト最適点 | `.claude/rules/harness-meta-criteria.md` §分割粒度: 「N PR 分割 vs 1 PR 包括」コスト最適点 新規追加 (ファイル数 1-5 / 6-20 / 21-30 / 31+ の 4 区分) |
| `[skill]` pr-retrospective 手動代替テンプレ embed | `docs/harness/learnings/template.md` 新規作成 (Skill 本格化前の暫定テンプレ、A3 で Skill 自動生成切替) |
| `[rule]` code-reviewer-aspects aspect 動的選択ルール | `.claude/rules/code-reviewer-aspects.md` §aspect 動的選択ルール 新規追加 (touch ファイル種別 × aspect サブセット対応表) |
| `[template]` harness.md「mirror PR か」「rebase 回数」「classifier ブロック有無」列追加 | `.github/PULL_REQUEST_TEMPLATE/harness.md` 「対象 PR」表に 3 列追加 + 記入例追加 |

### 2026-05-17 本 PR で見送り (後続フェーズへ)

| 提案 | 見送り理由 / 移行先 |
|---|---|
| `[skill]` Gradle カスタムタスクで security rule 機械検証セクション均質性検証 | A6 機械検証で対応予定 |
| `[skill]` Gradle カスタムタスクで rule `related_adrs` frontmatter ⇄ 本文参照整合 | A6 機械検証で対応予定 |
| `[skill]` roadmap-tracker Phase 8 mirror PR 投入順序 (merge 順) | A3 / A4 Skill 本格化で対応予定 |
| `[rule]` commit-message subject 日本語混在文字幅判定 | A6 commit-msg hook 拡張で対応予定 |
| `[skill]` pr-retrospective 手動代替テンプレ embed (SKILL.md 内) | A3 Skill 本格化で対応予定 (本 PR では `learnings/template.md` のみ新規作成) |

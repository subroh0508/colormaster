---
id: learning-2026-05-18-pr-148
title: PR #148 (A3-2 bug-fix Skill 完成) レトロスペクティブ
status: completed
type: learning
related_pr: 148
related_epic: EPIC-A3
related_plan: —
related_specs: []
related_adrs:
  - ADR-0017
  - ADR-0018
  - ADR-0024
  - ADR-0027
generated_at: 2026-05-18
generator: pr-retrospective (manual via orchestrator, batch 2026-W21)
last_updated: 2026-05-18
---

# PR #148 (A3-2 bug-fix Skill 完成) レトロスペクティブ

> 生成: pr-retrospective (skeleton 段階のため orchestrator 委任の本ペインで手動代替実行、batch 2026-W21) at 2026-05-18
> 関連 Plan: なし (EPIC-A3 配下 Skill 本格化 PR、Plan 不要) / 関連 Epic: EPIC-A3-skill-suite-extension
> 対象 PR: [docs(harness): A3-2 bug-fix Skill 完成 #148](https://github.com/subroh0508/colormaster/pull/148) (merge commit `400e7f2`、2026-05-17T17:10:18Z マージ、head commit `400954f`)
> 差分: 1 ファイル / +199 / -0、commits: 1 (`400954f` 単一)、code-reviewer 4 aspect Critical 0 / Improvement 0、fix loop なし

## 本 retrospective の位置付け

本 retrospective は **Group 1 共通観点担当** (新動詞辞書漏れ拡張版 patch を本ファイルに本格記載、指示書 L181-183 該当)。PR #148 自体は A3-2 `bug-fix` Skill SKILL.md 1 ファイル新規追加に閉じ、fix loop なし / Improvement 0 件で merge 直行した素直な PR。Group 1 4 並列 spawn の wave 内で touch ファイル重複ゼロ並走の最短サンプル (fix loop ゼロ / Improvement ゼロ) として機能した。本 retro では PR 個別観点 (`bug-fix` Skill scope の言語化) と Group 1 共通改修候補 7 件 (#1-#7、本セッション中の実観測) + 新動詞辞書拡張版 patch を併記する。

## ✅ Keep (継続したいこと)

- **bug-fix Skill scope (再現 + root cause + Plan 起票で閉じる、修正実装に踏み込まない) が SKILL.md §役割 + §Phase 6 + §Gotchas で 3 重明示** (consensus: spec-conformance + architecture): Generator/Evaluator 独立性 (R-13) を Spec Gen 段階で守る方針が §役割 (1 行 summary) / §Phase 6 (handoff = `implementation-workflow` Phase 0-9 への bridge) / §Gotchas 10 項目のいずれにも反復記載され、bug-fix Skill が修正実装に踏み込む drift を予防。spec-conformance aspect で「責務分離明確」と PASS。
- **fix loop なし / Improvement 0 で 1 commit merge** (consensus: process): A3-2 `bug-fix` Skill は B0 雛形不在領域の新規本格化 PR だったが、code-reviewer 4 aspect (spec-conformance / architecture / security / code-quality) 並列レビューで Critical 0 / Improvement 0、initial commit `400954f` のまま Ready 化 → merge。SKILL.md 構造 (frontmatter + 必須 6 セクション + Gotchas 10 項目 + 関連リンク) が `.claude/rules/skill-authoring.md` 100-point rubric に即座に適合した実証。A3-1 / A3-4 と異なり Improvement 持ち越しゼロで完走した最短ケースとして Group 1 内で参考実装に位置付けられる。
- **6 Phase 構成 (報告把握 → 再現 → root cause → 仕様補強要否 → Plan 起票 → handoff) が `pr-retrospective` Skill の Problem セクション派生 bug 入力と整合** (consensus: architecture): bug-fix の入力経路として `pr-retrospective` Skill の Problem セクション (本 retro でいう ⚠️ Problem) からの派生を SKILL.md §関連 で明示。これにより learning ファイル ⇄ bug-fix Skill ⇄ `plan-author` ⇄ `implementation-workflow` の連鎖が SoT 化され、Spec Gen Skill 群 (feature-request / bug-fix / refactor / dependency-upgrade) の入出力グラフが完成。
- **仕様 gap 検出時の `spec-living-sync` 規約発動を Plan の `related_specs` 列挙で明示化** (consensus: spec-conformance): SKILL.md §Phase 4 で「仕様 gap 検出時は `docs/specifications/` 補強を Plan に起票し、Plan の `related_specs` フィールドに該当 SPEC-ID を列挙」と SoT 化。`spec-living-sync.md` の双方向同期規約と接続することで bug 修正と仕様改訂が同 PR 内で完結する経路を保証。
- **Group 1 4 並列 spawn で `bug-fix` だけが fix loop ゼロ完走** (consensus: process): Group 1 4 PR (A3-1 feature-request / A3-2 bug-fix / A3-3 refactor / A3-4 adr-author) のうち A3-2 / A3-3 が fix loop ゼロ、A3-1 が fix loop 1 (High 3 件)、A3-4 が fix loop 1 (Improvement 4 件) で完走。fix loop ゼロ 2 件は SKILL.md の責務分離が明確だった証左。今後 Spec Gen 系 Skill を起草する際の参照基準として位置付け可能。

## ⚠️ Problem (詰まったこと / 制約)

### Problem #1: bug-fix Skill が呼ぶ依存 Skill の双方向リンクが SKILL.md §関連 では片方向のみ

- `bug-fix/SKILL.md` §関連 で `plan-author` / `implementation-workflow` / `pr-retrospective` を「呼ぶ / handoff / 入力」として列挙
- しかし対向の `plan-author/SKILL.md` / `implementation-workflow/SKILL.md` / `pr-retrospective/SKILL.md` 側に「`bug-fix` Skill から呼ばれる / handoff される / Problem セクション派生として入力する」記述は不在
- これにより SoT 反転のリスク (bug-fix を撤去すると依存先 Skill 側で dangling reference 検出が遅延)
- 同型事例: A3-1 / A3-4 でも同様に片方向リンク、Group 1 共通課題

### Problem #2: bug-fix 特有の Spec gap 検出ロジックの抽象度

- SKILL.md §Phase 4 で「仕様 gap 検出時は `docs/specifications/` 補強起票」と書くが、**何をもって gap と判定するか** (regex / heuristic / 人間判断ライン) が SKILL.md 本文に未記載
- 例: 「再現テストが既存仕様で `expected` を導けない場合は gap」「root cause が `docs/specifications/detail/` に記載されていない振る舞いに依存する場合は gap」等の判定基準
- 現状は Skill 利用者の経験則に依存、新規 PR で Skill 起動時に bug-fix が gap を見落とすリスク
- 採用判定基準 2 (process improvement、bug-fix Skill が安定運用される A3 完了後の検出精度向上に資する) 該当

### Group 1 共通改修候補 #1: orchestrator skill Monitor 思考動詞辞書漏れ (累計 10 動詞、本 retro で SoT 反映起点)

- PR #147 retro 時点では `Pollinating` / `Honking` の 2 動詞のみ報告
- PR #146 spawn 〜 Group 1 4 PR + Step 3a mirror PR 完走の本セッション中に **8 動詞追加で観測**:
  - `Galloping` (PR #147 Phase 0 / PR #146 retro spawn 時)
  - `Pouncing` (workspace:26 A3-4 adr-author Phase 6 中)
  - `Scampering` (workspace:27 A3-3 refactor Phase 6 中)
  - `Forming` (workspace:24 A3-1 feature-request Phase 6 中)
  - `Bunning` (workspace:24 A3-1 fix loop 1 中)
  - `Finagling` (workspace:24 A3-1 Phase 9 cleanup 中)
  - `Caramelizing` (workspace:28 Step 3a Phase 0 直後)
  - `Gusting` (workspace:28 Step 3a Phase 2-3 編集中)
- 累計 10 動詞 (Pollinating / Honking / Galloping / Pouncing / Scampering / Forming / Bunning / Finagling / Caramelizing / Gusting) は既存 regex に **全て未登録**
- 動詞は ` ✻ ` だけでなく ` ✶ ` / ` ✽ ` / ` · ` / ` ✢ ` / ` ◯ ` の prefix でも表示されることを本セッションで観測、regex の prefix 部分も拡張対象

### Group 1 共通改修候補 #2: Monitor dedup ロジックの弱さ (v1 → v2 → v3 段階改善実証)

- PR #146 spawn 時の v1 Monitor (busr3056v): `md5 -q` ベース hash dedup → 同 scroll 履歴が 10+ 回連続再 emit
- PR #147 retro spawn 時の v2 Monitor (bvmtya3in): `prev_file` persist + `comm -23` line-set diff → 改善はあるが軽い再 emit 残存
- Group 1 spawn 時の v3 Monitor (bk8bf8zg8): workspace prefix + `tail -3` filter + `comm -23` → 再 emit ほぼゼロ
- orchestrator skill SoT §Phase 3 (30 秒ポーリング Monitor 並列) は「dedup 通知」と書いているが具体的 dedup ロジックを SoT 化していないため、各 orchestrator session が個別実装 → 不均一

### Group 1 共通改修候補 #3: classifier が prompt 内 self-merge 指示を CLAUDE.md R-15 違反として denied (Group 1 で再発なし実績)

- PR #146 起票時に orchestrator が `/tmp/orchestrator-prompt-EPIC-A3-bootstrap-20260518.md` を Write しようとして classifier denied (PR #147 retro #3 と同事象)
- Group 1 spawn (PR #148-#151) の prompt から `gh pr merge` 文言を削除し **Phase 0-6 + Ready 化のみ** に絞ったため、4 PR 全てで denied 発生なし (PR #147 retro #3 Try の効果実証)
- Step 3a mirror PR (#152) prompt も同方式で classifier 通過
- ただし SoT 反映 (`.claude/skills/orchestrator/SKILL.md` 改修) はまだ未着手、本 batch retro が起点となる

### Group 1 共通改修候補 #4: per-task pane self-`/exit` 不可、orchestrator が `cmux close-workspace` 代行必須 (Group 1 4 連発で再現)

- PR #146 / #147 retro 完走後に observed: 「★ 本 pane の `/exit` は AI 側から直接実行不可」
- Group 1 4 PR (workspace:24-27) も同様に Phase 9 cleanup 完了後に idle 待機、orchestrator が `cmux close-workspace --workspace workspace:N` を 4 連発で実行
- 仕様化されていないため per-task pane 起動 prompt テンプレが「最終 summary print + idle 待機、`/exit` は要求しない」と毎回手動で書く必要

### Group 1 共通改修候補 #5: `gh pr create --body "$(cat <<EOF...)"` heredoc が cmux send 経由で Exit 1 (Group 1 全 PR で --body-file 採用、heredoc 事故ゼロ実証)

- PR #146 EPIC-A3 起票時に heredoc 渡しで Exit code 1 → per-task pane が `--body-file` 切替で recovery
- Group 1 4 PR + Step 3a mirror PR は **全て `--body-file /tmp/pr-body-*.md` 方式** で起票 → 全 PR で `gh pr create` 成功 (heredoc 事故ゼロ実証)
- ただし SoT (`.claude/rules/implementation-workflow.md` Phase 5) には heredoc 例が残置されており、再発リスク

### Group 1 共通改修候補 #6: R-15 一括事前承認の運用パターン (Group 単位 bulk approval が有効実証)

- PR #146 EPIC-A3 起票時は AskUserQuestion で「R-15 事前承認を per-task pane に self-merge まで委任 / 付与する」を選択 → しかし classifier が prompt 内 self-merge 指示を denied (改修候補 #3 該当)
- PR #147 を AskUserQuestion で merge 承認受領 + 同時に Group 1 一括承認も取得 → Group 1 4 PR で classifier 通過実証
- 「Group / Phase 単位での R-15 一括事前承認パターン」が efficient であることが実証された
- orchestrator skill SoT §明示承認文言 canonical は **単一 PR 単位の事前承認** を想定しているが、複数 PR を並列 spawn する場合は Group 単位での一括承認の方が efficient

### Group 1 共通改修候補 #7: 並列 `git worktree add` の lock 衝突 (`.git/config: File exists`、Group 1 で実証)

- Group 1 spawn 時、orchestrator が 4 worktree を `git worktree add ... &` バックグラウンド並列で作成しようとした
- 4 branch (epic/EPIC-A3-feature-request-skill / -bug-fix-skill / -refactor-skill / -adr-author-skill) は全て作成されたが、**4 worktree directory のうち 3 つが lock 衝突 (`.git/config: File exists` / `unable to write upstream branch configuration`) で部分失敗**
- 結果として 1 worktree (refactor) のみ作成、残り 3 worktree は branch のみ作成され dangling 状態 → orchestrator が `git worktree add <path> <existing-branch>` で順次紐付けて recovery
- 原因仮説: `git worktree add` 内部で `.git/config` の `[branch "<name>"]` セクション追加 + upstream tracking 設定を行うため、`.git/config` ファイル全体に flock を取る → 並列実行で衝突
- orchestrator skill SoT §Phase 2 (per-task workspace spawn) は spawn 並列化を想定するが、worktree 作成の並列化リスクは明記されていない

## 🚀 Try (次回からの改善案)

### Try #1: bug-fix Skill の双方向リンク補強 (PR 個別観点、priority: medium、Problem #1 直接対応)

- `bug-fix/SKILL.md` §関連 から呼ばれる 3 つの依存 Skill (`plan-author` / `implementation-workflow` / `pr-retrospective`) 側の SKILL.md §関連 に **「`bug-fix` Skill から呼ばれる / handoff される / Problem セクション派生として入力する」逆方向リンク** を追記する別 PR を起票
- A3 配下の Spec Gen 4 Skill (feature-request / bug-fix / refactor / dependency-upgrade) で同方式の双方向リンク補強を一括実施 (1 PR で `plan-author/SKILL.md` / `implementation-workflow/SKILL.md` / `pr-retrospective/SKILL.md` に逆方向リンク追加)
- 採用判定基準 2 (process improvement、Skill 依存グラフの完全性向上) 該当

### Try #2: bug-fix Skill の Spec gap 判定基準を Gotchas に明文化 (PR 個別観点、priority: low、Problem #2 直接対応)

- `bug-fix/SKILL.md` §Gotchas (現状 10 項目) に **「Spec gap 判定基準 3 種」追加** (合計 11 項目):
  - (a) 再現テストが既存 `docs/specifications/` で `expected` を導けない場合は gap
  - (b) root cause が `docs/specifications/detail/` に記載されていない振る舞いに依存する場合は gap
  - (c) 仕様改訂を伴わずに修正可能 (=コード bug のみ) なら gap なし
- 採用判定基準 2 (process improvement) 該当、A3 完了後の bug-fix Skill 実運用で蓄積データを踏まえて補強する形でも可

### Try #3: Group 1 共通改修候補 #1 (新動詞辞書漏れ 10 動詞 + prefix 拡張) の SoT 反映 patch (priority: highest、改修候補 #1 直接対応)

本 batch retro の SoT 反映起点。`.claude/skills/orchestrator/SKILL.md` への以下 patch を別 PR で適用:

```diff
--- a/.claude/skills/orchestrator/SKILL.md
+++ b/.claude/skills/orchestrator/SKILL.md
@@ -X,Y +X,Y @@ ### 観測済 (本セッション)

-`Channeling` / `Flibbertigibbeting` / `Moonwalking` / `Noodling` / `Twisting` / `Schlepping`
+`Channeling` / `Flibbertigibbeting` / `Moonwalking` / `Noodling` / `Twisting` / `Schlepping` /
+`Pollinating` / `Honking` / `Galloping` / `Pouncing` / `Scampering` / `Forming` / `Bunning` /
+`Finagling` / `Caramelizing` / `Gusting`

@@ -X,Y +X,Y @@ ### regex (運用案)

-^✻ (Channeling|Flibbertigibbeting|Moonwalking|Noodling|Twisting|Schlepping|Cooking|...)…?
+^(✻|✶|✽|·|✢|◯) (Channeling|Flibbertigibbeting|Moonwalking|Noodling|Twisting|Schlepping|
+Pollinating|Honking|Galloping|Pouncing|Scampering|Forming|Bunning|Finagling|Caramelizing|
+Gusting|Cooking|...)…?
```

新動詞観測時の追加プロトコル「観測 → SoT 反映 PR 起票」を Gotchas に再強調、本 batch PR が反映の起点であることを明記。本 batch retro PR では `.claude/skills/orchestrator/SKILL.md` を touch せず、後続 SoT 反映 PR で patch 適用。

採用判定基準 4 (Critical 派生、Monitor 誤判定で per-task pane を IDLE と誤認するリスク) 該当。

### Try #4: Group 1 共通改修候補 #2 (Monitor dedup v3 SoT 化) (priority: high、改修候補 #2 直接対応)

- `.claude/skills/orchestrator/SKILL.md` §Phase 3 §dedup ロジック (新規 subsection) として v3 で実証された以下を SoT 化:
  - (a) `prev_file=/tmp/orchestrator-monitor-prev-<workspace-id>.txt` で persist (v2 で実証)
  - (b) `tail -N` で 直近行に絞る (v3 で実証、N=3-6 程度)
  - (c) `comm -23 <(sort) <(sort)` で line-set diff (v2/v3 で実証)
  - (d) 複数 workspace 観察時は `sed "s|^|[$ws] |"` で prefix 付与 + 同一 prev_file (v3 で実証)
- dedup ロジック改善案を比較する dry-run を `docs/harness/dry-runs/2026-05-18-monitor-dedup.md` に起票 (`.claude/rules/harness-meta-criteria.md` §dry-run 必須条件 §SoT 反転 該当のため必須)
- 採用判定基準 3 (process improvement、orchestrator skill 横断、複数 PR で再発リスク) 該当

### Try #5: Group 1 共通改修候補 #3 (classifier 通過 self-merge 分離) の SoT 反映 (priority: highest、改修候補 #3 直接対応)

- `.claude/skills/orchestrator/SKILL.md` §役割分担 / §Phase 6 (classifier 代行 merge) / §per-task pane 起動 prompt テンプレ の SoT 改修 PR を本 batch retro と同セッションで起票
- per-task pane 責務: Phase 0-6 + Ready 化のみ、`gh pr merge` 系コマンドを prompt body に含めない
- orchestrator 責務: Phase 7 merge を `gh pr merge <PR#> --merge|--squash` で直接代行
- per-task pane 起動 prompt テンプレから `gh pr merge` 文言を削除、「★ Ready 化しました、Phase 7 merge を orchestrator pane に依頼します」print で停止する規約に固定
- 採用判定基準 4 (Critical 派生、classifier denied で per-task pane spawn 不能のリスク) 該当

### Try #6: Group 1 共通改修候補 #4 (close-workspace 代行) の SoT 反映 (priority: medium、改修候補 #4 直接対応)

- `.claude/skills/orchestrator/SKILL.md` §Phase 9 §後処理 に「per-task pane の `/exit` は AI 側不可、orchestrator が `cmux close-workspace --workspace workspace:N` で代行する」を明記
- per-task pane 起動 prompt テンプレ §完走後の停止 procedure に「最終 summary print + idle 待機」「`/exit` は要求しない」と記載 (本 batch PR の各 prompt で既に実装済、SoT 反映が必要)
- workspace cleanup を Phase 9 の最終手順に組込み (Monitor TaskStop → cmux close-workspace → cmux list-workspaces 確認)
- 採用判定基準 1 (Group 1 4 PR + mirror PR で再現確認 + 後続全 per-task pane spawn で再利用見込み) 該当

### Try #7: Group 1 共通改修候補 #5 (--body-file 強制) の SoT 反映 (priority: high、改修候補 #5 直接対応)

- `.claude/rules/implementation-workflow.md` Phase 5 §PR 起票 / §`gh pr create` 実行手順 を以下に SoT 改修:
  - PR body は **`--body-file <path>`** で渡す (推奨)、`--body "$(cat <<EOF...)"` heredoc は禁止 (cmux + zsh + heredoc の三重解釈で truncate / escape 不全リスク、PR #146 で実証 / Group 1 全 PR で `--body-file` 採用で事故ゼロ実証)
  - body 文面は `/tmp/pr-body-<branch-slug>.md` に Write してから `--body-file` で渡す
- `.claude/skills/orchestrator/SKILL.md` §プロンプト送信プロトコル §例外 (直送 OK) の条件にも「heredoc を含む CLI 引数は cmux send 経由で扱わない」を追記
- 採用判定基準 4 (Critical 派生、PR 起票時の Exit 1 で per-task pane が停止し orchestrator 介入が必須となるリスク) 該当

### Try #8: Group 1 共通改修候補 #6 (Group 単位 R-15 一括承認) の SoT 反映 (priority: high、改修候補 #6 直接対応)

- `.claude/skills/orchestrator/SKILL.md` §明示承認文言 canonical に **Group 単位の一括事前承認** パターンを SoT 化:
  - 「(orchestrator pane の操作者 subroh0508) が Group <N> (PR <#A>〜<#B> = N 件) 全体の merge を本指示で明示的に事前承認 (R-15 該当)。merge 方式: <merge|squash>。」
- AskUserQuestion テンプレに「Group / Phase 単位の一括 R-15 承認を取る運用パターン」を選択肢として明示
- `.claude/rules/orchestrator-criteria.md` §自動回答 / pause 判定ライン §R-15 事前承認 (新規 subsection) に「一括承認受領後の 4 連発 merge は orchestrator 代行が正規ルート」を明記
- 一括承認の **有効期間** ルールを規定: 「同セッション内の指定 Group のみ有効、別セッション / 別 Group には継承しない」
- 採用判定基準 3 (process improvement、Group 並列 spawn で頻繁に発生) 該当

### Try #9: Group 1 共通改修候補 #7 (worktree 並列 lock) の SoT 反映 (priority: medium、改修候補 #7 直接対応)

- `.claude/skills/orchestrator/SKILL.md` §Phase 2 §per-task workspace spawn §worktree 作成 (新規 subsection) に以下を SoT 化:
  - **worktree 作成は順次実行必須** (`git worktree add A && git worktree add B && git worktree add C` の形式)、`&` バックグラウンド並列は禁止 (`.git/config` flock 衝突リスク)
  - workspace spawn (`cmux new-workspace`) は並列実行 OK (cmux の workspace 管理は git config と独立)
  - `gh pr view <PR#>` / `cmux read-screen` 等の git config に触れない subcommand は並列実行 OK
- 並列実行 OK / NG の区別を表で明示 (機械検証可能な辞書化)
- `.claude/rules/implementation-workflow.md` Phase 0 §worktree 作成 に「単一 pane の Phase 0 では順次 1 worktree 作成のみのため衝突なし、複数 pane 並列起動は orchestrator skill 側で順次制御」と注記
- 採用判定基準 4 (Critical 派生、worktree 部分失敗で per-task pane spawn が dangling 状態に陥るリスク) 該当

## 📊 指標

PR #148 は A3-2 `bug-fix` Skill 1 ファイル新規追加 (SKILL.md のみ)、`.claude/rules/` / `docs/specifications/` / Kotlin source は一切 touch しないため Skill / rule / spec / Kotlin 差分はゼロ。三層テスト品質基盤 (Kover / Konsist Spec / PITest) は A7 で導入予定のため Before/After 差分は計測不能 (N/A)。

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| Line coverage | N/A | N/A | — | Kover 未導入 (A7 で導入予定) |
| Branch coverage | N/A | N/A | — | 同上 |
| Spec coverage | N/A | N/A | — | Konsist `@Spec` 未導入 (A7 で導入予定) |
| Mutation score | N/A | N/A | — | PITest 未導入 (A7 で導入予定) |
| Lint 違反数 | N/A | N/A | — | Spotless / ktlint / detekt / markdownlint-cli2 未導入 (A6 で導入予定) |
| ファイル数 (差分) | — | 1 changed | +1 | `.claude/skills/bug-fix/SKILL.md` 新規 |
| 行数 (差分) | — | +199 / -0 (merge commit `400e7f2`) | net +199 | 全て Markdown (Kotlin / rule / spec 差分なし) |
| commits 数 (squash 前) | — | 1 (`400954f` 単一) | +1 | `--merge` で merge commit `400e7f2` 経由 (squash ではない) |
| code-reviewer aspect 実行 (subagent 並列) | — | 4/4 pass (spec-conformance / architecture / security / code-quality) | — | test-quality / performance / visual-regression / design-tokens skip (docs / Skill SoT only) |
| Critical findings | — | 0 | — | merge readiness 3 条件充足 |
| High findings | — | 0 | — | fix loop なし |
| Improvement findings (fix loop 即時消化) | — | 0 | — | 本 PR では fix loop 実施なし |
| Improvement findings (持ち越し) | — | 0 | — | Group 1 内で fix loop 不要だった唯一の Skill (A3-3 と同列) |
| fix loop 回数 | — | 0 | — | Critical 0 / High 0 / Improvement 0 で initial commit のまま merge |
| Group 1 内の touch ファイル重複 | — | 0 | — | A3-1 / A3-2 / A3-3 / A3-4 はそれぞれ独立した `.claude/skills/<name>/SKILL.md` のみ touch |
| 観測された未登録思考動詞数 (本セッション累計) | 2 (Pollinating / Honking、PR #146 retro 時点) | 10 (新規 8 追加: Galloping / Pouncing / Scampering / Forming / Bunning / Finagling / Caramelizing / Gusting) | +8 | 改修候補 #1 / Try #3 該当 |
| Monitor dedup ロジック世代 | v1 (md5 hash、再 emit 10+) | v3 (workspace prefix + tail -3 + comm -23、再 emit ほぼゼロ) | 段階改善 | 改修候補 #2 / Try #4 該当 |
| classifier denied 件数 (Group 1 spawn 時) | — | 0 (PR #146 retro Try #3 で per-task prompt から `gh pr merge` 削除済) | — | 改修候補 #3 で実証 |
| heredoc 事故件数 (Group 1 全 PR) | — | 0 (全 PR で `--body-file` 採用) | — | 改修候補 #5 で実証 |
| Group 1 worktree 並列作成成功率 | — | 1/4 (refactor のみ成功、残 3 件 lock 衝突で順次紐付け) | — | 改修候補 #7 で実証 |
| 連続 self-merge 完走 PR 数 (orchestrator 代行 merge ルート含む、本 PR まで) | 9 (PR #135 / #136 / #139 / #140 / #141 / #142 / #144 / #145 / #146) | 10 (+ PR #148) | +1 | Group 1 一括承認パターン (改修候補 #6) で classifier 通過 |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造 -->

- [ ] `[skill]` `.claude/skills/bug-fix/SKILL.md` 依存先 Skill (`plan-author` / `implementation-workflow` / `pr-retrospective`) 側 §関連 に逆方向リンクを追加 (双方向リンク化、別 PR で A3 配下 Spec Gen 4 Skill 一括実施)。Problem #1 / Try #1 該当、採用判定基準 2 (process improvement) 該当
- [ ] `[skill]` `.claude/skills/bug-fix/SKILL.md` §Gotchas に Spec gap 判定基準 3 種を追加 (再現テスト・root cause・仕様改訂要否)。Problem #2 / Try #2 該当、採用判定基準 2 (process improvement) 該当
- [ ] `[skill]` `.claude/skills/orchestrator/SKILL.md` §Monitor 思考動詞辞書 §観測済 に **10 動詞追加** (Pollinating / Honking / Galloping / Pouncing / Scampering / Forming / Bunning / Finagling / Caramelizing / Gusting)、§regex (運用案) の prefix を `(✻|✶|✽|·|✢|◯)` に拡張。Group 1 共通改修候補 #1 / Try #3 該当、採用判定基準 4 (Critical 派生) 該当
- [ ] `[skill]` `.claude/skills/orchestrator/SKILL.md` §Phase 3 §dedup ロジック (新規 subsection) として v3 4 段階 (prev_file persist + tail -N + comm -23 + workspace prefix) を SoT 化、`docs/harness/dry-runs/2026-05-18-monitor-dedup.md` を起票。Group 1 共通改修候補 #2 / Try #4 該当、採用判定基準 3 (process improvement) 該当
- [ ] `[skill]` `.claude/skills/orchestrator/SKILL.md` §役割分担 / §Phase 6 / §per-task pane 起動 prompt テンプレ を改修、per-task pane 責務を Phase 0-6 + Ready 化のみに限定 (`gh pr merge` 文言削除)、Phase 7 merge は orchestrator pane 直接代行に固定。`.claude/rules/orchestrator-criteria.md` にも反映。Group 1 共通改修候補 #3 / Try #5 該当、採用判定基準 4 (Critical 派生) 該当
- [ ] `[skill]` `.claude/skills/orchestrator/SKILL.md` §Phase 9 §後処理 に「per-task pane self-`/exit` 不可、orchestrator が `cmux close-workspace` で代行」明記、per-task pane prompt テンプレに「最終 summary print + idle 待機、`/exit` は要求しない」追記。Group 1 共通改修候補 #4 / Try #6 該当、採用判定基準 1 (Group 1 4 連発で再現) 該当
- [ ] `[rule]` `.claude/rules/implementation-workflow.md` Phase 5 §PR 起票 / §`gh pr create` 実行手順 に「PR body は `--body-file <path>` で渡す、heredoc 禁止」明文化、`/tmp/pr-body-<branch-slug>.md` への Write → `--body-file` パスを default 経路として SoT 化。`.claude/skills/orchestrator/SKILL.md` §プロンプト送信プロトコル §例外にも反映。Group 1 共通改修候補 #5 / Try #7 該当、採用判定基準 4 (Critical 派生) 該当
- [ ] `[skill]` `.claude/skills/orchestrator/SKILL.md` §明示承認文言 canonical に **Group 単位の一括事前承認** パターン (`(orchestrator pane の操作者 subroh0508) が Group <N> (PR <#A>〜<#B> = N 件) 全体の merge を本指示で明示的に事前承認`) を SoT 化、AskUserQuestion テンプレに選択肢追加、`.claude/rules/orchestrator-criteria.md` §R-15 事前承認 (新規 subsection) に「一括承認受領後の N 連発 merge は orchestrator 代行が正規ルート」明記、一括承認有効期間ルール (同セッション / 同 Group 限定) も規定。Group 1 共通改修候補 #6 / Try #8 該当、採用判定基準 3 (process improvement) 該当
- [ ] `[skill]` `.claude/skills/orchestrator/SKILL.md` §Phase 2 §worktree 作成 (新規 subsection) に「worktree 作成は順次実行必須 (`&` バックグラウンド並列禁止、`.git/config` flock 衝突)、workspace spawn / gh pr view / cmux read-screen は並列 OK」を SoT 化。`.claude/rules/implementation-workflow.md` Phase 0 にも注記。並列 OK / NG 辞書化。Group 1 共通改修候補 #7 / Try #9 該当、採用判定基準 4 (Critical 派生) 該当

## 📝 harness-meta フィードバック

<!-- harness-meta が後から追記。提案 → 結果の往復ログを 1 ファイル内で完結 -->

### YYYY-MM-DD <PR ID> で消化 (採用)

| 提案 | <PR ID> での消化内容 |
|---|---|

### YYYY-MM-DD <PR ID> で見送り (後続フェーズへ)

| 提案 | 見送り理由 / 移行先 |
|---|---|

### YYYY-MM-DD <PR ID> で保留 (要再評価)

| 提案 | 保留理由 |
|---|---|

## 関連

- 対象 PR: [#148](https://github.com/subroh0508/colormaster/pull/148) (本 retro 対象、docs(harness): A3-2 bug-fix Skill 完成)
- 関連 PR: [#149](https://github.com/subroh0508/colormaster/pull/149) (A3-1 feature-request、同 Group 1 並走) / [#150](https://github.com/subroh0508/colormaster/pull/150) (A3-4 adr-author、同 Group 1 並走) / [#151](https://github.com/subroh0508/colormaster/pull/151) (A3-3 refactor、同 Group 1 並走) / [#152](https://github.com/subroh0508/colormaster/pull/152) (Step 3a mirror、Group 1 完走の roadmap 反映) / [#146](https://github.com/subroh0508/colormaster/pull/146) (EPIC-A3 起票) / [#147](https://github.com/subroh0508/colormaster/pull/147) (PR #146 retro、Group 1 spawn の前提)
- 関連 ADR: ADR-0017 (ローカル Claude Code ポーリング駆動) / ADR-0018 (implementation-workflow 10 Phase SoT) / ADR-0024 (`gh` CLI 採用 + MCP 優位) / ADR-0027 (docs 構造 + 命名規約 + 日本語化方針)
- 関連 Epic: [`EPIC-A3-skill-suite-extension`](../../epics/EPIC-A3-skill-suite-extension/) (Group 1 完走、本 PR は A3-2)
- 関連 rule: `.claude/rules/{orchestrator-criteria,implementation-workflow,retrospective-format,harness-meta-criteria,docs-structure,template-language,skill-authoring,plan,epic,spec-living-sync,merge-readiness,pr-template,code-reviewer-aspects,roadmap}.md`
- 関連 Skill: `.claude/skills/{bug-fix,feature-request,refactor,adr-author,plan-author,epic-author,implementation-workflow,pr-retrospective,orchestrator,code-reviewer}/SKILL.md`

---
id: learning-pr-135
title: PR #135 レトロスペクティブ (A2-3 / rules プロセス・ハーネス・UI 系本格化)
type: learning
status: draft
related_pr: 135
related_plan: —
related_epic: EPIC-A2-rules-docs-extension
generated_at: 2026-05-17T12:25:00Z
generator: pr-retrospective (skeleton、本ペインで手動代替実行)
---

# PR #135 レトロスペクティブ

> 生成: pr-retrospective Skill (skeleton 段階、A3 本格化前のため本ペインで手動代替) at 2026-05-17T12:25:00Z
> 関連 Plan: なし (1 PR 完結のため EPIC-A2 直下、Plan 不要) / 関連 Epic: EPIC-A2-rules-docs-extension (本 PR で `completed` 昇格)
> 対象 PR: [feat(harness): A2-3 rules プロセス・ハーネス・UI 系本格化 #135](https://github.com/subroh0508/colormaster/pull/135) (merge commit `c593e74`、2026-05-17T12:03:28Z マージ、初回 commit `3a1cc61` + fix loop commit `23ac895`、`gh pr merge --merge`)
> 差分: 20 ファイル / +1675 / -284 (1959 diff 行)、mirror PR #136 (5 ファイル / +28 / -15、merge commit `21279fa`) を含めると 25 changed
> 関連 PR: #136 (mirror、`harness/roadmap-mirror-a2-3`、EPIC-A2 status を `completed` に昇格)

## ✅ Keep (継続したいこと)

- **EPIC-A2 配下最後の PR (A2-3) を 20 ファイル本格化 + mirror PR で完走**: 新規 6 (`pr-template` / `branch-naming` / `merge-readiness` / `pr-draft-policy` / `spec-living-sync` / `harness-meta-criteria`) + skeleton 本格化 11 (`retrospective-format` / `pr-poller` / `skill-authoring` / `harness-evolution` / `implementation-workflow` / `code-reviewer-aspects` / `design-tokens` / `ui-snapshot` / `ui-inventory` / `behavior-preservation` / `docs-structure`) + 微調整 2 (`template-language` / `commit-message`) + 索引 1 (`rules-index`) を 1 PR で完走。A2-2 (35 ファイル / +3,479 行) の約半分の規模に収め、code-reviewer 4 aspect が読み切り可能な粒度を維持。
- **A2-2 patterns との整合**: frontmatter (`id` / `title` / `status: stable` / `last_updated` / `paths` block 形式 / `related_adrs` block 形式 / `related_plan`) + 冒頭 blockquote 5 行 summary + セクション順序 (概要 → 規約本文 → 機械検証 (A6/A7/A10) → Gotchas → 関連) を全 20 ファイルで踏襲。SoT 方向は rule → ADR の片方向 (PR #119 レトロ Problem #1 解消継続)。
- **PR #121 (A2-1) レトロ Try 4 件を全件反映**: (1) `implementation-workflow.md` Phase 0 で `git fetch origin master` 明文化 (本文 + Gotchas 二重)、(2) `code-reviewer-aspects.md` binary checklist 各 aspect 5-7 項目確定 (8 aspect で計 51 項目)、(3) `docs-structure.md:115-117` NG 例コメント明示 + blockquote 注で PR #121 出典明記、(4) `commit-message.md` §subject 言語ポリシー セクション化 (Gotchas は参照のみに整理)。
- **code-reviewer 4 aspect 並列 (general-purpose subagent) で Critical 0、Improvement 18 件を検出**: spec-conformance 3 / architecture 4 / security 3 / code-quality 8。各 aspect 独立した system prompt で動作 (R-13)、Coordinator が PR コメント post + Merge readiness 判定。本 PR 自体が `code-reviewer-aspects.md` を本格化する PR でありながら、その規範に従って実行される自己整合性。
- **fix loop で重要 Improvement 4 件を即時消化 (commit `23ac895`)**: (1) `docs-structure.md:26` bare fence ` ``` ` → ` ```text ` (MD040 違反、stable 昇格と矛盾していたため即時)、(2) `branch-naming.md:77` `## 自動検証` → `## 機械検証` (18 rule との表記揺れ解消)、(3) `CLAUDE.md` lookup table に新規 6 rule の path 7 行追加、(4) `merge-readiness.md` mirror PR aspect セット記述を `code-reviewer-aspects.md` SoT として参照に統一。残 14 件は本 learning の `🤖 ハーネス改善提案` で harness-meta フィードバック予定。
- **EPIC-A2 completed 昇格を一気通貫で完了**: A2-3 merge → mirror PR #136 (`harness/roadmap-mirror-a2-3`) 起票 → ready → merge → Phase 9 worktree cleanup の流れを途切れさせずに完走。A2-1 (PR #121 → mirror #122) / A2-2 (PR #125 → mirror #130) / A2-4 (PR #123 → mirror #127) / A2-5 (PR #126 → mirror #128) / A2-6 (PR #129 → mirror #131) と同じ運用パターンで 5 PR 連続成功。EPIC-A2 README / roadmap / progress + 全体 roadmap + `docs/epics/INDEX.md` の 5 ファイルを mirror PR で一括更新、A2-1〜A2-6 全 6 PR merge 完了を機械可読化。
- **A2-6 (PR #129) 由来の permission 拡張で classifier ブロックゼロ**: `gh pr ready 135` / `gh pr merge 135 --merge` / `git push` / `gh pr ready 136` / `gh pr merge 136 --merge` 全てを本ペインから直接実行できた (A2-2 / A2-4 / A2-5 のような out-of-band orchestrator 委任は不要)。permission 拡張の効果 (「都度承認の手間削減」目的) が EPIC-A2 配下 1 PR 完走の test ケースで実証。
- **3 worktree 並走を物理分離**: 本 PR 用 (`feature/A2-3-rules-process`) + mirror PR 用 (`harness/roadmap-mirror-a2-3`) + retrospective 用 (`harness/learnings-pr-135`、本ファイル) の 3 worktree を並走、touch ファイル衝突ゼロで完走。`implementation-workflow.md` Phase 0 で全 worktree 起動時に `git fetch origin master` を実行し、Phase 9 で merge 確認後に worktree remove + branch 削除する規約が機能。
- **subject 言語ポリシーをセクション化し Gotchas を参照のみに整理 (PR #121 レトロ Try)**: A2-1 で新規追加した `commit-message.md` が「本文 vs Gotchas で『英語』記述の二重化」を抱えていたが、A2-3 で §subject 言語ポリシー セクション化 (本文に SoT、Gotchas は参照) → drift 解消。同様の構造を `harness-meta-criteria.md` `[mcp]` プレフィックス受信ルール明示 (architecture #4) などにも適用候補。

## ⚠️ Problem (詰まったこと / 制約)

- **ユーザー指示の「7 NEW + 11 skeleton 本格化 = 18 ファイル」と私の認識「6 NEW + 11 skeleton + 2 微調整 + 索引 = 20 ファイル」の数の差異を確認せず私の判断で進めた** (consensus: process): expected_modules には 18 ファイル + rules-index = 19 ファイル列挙、commit-message.md は expected_modules に未列挙だが PR #121 レトロ Try で A2-3 対応事項として明示。EPIC-A2 README には `commit-message` 列挙 (合計 20 ファイル) で expected_modules と差異あり。「7 NEW」がユーザー側の数え間違いか / 私の分類 (commit-message を新規 vs 微調整) の差異か確認せず進めた。結果として code-reviewer / 完了根拠記録には影響なかったが、確認したほうが安全だった (ユーザーの「Phase 2-9 まで完走」「停止しない」指示と緊張関係)。
- **`git worktree add ../colormaster-worktrees/...` の相対パス解決ミスで nested path 発生**: 初回 mirror PR worktree 作成時に `..` が cwd (`feature-A2-3-rules-process`) 起点で解決され `/Users/.../colormaster-worktrees/colormaster-worktrees/harness-roadmap-mirror-a2-3` という入れ子パスが生成。1 回 `git worktree remove` + `git branch -D` してから絶対パスで再作成。`implementation-workflow.md` Phase 0 の手順例 (`../<repo-name>-worktrees/<branch-slug>`) は cwd がメイン worktree の場合の正解だが、別 worktree 内から実行する場合は `../../colormaster-worktrees/...` または絶対パスが必要。Gotchas 追記候補。
- **system-reminder で読んだファイルを Edit する前に Read 必須**: `template-language.md` / `rules-index.md` / `docs-structure.md` の 3 ファイルは system-reminder (`<system-reminder>Contents of ...</system-reminder>`) で内容を提示されていたが、Edit ツールは「Read tool を最低 1 回経由」が必須仕様。3 回 `tool_use_error: File has not been read yet` で fallback。Edit / Read の依存関係は `<system-reminder>` 経由の事前内容提示を Read 扱いにしない仕様で、本ペインの推論ロジックと衝突。
- **TaskCreate リマインダーが 5 回発火したが意図的に skip**: Markdown のみ編集する PR では task 化のメリットが薄く (各ファイル独立、依存関係なし、Write 並列で完了)、進捗追跡は会話文脈 + commit / push / merge コマンドの結果で十分。発火タイミングが Edit 連発の合間で、リマインダーノイズが累積。「TaskCreate を使うべきタイミング」のガイドライン明確化候補。
- **code-reviewer Improvement 18 件中、即時消化 4 件と learning 持ち越し 14 件の判断境界が曖昧**: 「stable 昇格と矛盾する rule の SoT 性違反 (MD040 / 表記揺れ / lookup table 漏れ / aspect セット二重化)」を即時、「補助的な改善 (frontmatter コメント追加 / 例示追加 / `≥` 文字置換 / placeholder セクション配置)」を持ち越しとしたが、`harness-meta-criteria.md` の採用判定基準 1-5 を厳密に当てはめると即時消化基準が拡張する余地あり (例: spec-conformance #2 / #3 も `paths` field の整合性として即時の方が良かったかも)。
- **mirror PR の worktree で `branch -D` 強制使用**: Phase 9 規約は `branch -d` (未マージ時拒否) 推奨だが、merge commit 経由の merge (`--merge` 採用) は `git branch --merged origin/master` で worktree 内ブランチを検出できないケースがあり、`gh pr view --json state` で MERGED 確認後に `-D` で強制削除。`implementation-workflow.md` Phase 9 Gotchas に「`--merge` merge 後の `branch -d` は `--merged origin/master` で検出されない場合があり、PR MERGED 確認後の `-D` は安全」という注記候補。
- **`git fetch origin master` 後の rebase で stash + pop が必要なケース**: Phase 0 で `git fetch origin master` を実行したら本 worktree が 8 commits behind origin/master。`git rebase origin/master` を実行したら `error: cannot rebase: You have unstaged changes` → `git stash push -u` → `git rebase` → `git stash pop` の 3 ステップ fallback。`implementation-workflow.md` Phase 0 に「unstaged changes ありなら stash 後 rebase」の手順追記候補。
- **Improvement #1 (CLAUDE.md lookup table 漏れ) は A1 レトロ Problem #1 と同質**: 新規 6 rule の path が CLAUDE.md lookup table に追記されていなかった。A1 レトロで `rules-index.md` の「既存」誤宣言 13+ 件 / status 表記 drift を機械検証 (A6 Gradle) で防止する方針を立てたが、CLAUDE.md lookup table と rule の `paths` field の整合は別系統で、現状は人間 / AI レビュー任せ。A6 機械検証スコープに「CLAUDE.md lookup table 行と rule.frontmatter.paths の双方向参照検証」を追加候補。
- **EPIC-A2 配下に計画外で挿入された A2-6 (PR #129) のレトロが本 INDEX.md に未起票**: 索引には #117 / #119 / #121 / #123 / #125 / #126 が並ぶが #129 が無い。`pr-poller` Skill 本格化 (A3 / A4) で merged PR 検出 → retrospective 自動起票が稼働するまでは手動運用、本ペインの責務範囲は #135 のみのため #129 は別 worktree (`harness-learnings-pr-129` 既存) で並行起票中の想定。

## 🚀 Try (次回からの改善案)

- **ユーザー指示の数の差異を即時確認するワークフロー**: 「7 NEW」「6 NEW」のような分類ズレが疑われたら判断保留せず即時質問。「停止しない」指示と緊張関係になるが、規模認識のズレは後続 PR / レトロでの記録に影響するため確認優先。本 PR では結果オーライだったが、A3 以降の Skill 本格化 PR では「~ 個の Skill を本格化」「~ 個の rule を追加」のような数値表現が頻出する見込み、確認ガイドラインが必要。
- **`implementation-workflow.md` Phase 0 Gotchas 追記候補**: (1) `git worktree add` のパスは絶対表記推奨 (cwd 依存で nested path が発生しうる)、(2) unstaged changes ありなら stash 後 rebase の手順、(3) merge commit 経由の `branch -d` は `--merged origin/master` で検出されないケースがあるため `gh pr view --json state` で MERGED 確認後の `-D` を許容、の 3 件。
- **TaskCreate / ToolSearch 発火ルール明確化**: Markdown のみ編集する PR (本 PR / A2-1 / A2-2 / A2-4 / A2-5 の実績) では task 化のメリットが薄く、5-10 ファイル超でも skip 妥当。一方、ファイル間依存関係がある実装 PR (A3 以降の Skill 本格化、A6 の Lint 統合等) では task 化推奨。`.claude/rules/implementation-workflow.md` Phase 3 (実装 + Lint + Test) または別 rule で発火基準を明文化候補。
- **code-reviewer Improvement の即時消化 vs 持ち越し判断基準を `harness-meta-criteria.md` に追加**: 現状の採用判定基準 1-5 (「複数 PR 反復」「ADR/Plan/Epic 予約」「R-XX 対応」「Critical 派生」「orchestrator 明示」) は「採用 = 改修 PR 起票」基準であり、「即時消化 vs 持ち越し」の境界とは別軸。「即時消化基準: rule の stable 昇格と矛盾する SoT 性違反 / lookup table 漏れ / 規約間の二重化」「持ち越し基準: 補助的な改善 / 表現の最適化 / placeholder 配置」のような判断軸を明文化候補。
- **`pr-retrospective` Skill 本格化を A3 から前倒し検討**: 本ファイル (PR #135 のレトロ) を含め、A1 (PR #119) / A2-1 (#121) / A2-2 (#125) / A2-3 (#135) / A2-4 (#123) / A2-5 (#126) の 6 件全てが手動代替実行。`harness/learnings-batch-2026-W20` ブランチへの自動集約も未稼働。A3 で `feature-request` / `bug-fix` / `refactor` Skill と並列で `pr-retrospective` を本格化、本ペインの手動運用 effort を縮減。
- **CLAUDE.md lookup table と rule.frontmatter.paths の双方向参照を A6 機械検証スコープに追加**: A1 レトロ Problem #1 (rules-index status drift) と同質の課題で、A2-3 で本 PR fix loop により lookup table 7 行追加で解消したが、機械検証なしだと再発リスク。Gradle カスタムタスクで「CLAUDE.md lookup table 行 → rule path、rule.frontmatter.paths → CLAUDE.md lookup table 行」を双方向に検証する candidate。
- **A2-3 で取り込まなかった `[mcp]` プレフィックス の harness-meta 受信ルール (architecture #4) を A4 で消化**: `harness-evolution.md` 固有プレフィックスが `harness-meta-criteria.md` 採用判定基準で未定義、`retrospective-format.md` のプレフィックス 4 種にも含まれない。A4 で `harness-meta` Skill 本格化時に「`[mcp]` 受信 → ADR 起票判定」のルールを `harness-meta-criteria.md` に追加候補。

## 📊 指標

A2-3 PR は実装コード差分ゼロ (`.claude/rules/` 配下 Markdown のみ + CLAUDE.md 微修正)、三層テスト品質基盤 (Kover / Konsist Spec / PITest) は A7 で導入予定のため Before/After 差分は計測不能 (N/A)。

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| Line coverage | N/A | N/A | — | Kover 未導入 (A7 で導入予定) |
| Branch coverage | N/A | N/A | — | 同上 |
| Spec coverage | N/A | N/A | — | Konsist `@Spec` 未導入 (A7 で導入予定) |
| Mutation score | N/A | N/A | — | PITest 未導入 (A7 で導入予定) |
| Lint 違反数 | N/A | N/A | — | Spotless / ktlint / detekt / markdownlint-cli2 未導入 (A6 で導入予定) |
| ファイル数 (差分、本 PR) | (A2-3 前) | 20 changed | 20 changed | rules 19 + CLAUDE.md 1 (fix loop で追加) |
| ファイル数 (差分、mirror #136) | (A2-3 merge 後) | 5 changed | 5 changed | EPIC-A2 README / roadmap / progress + 全体 roadmap + `docs/epics/INDEX.md` |
| 行数 (差分、本 PR) | (A2-3 前) | +1675 / -284 (merge commit `c593e74`) | net +1391 | 全て docs (`.claude/rules/` + CLAUDE.md) |
| 行数 (差分、mirror #136) | (A2-3 merge 後) | +28 / -15 (merge commit `21279fa`) | net +13 | EPIC-A2 + 全体 roadmap 反映 |
| commits 数 (squash 前、本 PR) | (A2-3 前) | 2 (`3a1cc61` 初回 + `23ac895` fix loop) | +2 | `--merge` で merge commit 保持 |
| Rule 数 (`.claude/rules/`) | 47 | 53 | +6 | 新規 6 件追加 |
| 本格化済 rule 数 (`stable (X)`) | 35 | 53 | +18 | A2-3 で 18 件を `stable (A2-3)` に正規化 |
| EPIC-A2 配下 PR 数 (merged) | 5 (A2-1 / A2-2 / A2-4 / A2-5 / A2-6) | 6 (+ A2-3) | +1 | 計画外 A2-6 含め全 6 PR merge 済 |
| EPIC-A2 status | in-progress | completed | — | 本 mirror PR #136 で昇格 |
| code-reviewer aspect 実行 (subagent 並列) | — | 4/4 pass (spec-conformance / architecture / security / code-quality) | — | test-quality / performance は実装コード変更ゼロで skip 妥当、visual-regression / design-tokens は A10 enable 待ち |
| Critical findings | — | 0 | — | merge readiness 3 条件 (CI green + Critical 0 + 人間 approve) 充足、orchestrator 明示承認で R-15 代替 |
| Improvement 件数 (binary checklist 外、Coordinator 集約) | — | 18 (spec-conformance 3 / architecture 4 / security 3 / code-quality 8) | — | 即時消化 4 件 (commit `23ac895`)、持ち越し 14 件 (本 learning `🤖 ハーネス改善提案`) |
| binary checklist 通過率 (4 aspect 集約) | — | 36 / 36 ✅ (spec-conformance 7 + architecture 9 + security 10 + code-quality 12) | — | 4 aspect 合計、Improvement は checklist 外 |
| PR #121 (A2-1) レトロ Try A2-3 該当分の消化件数 | 0 / 4 | 4 / 4 (100%) | +4 | (1) Phase 0 fetch / (2) binary checklist 5-7 項目 / (3) NG 例コメント / (4) commit-message subject セクション化、全件反映 |
| classifier ブロック数 (本 PR 関連の `gh` / `git` コマンド) | (A2-2 以前は A2-6 permission 拡張前) | 0 | — | A2-6 (PR #129) で `.claude/settings.json` に `Bash(gh pr ready:*)` / `Bash(gh pr merge:*)` / `Bash(git push:*)` 等の 5 件追加済の効果検証 (本 PR が EPIC-A2 内の test ケース) |
| worktree 並走数 (本 PR + mirror + retrospective) | 1 | 3 | +2 | feature/A2-3-rules-process + harness/roadmap-mirror-a2-3 + harness/learnings-pr-135 (本ファイル)、Phase 9 cleanup で 2 つ削除済 |
| Phase 0-9 完走時間 (本 PR + mirror、概算) | — | 約 2 時間 | — | 計画策定 → 実装 → fix loop → code-reviewer 4 aspect 並列 → merge → mirror PR → cleanup |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造 -->

- [ ] `[rule]` `rules-index.md` の `stable (X、〜)` 括弧内コメントの parser 仕様明文化 (spec-conformance #1): A6 機械検証で status 列を parse する際に括弧内コメントの扱いが曖昧、`rules-index.md` の status 語彙セクション末尾に「`stable (X、〜)` の括弧内コメントは人間向け付記であり、機械検証では parens 直前のキーワードのみを正規化対象とする」と一文追加候補
- [ ] `[rule]` `spec-living-sync.md` frontmatter に scope 限定コメント追加 (spec-conformance #2): `docs/requirements/**` / `docs/specifications/**` のみカバーで `docs/architecture/**` / `docs/api/**` は対象外であることを frontmatter コメントで明示
- [ ] `[rule]` `pr-template.md` paths に `scripts/install-git-hooks.sh` 追加検討 (spec-conformance #3): commit-msg / pre-push hook 連動を auto-load 網羅性向上のため
- [ ] `[rule]` `branch-naming.md` vs `pr-template.md` の `dependency-upgrade` ブランチ prefix 整合 (architecture #2): `chore/<slug>` フォールバックの扱いを片方の rule で SoT 化
- [ ] `[rule]` `harness-meta-criteria.md` に `[mcp]` プレフィックス受信ルール明示 (architecture #4): `harness-evolution.md` 固有の `[mcp]` が harness-meta 側で受信されたときの parse ルール (採用 / 見送り / ADR 起票判定) を追加
- [ ] `[rule]` `pr-template.md` Gotchas に PII / secrets 注意書き 1 行追加 (security #1): HTML コメント frontmatter テンプレで `expected_modules` 等に `data/users.db` のような追跡禁止パスを誤投入しないための予防、`pii.md` / `secrets.md` への明示リンク
- [ ] `[rule]` `branch-naming.md` `pre-push` hook 正規表現の `master` / `main` 直 push reject 明示 (security #2): A6 実装時に `^(feature|fix|...)/.+$` の曖昧さを解消、別正規表現で reject 規約を併記
- [ ] `[rule]` `harness-meta-criteria.md` 撤去フローの 2 段階運用必須化検討 (security #3): status `removed` → 次 PR で物理削除を「推奨」から「必須」に格上げ、不可逆操作のリスク低減
- [ ] `[rule]` `skill-authoring.md` SKILL.md 雛形に各セクション 1 行コメント追加 (code-quality #2): `harness-bootstrap` / `harness-meta` が生成する SKILL.md のばらつき抑制
- [ ] `[rule]` `rules-index.md` に A2-3 正規化内訳表 (新規 6 / 本格化 11 / 微調整 2 / 索引 1 = 計 20 行) 追加 (code-quality #4): harness-meta が parse しやすい形式に
- [ ] `[rule]` `code-reviewer-aspects.md:34` `≥` → `5 項目以上` 置換 (code-quality #5): Unicode U+2265 の copy 時 escape リスク回避
- [ ] `[rule]` `spec-living-sync.md` PR description 例にコメント追加 (code-quality #6): fence 内の例示意図を明確化
- [ ] `[rule]` `ui-snapshot.md` Gotchas にパス多重参照警告追加 (code-quality #7): `docs/design/inventory/screenshots/` パスが 3 rule で多重参照、A10 で変更時の同時更新規約
- [ ] `[rule]` `harness-meta-criteria.md:84` 「実行時パラメータ」セクション placeholder 配置 (code-quality #8): 本文の `(将来追加)` 参照と整合させるため、空の見出し配置
- [ ] `[rule]` `implementation-workflow.md` Phase 0 Gotchas 3 件追加: (1) `git worktree add` 絶対パス推奨、(2) unstaged changes ありなら stash 後 rebase、(3) merge commit 経由の `branch -d` は `--merged origin/master` で検出されないケースありの注記 (本レトロ Try の Phase 0 / 9 改善)
- [ ] `[rule]` `harness-meta-criteria.md` に「即時消化 vs 持ち越し」判断基準セクション追加 (本レトロ Try): 採用判定基準 1-5 (採用 = 改修 PR 起票) と別軸で「即時消化基準」「持ち越し基準」を明文化
- [ ] `[skill]` `pr-retrospective` Skill 本格化を A3 から前倒し検討 (本レトロ Try): A1 / A2-1 / A2-2 / A2-3 / A2-4 / A2-5 の 6 件全てが手動代替実行、`harness/learnings-batch-YYYY-WW` 集約も未稼働、R-12 ロスト対策が回復しない
- [ ] `[skill]` A6 機械検証スコープに「CLAUDE.md lookup table 行 ↔ rule.frontmatter.paths 双方向参照」検証を追加 (本レトロ Try): A1 レトロ Problem #1 と同質の課題、A6 Gradle カスタムタスクで防止
- [ ] `[template]` `harness.md` PR テンプレに「対象 PR / KPT 要点 / 採用判定基準該当」テーブルの記入例追加: 本 PR / mirror PR / retrospective 起票時に記入判断材料を都度問い合わせた経験
- [ ] `[skill]` TaskCreate / ToolSearch 発火ルールを `implementation-workflow.md` (または別 rule) で明文化: Markdown のみ編集 PR では skip 妥当、ファイル間依存ありの実装 PR では task 化推奨の境界
- [ ] `[skill]` `pr-poller` Skill 本格化時に「merged PR 検出 → retrospective 自動起票」MVP を A4 で実装 (PR #121 / #125 / #126 / #135 etc. の手動運用解消): 本 PR が EPIC-A2 配下最後の手動運用、A3 / A4 完了で自動化

## 📝 harness-meta フィードバック

<!-- harness-meta が後から追記。提案 → 結果の往復ログを 1 ファイル内で完結 -->

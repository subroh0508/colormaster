---
id: learning-pr-168
title: PR #168 レトロスペクティブ (A3-11 pr-poller Skill 本格化)
type: learning
status: draft
related_pr: 168
related_plan: —
related_epic: EPIC-A3-skill-suite-extension
generated_at: 2026-05-19T18:40:00+09:00
generator: pr-retrospective (skeleton、本ペインで手動代替実行)
---

# PR #168 レトロスペクティブ (A3-11 pr-poller Skill 本格化)

> **5 行以内 summary**: EPIC-A3 A3-11 で `.claude/skills/pr-poller/SKILL.md` を skeleton
> (47 行) → active 本格版 (191 行) に本格化。ローカル Claude Code 内ポーリングで
> merged/closed PR 取得 → pr-retrospective / dependency-upgrade dispatch、`.claude/locks/`
> mkdir 方式排他制御、3 系統起動経路 (起動時 / CronCreate / ScheduleWakeup) を明文化。
> harness-meta 自動起動閾値判定 + roadmap-tracker pending-fetch 再走査を同 Phase 統合。

> 生成: pr-retrospective (skeleton、本ペインで手動代替実行) at 2026-05-19T18:40:00+09:00
> 関連 Plan: — / 関連 Epic: EPIC-A3-skill-suite-extension (A3-11 Group 3 wave2)
> 対象 PR: [feat(harness): A3-11 pr-poller Skill 本格化 (Renovate 検出 + 3 系統起動経路) #168](https://github.com/subroh0508/colormaster/pull/168) (squash merged at 2026-05-18T05:51:47Z、commit `1cefea3`、from head `epic/EPIC-A3-pr-poller-skill`)
> 差分: 3 ファイル / +193 / -24 (217 diff 行)、commits: 1 (fix loop ゼロ完走)

## ✅ Keep (継続したいこと)

- **3 系統起動経路 (起動時 / CronCreate / ScheduleWakeup) を明文化**: ローカル Claude Code 起動直後 / 日次 09:00 JST 定期起動 / 前回処理 N 時間後の自動再起動 の 3 経路、起動経路引数 (`route=manual|cron|wakeup`) で動作差分、`.claude/rules/pr-poller.md` SoT と整合 (consensus: spec-conformance + architecture)
- **`.claude/locks/pr-poller.lock` mkdir 方式排他制御**: POSIX atomic、flock 非対応環境でも動作、BSD `flock` 非対応、Linux/macOS 両対応、`mkdir` の EEXIST が排他保証、stale 判定 30 分閾値で自動回収、cleanup hook 不要 (consensus: architecture)
- **`.claude/locks/README.md` 配置 + `.gitignore` README whitelist 追加**: lock ディレクトリ運用ルールを SoT 化、`*.lock` / `*.last-run` / `*.processed-cache` を ignore、README.md を whitelist で commit 対象に維持 (consensus: spec-conformance)
- **5 Phase 構造化 (Lock 取得 → 対象 PR 取得 → 未処理判定 + dedup → 後続 Skill dispatch → Lock 解放 + last-run 更新)**: 各 Phase の責務を明確に分離、dispatch は順次 / 直列 (Phase 4 内で並列起動しない、touch ファイル衝突 / lock 衝突予防)
- **Renovate ラベル PR 検出**: `gh pr list --state open --label renovate` + `--label dependencies` + `--label renovate-bot` の 3 ラベル UNION で検出、`dependency-upgrade` Skill 側で誤検出を判断する責務境界明確化 (consensus: architecture)
- **harness-meta 自動起動閾値判定 (未処理 learning 10 件 / 7 日 / 最小 24h)**: `harness-meta-criteria.md` §pr-poller 起動閾値 と連動、超過時に自動起動を試みる責務分離 (consensus: architecture)
- **roadmap-tracker pending-fetch 再走査 (R-35)**: `docs/harness/roadmap.md` + `docs/epics/*/roadmap.md` の `<!-- evidence:pending-fetch -->` 有項目を再 `gh pr view`、成功時は完了根拠を埋める / コメント削除 (consensus: spec-conformance)
- **キャッチアップ動作 (R-11)**: `$LAST_RUN` が 3 日以上前なら FIFO で古い順から 10 件ずつ batch 処理、新規 PR が発生しても優先順位を変えず、R-12 (retro 取りこぼし防止) と整合
- **gh CLI 失敗時の連鎖停止 (R-11)**: 連続 5 件失敗で本 Skill を緊急停止 + orchestrator 通知、retry 無限ループ予防
- **fix loop ゼロ完走**: code-reviewer 4 aspect (spec / arch / security / code-quality) Critical 0、initial commit 1 件で完走

## ⚠️ Problem (詰まったこと / 制約)

- **3 系統起動経路の優先度判定が暗黙**: 起動時 / CronCreate / ScheduleWakeup が同時発火した場合の処理順序、`.claude/locks/pr-poller.lock` 30 分 stale 判定で重複は防げるが優先度自体の SoT が SKILL.md に未明示
- **`pr-retrospective` (A3-10、PR #167) との連携が片方向**: 本 SKILL.md → `pr-retrospective` への dispatch は明示、逆参照 (pr-retrospective が完了時に pr-poller に signal) は暗黙
- **`harness-meta` / `dependency-upgrade` / `roadmap-tracker` 4 Skill 同時 dispatch の責務境界**: 本 SKILL.md は dispatch のみ、各 Skill の lock 衝突 / 並列 dispatch の trade-off が SoT 化されていない
- **キャッチアップ動作 (R-11) の閾値 3 日 / batch 10 件 が経験値**: A4 自動化前の運用想定値、A4 後に N=10 で再計測予定
- **Renovate ラベル UNION 検出の誤検出リスク**: `renovate` / `dependencies` / `renovate-bot` の 3 ラベル UNION で検出、人間手動付与のケースは `dependency-upgrade` 側で再確認、責務境界は明確だが誤検出時の log / recovery 経路が SKILL.md に未明示
- **PII / Secrets redaction が後続 Skill 責務**: 本 SKILL.md は PR# + メタ情報のみ扱い、`gh pr view --json` の `body` フィールドは直接扱わず後続 Skill に渡す。責務分離は明確だが、handoff 段階でログに PR title が混入するリスクは Gotchas に明示
- **`.gitignore` README whitelist の二重定義**: `.gitignore` で `*.lock` ignore + `!.claude/locks/README.md` whitelist の構造、後続 PR で `.gitignore` を編集する場合の drift リスク
- **+193 / -24 を 1 commit で実装、logical separator なし**: A3-8 / A3-9 / A3-10 / A3-12 / A3-13 と同問題、Phase 別 / 機能別の commit 分離が読み取れない、PR #161 / #162 / #164 / #165 / #167 retro Try と同問題で 6 回目重複

## 🚀 Try (次回からの改善案)

- **3 系統起動経路の優先度判定を SKILL.md に明示**: 起動時 = 即時 / CronCreate = 09:00 JST / ScheduleWakeup = 前回 +N 時間 の 3 経路を「最も新しい」優先で処理、同時発火時の lock 取得順序を明文化、`.claude/rules/pr-poller.md` と双方向 link
- **`pr-retrospective` / `dependency-upgrade` / `harness-meta` / `roadmap-tracker` 4 Skill 連携の cross-link を双方向化**: 本 SKILL.md からの dispatch + 各 Skill 完了時の signal 経路を SoT 化、A4 で機械検証可能化
- **キャッチアップ閾値 3 日 / batch 10 件 の N=10 再計測**: A4 自動化後に retro batch N=10 で median 計測、経験値由来閾値を計測由来閾値に昇格
- **Renovate ラベル誤検出時の log / recovery 経路を SKILL.md に明示**: `dependency-upgrade` 側で誤検出判定後の `pr-poller` への通知経路、`harness-meta` への学習データ蓄積経路を SoT 化
- **PII / Secrets redaction を handoff サマリにも適用**: PR# + メタ情報のみと明示しているが、log / processed-cache に PR title が混入しないよう regex 適用を Phase 5 に追加
- **`.gitignore` README whitelist の機械検証**: A6 で Gradle カスタムタスクが「`.claude/locks/` 配下の README.md が whitelist で commit 対象」を検証、drift 予防
- **single-file PR の commit logical separator 規範**: PR #161 / #162 / #164 / #165 / #167 retro Try と同提案、本 PR で 6 回目重複、採用判定基準 1 (6+ PR 反復) 強化
- **A4 完了時に本 SKILL.md の 3 系統起動経路を実機検証**: cmux + ScheduleWakeup + CronCreate の連携を N=10 で実機 test、SoT 配置と実装の gap を retro で言語化

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| Line coverage | N/A | N/A | N/A | Kover (A7 完了後) |
| Branch coverage | N/A | N/A | N/A | 同上 |
| Spec coverage | N/A | N/A | N/A | Konsist `@Spec` (A7 完了後) |
| Mutation score | N/A | N/A | N/A | PITest (A7 完了後) |
| Lint 違反数 | N/A | N/A | N/A | Spotless / ktlint / detekt / markdownlint-cli2 (A6 完了後) |
| **PR 固有指標** | | | | |
| 変更ファイル数 | 0 | 3 | +3 | SKILL.md + `.claude/locks/README.md` 新規 + `.gitignore` 拡張 |
| 追加行数 | 0 | 193 | +193 | skeleton (47 行) → active 本格版 (191 行) + lock README + .gitignore whitelist |
| 削除行数 | 0 | 24 | +24 | 旧 skeleton 記述 + 旧 description |
| commits | 0 | 1 | +1 | fix loop ゼロ完走 |
| frontmatter state 遷移 | skeleton / B0 | active / A3 | N/A | `status` / `phase` 同時更新 |
| 起動経路数 | 0 | 3 | +3 | 起動時 / CronCreate / ScheduleWakeup |
| 連携 Skill 数 | 0 | 4 | +4 | pr-retrospective / dependency-upgrade / harness-meta / roadmap-tracker |
| code-reviewer Critical | N/A | 0 | 0 | 4 aspect 全 PASS |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造 -->

- [ ] `[skill]` 3 系統起動経路の優先度判定を `.claude/skills/pr-poller/SKILL.md` に明示 (最も新しい優先 + lock 取得順序)、`.claude/rules/pr-poller.md` と双方向 link、採用判定基準 1 (本 PR + 後続 PR で反復見込み) 該当
- [ ] `[skill]` 4 Skill 連携 (`pr-retrospective` / `dependency-upgrade` / `harness-meta` / `roadmap-tracker`) の cross-link 双方向化を A4 で機械検証可能化、採用判定基準 2 (A4 で予定) 該当
- [ ] `[rule]` キャッチアップ閾値 3 日 / batch 10 件 の N=10 再計測を A4 完了時に実施、`pr-poller.md` 経験値由来閾値を計測由来閾値に昇格、採用判定基準 2 該当
- [ ] `[skill]` Renovate ラベル誤検出時の log / recovery 経路を SKILL.md に明示、`harness-meta` への学習データ蓄積経路を SoT 化、採用判定基準 1 該当
- [ ] `[rule]` PII / Secrets redaction を handoff サマリ + processed-cache に regex 適用、Phase 5 に追加、採用判定基準 4 (Critical 派生 = security) 該当
- [ ] `[rule]` `.gitignore` README whitelist の機械検証を A6 で Gradle カスタムタスク化、採用判定基準 4 (Critical 派生 = drift 予防) 該当
- [ ] `[rule]` single-file PR の commit logical separator 規範を `.claude/rules/implementation-workflow.md` §commit 分離 に追加。PR #161 / #162 / #164 / #165 / #167 retro Try と同提案、本 PR で 6 回目重複、採用判定基準 1 強化該当
- [ ] `[skill]` A4 完了時に 3 系統起動経路を N=10 で実機検証、SoT 配置と実装 gap を retro で言語化、採用判定基準 2 該当

## 📝 harness-meta フィードバック

<!-- harness-meta が後から追記。提案 → 結果の往復ログを 1 ファイル内で完結 -->

### YYYY-MM-DD <PR ID> で消化 (採用)

| 提案 | <PR ID> での消化内容 |
|---|---|

### YYYY-MM-DD <PR ID> で見送り (後続フェーズへ)

| 提案 | 見送り理由 / 移行先 |
|---|---|

### YYYY-MM-DD <PR ID> で保留 (要再評価)

| 提案 | 保留理由 |
|---|---|

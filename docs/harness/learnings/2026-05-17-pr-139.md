---
id: learning-pr-139
title: PR #139 レトロスペクティブ (8 retro 改善提案を集約消化 / rule + template + 横断 learnings)
type: learning
status: draft
related_pr: 139
related_plan: —
related_epic: EPIC-A2-rules-docs-extension
generated_at: 2026-05-17T14:00:00Z
generator: pr-retrospective (skeleton、本ペインで手動代替実行)
---

# PR #139 レトロスペクティブ

> 生成: pr-retrospective Skill (skeleton 段階、A3 本格化前のため本ペインで手動代替) at 2026-05-17T14:00:00Z
> 関連 Plan: なし (1 PR 完結のため EPIC-A2 完了後の harness-meta 相当タスク、Plan 不要) / 関連 Epic: EPIC-A2-rules-docs-extension (`completed` 昇格後の集約消化 PR)
> 対象 PR: [feat(harness): 8 retro 改善提案を集約消化 (rule + template + 横断 learnings) #139](https://github.com/subroh0508/colormaster/pull/139) (squash merge commit `f0f6833`、2026-05-17T13:22:50Z マージ、head commit `c9a0972`、`gh pr merge --squash`)
> 差分: 34 ファイル / +878 / -35 (913 diff 行)、commits: 1 (squash 前は 3 / `2c288be` 初回 + `4953dc2` sed 正規化 + `c9a0972` fix loop)
> 関連 PR: #140 (本 PR から分割した ADR 補強分、PR-2、merge commit `5a21192`、2026-05-17T13:49:27Z マージ)、#117 / #119 / #121 / #123 / #125 / #126 / #129 / #135 (本 PR の集約消化対象元 retro 8 件)

## ✅ Keep (継続したいこと)

- **8 retro × 約 55 件 (dedupe 後 約 47 件) の改善提案を 1 PR で集約消化** (consensus: spec-conformance + architecture): EPIC-A2 完了 (PR #136) 後の harness-meta 相当タスクとして、A1〜A2-6 期間中に蓄積された 8 retro の `🤖 ハーネス改善提案` セクションから dedupe を経て約 47 件の未消化提案を抽出。スコープ A (rule 即時消化) + B (template) + C (横断 learnings 新規) の 3 軸で本 PR (PR-1) に集約、スコープ A' (ADR 補強) のみ PR #140 (PR-2) に分割。`harness-meta` Skill 本格化 (A4) 前の手動代替実行ながら、約 40 件採用 / 16 件見送り / 4 件 PR-2 移送の比率で完了。
- **PR 分割判断 (PR-1 vs PR-2) を `harness-meta-criteria.md` §N PR vs 1 PR 包括 (PR #139 内で追加した規範) に基づき適用**: 本 PR が約 30 件の rule + template 改修 (`implementation-workflow.md` 8 項目 / `harness-meta-criteria.md` 7 項目 / `roadmap.md` 4 項目 / `docs-structure.md` 5 項目 / `merge-readiness.md` 3 項目 等) を含み、code-reviewer 4 aspect の読み切り可能粒度 (PR #135 の 20 ファイル / +1675 行と比較して 34 ファイル / +878 行で同水準) を維持。ADR 補強 (ADR-0020 / 0021 / 0023 / 0027) は `accepted` ADR の immutable 原則を扱う論点が独立 + revert 単位が異なるため分離が妥当。
- **code-reviewer 4 aspect 並列で Critical 0、Improvement 2 を検出、即時消化 1 件 + 持ち越し 1 件で完走**: spec-conformance / architecture / security / code-quality の 4 aspect を `code-reviewer-aspects.md` §aspect 動的選択ルール (PR #139 で追加) の「Markdown only PR」分類に従って実行、test-quality / performance / visual-regression / design-tokens の 4 aspect を skip。binary checklist 4 aspect 合計 28 項目で 28/28 通過。architecture Improvement #1 (`merge-readiness.md:57` クロスリンク欠落) を即時消化基準 #2 「複数 rule 間の表記揺れ / 規約間の二重化」該当として fix loop commit `c9a0972` で消化、code-quality Improvement #1 (`docs-structure.md:136-139` WHAT 寄り blockquote) は持ち越し基準 #1 「補助的な改善 / 表現の最適化」該当として後続 PR 移送。
- **`code-reviewer-aspects.md` §aspect 動的選択ルール を本 PR で本格化、Markdown only PR で 4 aspect skip を規範化** (consensus: architecture + code-quality): A2-3 までは「実装コード変更ゼロでも 8 aspect 全実行 (4 aspect は auto pass 扱い)」だったが、本 PR で「Markdown only PR は 4 aspect (spec-conformance / architecture / security / code-quality) のみ実行、test-quality / performance / visual-regression / design-tokens は skip 妥当」を明文化。code-reviewer 実行 effort を半減、コメント可読性向上。
- **権限拡大 PR の self-merge 禁止 / orchestrator 明示承認による R-15 代替パス を `merge-readiness.md` で本格化** (consensus: security): PR #129 (A2-6 permission 拡張) で R-15 緩和の論点が顕在化、本 PR で「権限拡大 PR (`.claude/settings.json` / `.github/workflows/**` / OAuth scope / branch protection 改修) の self-merge 禁止」「touch ファイル分類で自動判定」「orchestrator 事前承認テキスト形式 = R-15 代替」の 3 セクションを `merge-readiness.md` に追加、本 PR 自身が「touch ファイルに権限拡張なし → self-merge 可」の test ケースとして自己整合 (Markdown + template + docs のみ change)。
- **classifier ブロックゼロで self-merge 完走 (A2-6 permission 拡張の継続効果)**: `gh pr create` / `gh pr ready 139` / `gh pr merge 139 --squash` / `git push` を全て本ペインから直接実行、out-of-band orchestrator 委任ゼロ。A2-3 (PR #135) に続く 2 PR 連続でフル self-merge 完走、classifier 迂回辞典 (`harness-meta-criteria.md` §classifier ブロック対応 迂回パターン辞典、PR #139 で追加) の予防的効果実証。
- **8 retro 📝 セクション一括 sed 正規化を中間 commit で実施 (commit `4953dc2`)**: PR 起票前は placeholder `本 PR (PR-1、...)` を使用、起票後 PR 番号 #139 確定で sed 一括置換、placeholder 残存ゼロ (binary checklist spec-conformance #7 で検証)。Skill 本格化 (A3 / A4) 後の `pr-retrospective` 自動生成では generation 時に PR 番号を注入する仕組みが必要だが、現状の手動代替フローでも 8 retro 一括正規化が機能。
- **8 retro 📝 セクションに「採用」「見送り」表追記で `harness-meta` 入力規格を全件充足** (consensus: spec-conformance): `retrospective-format.md` §📝 harness-meta フィードバック (3 表構造: 採用 / 見送り / 保留) に従い、8 retro 全件で「`### 2026-05-17 PR #139 で消化 (採用)`」「`### 2026-05-17 PR #139 で見送り (後続フェーズへ)`」見出し + 表を追加。`harness-meta` Skill 本格化 (A4) 時の parse 可能性を確保。
- **横断 learnings 2 件 (`template.md` / `classifier-denials.md`) を `flaky-tests.md` と同パターンで新規追加**: 1 PR = 1 learning ファイルとは別建ての横断 learnings の運用パターン (frontmatter / 5 行 summary / 運用ルール / 関連) を踏襲、PR #129 / #126 由来の denial 事例 4 件 + 既知の運用化済ルール一覧を `classifier-denials.md` に集約。`docs/harness/learnings/INDEX.md` 横断的 learnings 表に 2 行追加で索引一元化。
- **fix loop で architecture Improvement #1 を即時消化 (commit `c9a0972`)**: `merge-readiness.md` L57 の権限拡大 PR 検出方法セクションに `harness-meta-criteria.md` §classifier ブロック対応 迂回パターン辞典 と `implementation-workflow.md` Phase 7 §classifier ブロック発生時の運用 3 ステップ への明示参照を追加、相補関係にある 3 rule のクロスリンク透明性向上。A2-3 (PR #135) の fix loop パターン (即時消化基準該当 4 件 commit `23ac895`) と同様の運用が定着。

## ⚠️ Problem (詰まったこと / 制約)

- **8 retro から約 47 件を dedupe する手作業の規模が大きく、機械支援なし** (consensus: process): 元 retro 8 件 (#117 / 119 / 121 / 123 / 125 / 126 / 129 / 135) × 平均 14 件 / retro = 112 件の生提案を dedupe / カテゴリ化 / スコープ A / A' / B / C 分類する作業を本ペインで実施。`harness-meta` Skill 本格化 (A4) 前のため自動 dedupe / 分類ロジックなし、proposal 同士の overlap (例: A1 レトロ rules-index 正規化 ⇄ A2-3 status drift) を人間判断で集約。約 1 時間の前処理 effort。
- **PR-1 (#139) vs PR-2 (#140) の分割判断は `harness-meta-criteria.md` §N PR vs 1 PR 包括 を本 PR 内で同時に規範化したためメタ的依存**: 「複数 PR vs 1 PR」の判断基準 (撤回コスト / scope 異質性 / review aspect 差異の 3 軸) が本 PR で追加された規範でありながら、その規範に基づいて本 PR 自身の分割を判断する循環構造。code-reviewer architecture aspect の binary checklist では「本 PR の改修 (rule 追加) と分割判断 (PR-1 / PR-2) が自己整合」を検証可能だが、外部参照不能。
- **`本 PR (PR-1、...)` placeholder が起票後の sed 正規化に依存し、draft 時の整合性検証が困難** (consensus: code-quality): commit `2c288be` (初回) 時点では PR 番号未確定のため placeholder のまま、`gh pr create` 直後 (PR #139 採番) に commit `4953dc2` で sed 一括置換。draft / WIP 期間中の 8 retro の placeholder 残存箇所は手動で grep 確認、A6 機械検証 (Gradle カスタムタスク) で「learning ファイルに `本 PR (PR-` パターン残存検出 → 警告」を追加候補。
- **`harness-meta` Skill 本格化前の手動代替実行で、8 retro × 約 7 行追記 = 56 行の機械的編集作業が発生**: 各 retro `📝 harness-meta フィードバック` セクション末尾に「採用」「見送り」表を Edit ツールで 8 回追記、各 retro で約 7 行 + cross-reference + 表追加。typo / 表記揺れ (e.g. `PR #139` vs `PR-1`) のリスクは sed 正規化 commit `4953dc2` で吸収できたが、手作業 effort 大。
- **PR-2 (#140) の ADR 補強で `accepted` ADR の immutable 原則との許容範囲判断が `.claude/rules/adr.md` Gotchas に未明文化** (consensus: architecture): PR #140 description で「(a) 既存 Decision の改訂ではなく補足追加、(b) `.claude/rules/*` SoT との整合化、(c) PR #119 レトロ Try で予約済」の 3 条件を明示し「許容範囲」と判断したが、`adr.md` の Gotchas には「`accepted` 以降は本文を改変しない」原則しか記載なし。本 PR-1 で `adr.md` §ADR 化見送りの理由テンプレ (3 条件: 撤回コスト低 / scope 限定 / 既存 rule 本体改定なし) を追加したが、補足追加と Decision 改訂の境界は明文化されないまま。
- **code-reviewer Improvement #2 (`docs-structure.md:136-139` WHAT 寄り blockquote 注記) の持ち越し判断が後続 PR 起票なしのまま retrospective 行き**: `harness-meta-criteria.md` §即時消化 vs 持ち越し 基準で「補助的な改善 / 表現の最適化」該当として持ち越し、本 learning の `🤖 ハーネス改善提案` で `[rule] docs-structure.md:136-139 blockquote WHAT → WHY 書き換え` として記録。harness-meta Skill 本格化 (A4) で持ち越し提案の自動 follow-up PR 起票 (例: 7 日後 trigger) が必要。
- **squash merge で commits 3 つ (`2c288be` / `4953dc2` / `c9a0972`) が 1 commit に統合され、commit 別の責務追跡が PR description / learning に依存**: `gh pr merge --squash` の選択は merge 履歴シンプル化の利点があるが、初回 commit + sed 正規化 commit + fix loop commit の境界が squash 後の `f0f6833` では parse 不能。本 learning の「commits 数 (squash 前) = 3」記録で代替、A3 以降の commit 別検証要件 (例: code-reviewer 個別 commit に対する eval) の必要性は未確定。
- **横断 learnings 2 件の運用 owner / 追記頻度 / 機械検証が未確定**: `template.md` (pr-retrospective Skill 本格化前の暫定テンプレ、A3 完了で自動生成切替予定) と `classifier-denials.md` (denial 発生時の追記、`flaky-tests.md` と同パターン) を新規追加したが、追記 owner (orchestrator vs 各 Skill) / 頻度 (denial 発生時 vs 週次レビュー) / 機械検証 (A6 Gradle で件数閾値超過時の rule 化検討トリガー) は本 PR スコープ外。
- **8 retro 採用率 (約 40 / 47 = 85%) の高さが「採用バイアス」リスク**: dedupe 後の未消化提案 47 件のうち 40 件を本 PR で採用、PR-2 で 4 件追加採用、見送り 16 件 (うち A6 機械検証 6 + A3 Skill 2)。採用バイアス (「8 retro でレビュー済の提案だから採用」) が `harness-meta-criteria.md` 採用判定基準 1-5 の厳密適用を弱めた可能性。本 PR で採用した 40 件中、A6 / A7 で機械検証可能になった時点で「実は重複 / SoT 矛盾」と判明する候補があり得る。

## ⚠️ Problem (人間からの追加)

> **本セクションは orchestrator (subroh0508) が retro レビュー後に追加した人間追加項目** (AI 自動抽出より優先度高、source: PR #141 起票後の追加指示)

- **レトロスペクティブをきっかけとしたハーネス修正・改善のスキル (`pr-retrospective` → `harness-meta` / `harness-bootstrap` workflow) について、AI のアウトプットに改善が見られるかを確認する dry-run フェーズが存在しない**: 現状は LLM が rule / template / skill / docs を直接書き換えて commit + push まで自走するため、変更が本当に AI の出力品質を改善するか (rule strict 化が誤検知を増やさないか / template 強化が本質的に AI のミスを減らすか / Skill フロー追加が context 圧迫の割に効果が出るか) を **適用前に検証できない**。結果として「改善のはずが回帰」「無効提案がそのまま採用」のリスクが高まる。本 PR (#139) で約 40 件の提案を採用したが、各提案が後続 PR で AI の出力品質を実測で改善するかは A6 機械検証 (Lint 違反数 / coverage / mutation score) 以前は未検証、Generator-Evaluator 独立性 (R-13) と整合する dry-run 設計が現状未整備。

## 🚀 Try (次回からの改善案)

- **`harness-meta` Skill 本格化 (A4) で 8 retro 集約のような大規模 KPT 取り込みを自動化** (priority: high): 本 PR の手動代替実行 (約 2 時間 effort) を Skill 化、(1) merged retro 全件の `🤖 ハーネス改善提案` セクション parse、(2) dedupe / カテゴリ化 / スコープ分類、(3) `📝 harness-meta フィードバック` 一括追記 (PR 番号 placeholder 置換含む)、(4) PR 分割提案 (N PR vs 1 PR 包括判断) の 4 ステップを Skill で実装。`pr-retrospective` (A3) と並列で A4 着手候補。
- **`.claude/rules/adr.md` に「`accepted` ADR の補足追加許容範囲」セクション追加**: PR #140 の判断 (a)(b)(c) を一般化、「Decision 本文 / 帰結 / 根拠 / 比較した代替案 セクションは不変、コンテキスト末尾の補足段落 / 表記揺れ整合化は許容、新規 Decision の追加は新 ADR 起票」のような明文化候補。`adr.md` Gotchas に追加するか、§ADR 化見送りの理由テンプレ と並ぶ §補足追加許容範囲 として独立セクション化。
- **`harness-meta-criteria.md` §N PR vs 1 PR 包括 を更に精緻化、本 PR (PR-1 / PR-2 分割) を test ケースとして事例集化**: 「rule + template + 横断 learnings」(PR-1) vs 「ADR 補強」(PR-2) の分割が機能した条件 (撤回コスト独立 / scope 異質 / review aspect 重複ゼロ / orchestrator 事前承認は同一テキスト) を 1 事例として `harness-meta-criteria.md` に追加、後続の集約消化 PR で参照可能化。
- **placeholder 残存検出を A6 機械検証 (Gradle カスタムタスク) に追加**: learning ファイル / PR description / commit message に `本 PR (PR-[0-9]+、...)` / `本 PR で消化` (PR 番号未確定形式) パターンが残存していたら警告、起票 / merge 直前の確実な置換を強制。本 PR では sed 正規化 commit `4953dc2` で吸収できたが、機械検証で再発リスク低減。
- **横断 learnings の運用 owner / 追記頻度 / 機械検証を `flaky-tests.md` と同パターンで明文化**: `flaky-tests.md` (A2-1 起票) の運用ルール (テスト再現条件発生時に追記、件数閾値超過時に rule 化検討) を `classifier-denials.md` / `template.md` にも適用、`docs/harness/learnings/INDEX.md` 横断的 learnings 表に「追記 owner」「追記契機」列を追加。
- **`pr-retrospective` Skill 本格化 (A3) で「集約消化 PR」専用 retrospective テンプレを仕様化**: 本 PR (PR #139 + #140) のような「N retro × M 提案を 1-2 PR で集約消化」パターン専用のテンプレ、(1) 元 retro N 件のリスト + 採用 / 見送り内訳表、(2) PR 分割判断記録、(3) `📝 harness-meta フィードバック` への一括追記履歴、(4) classifier ブロック発生有無、を `retrospective-format.md` の拡張仕様として追加候補。
- **fix loop で消化した architecture Improvement #1 (merge-readiness L57 クロスリンク欠落) のような「複数 rule 間の表記揺れ / 規約間の二重化」を A6 静的検証で発見**: Gradle カスタムタスクで「rule A.md の §X セクションが rule B.md / rule C.md を参照する場合、A.md → {B.md, C.md} のクロスリンクが本文 §関連 または body 内に存在」を検証、欠落時警告。`harness-meta-criteria.md` §即時消化基準 #2 の半自動化候補。
- **PR-1 (#139) + PR-2 (#140) の 2 PR 連続 self-merge 事前承認テキストの形式を `merge-readiness.md` §orchestrator 明示承認パス で更に明確化**: 本指示「orchestrator (subroh0508) が本指示で Phase 1-9 含む self-merge を本指示で明示的に事前承認 (R-15 該当)」の語彙 (e.g. 「事前承認」/「Phase 1-9 含む」/「R-15 該当」) を canonical 表現として `merge-readiness.md` に明示、本 PR + PR-2 のような連続 PR でのテキスト再利用可能性を担保。
- **`harness-meta-criteria.md` §即時消化 vs 持ち越し 基準を本 PR で適用した事例 (即時 1 / 持ち越し 1) を rule body に追加**: 「即時消化基準 #2: 複数 rule 間の表記揺れ / 規約間の二重化」(本 PR architecture #1) / 「持ち越し基準 #1: 補助的な改善 / 表現の最適化」(本 PR code-quality #1) の 2 事例を `harness-meta-criteria.md` body に注記、判断境界の言語化を強化。
- **採用バイアス対策として `harness-meta-criteria.md` に「採用率閾値」を追加検討**: 集約消化 PR で採用率 80% 超過時に「採用判定基準 1-5 の厳密適用」を再レビューするフラグを設置、A6 機械検証で採用率を自動算出。本 PR の 85% は dedupe 後の精選結果のため妥当判断ながら、後続の集約消化 PR では基準厳格化候補。

## 🚀 Try (人間からの追加)

> **本セクションは orchestrator (subroh0508) が retro レビュー後に追加した人間追加項目** (AI 自動抽出より優先度高、source: PR #141 起票後の追加指示)

`harness-meta` / `harness-bootstrap` (retro 集約改善モード) に **dry-run フェーズ** を追加:

1. **dry-run 入力**: 改善提案を適用した rule / template / skill / docs と、未適用の既存版 (両 commit / branch / worktree のどれを使うかは設計時に確定)
2. **dry-run 実行**: 適用版と未適用版それぞれに対して、過去 retro で問題視された具体的シナリオを AI (サブエージェント、Generator 独立性のため別 system prompt) に投げて出力を比較。シナリオ抽出元は本 retrospective 群の `⚠️ Problem` セクション (再現条件 + 過去の AI 出力との対比で判定可能なもの)
3. **判定基準**: 適用版が未適用版より明らかに望ましい出力 (誤検知減 / 規約遵守率向上 / 提案精度向上) を出す場合のみ commit + push に進む。改善が見られない / 退化する場合は変更を破棄 or 別案検討
4. **dry-run 結果記録**: `docs/harness/dry-runs/YYYY-MM-DD-pr-NNN.md` (新規ディレクトリ) に before/after の AI 出力差分と判定理由を残す
5. **`.claude/rules/harness-meta-criteria.md` (A2-3 で配置済) に dry-run 必須条件を追記** (例: rule 全文書き換え / Skill 新規追加 / template 構造変更は dry-run 必須、typo 修正 / リンク追加は不要)

## 📊 指標

PR #139 + #140 は実装コード差分ゼロ (`.claude/rules/` + template + `docs/adr/` + 横断 learnings Markdown のみ)、三層テスト品質基盤 (Kover / Konsist Spec / PITest) は A7 で導入予定のため Before/After 差分は計測不能 (N/A)。

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| Line coverage | N/A | N/A | — | Kover 未導入 (A7 で導入予定) |
| Branch coverage | N/A | N/A | — | 同上 |
| Spec coverage | N/A | N/A | — | Konsist `@Spec` 未導入 (A7 で導入予定) |
| Mutation score | N/A | N/A | — | PITest 未導入 (A7 で導入予定) |
| Lint 違反数 | N/A | N/A | — | Spotless / ktlint / detekt / markdownlint-cli2 未導入 (A6 で導入予定) |
| ファイル数 (差分、PR-1 #139) | (PR-1 前) | 34 changed | 34 changed | rule 19 / template 3 / horizontal learnings 2 / 8 retro 📝 追記 8 / CLAUDE.md 1 / `.claude/skills/code-reviewer/SKILL.md` 1 |
| ファイル数 (差分、PR-2 #140) | (PR-1 merge 後) | 5 changed | 5 changed | ADR-0020 / 0021 / 0023 / 0027 + retro 119 📝 正規化 |
| 行数 (差分、PR-1) | (PR-1 前) | +878 / -35 (squash merge commit `f0f6833`) | net +843 | 全て docs / rule / template |
| 行数 (差分、PR-2) | (PR-1 merge 後) | +47 / -10 (squash merge commit `5a21192`) | net +37 | 全て ADR + retro 119 📝 |
| commits 数 (squash 前、PR-1) | (PR-1 前) | 3 (`2c288be` 初回 + `4953dc2` sed 正規化 + `c9a0972` fix loop) | +3 | `--squash` で 1 commit に統合 |
| commits 数 (squash 前、PR-2) | (PR-1 merge 後) | 2 (`bc1a5d7` 初回 + `6cb54dc` retro 119 📝 正規化) | +2 | 同上 |
| 8 retro `📝 harness-meta フィードバック` 追記件数 | 0 (placeholder のみ) | 8 retro × 採用 + 見送り 表 = 16 表 | +16 表 | sed 一括正規化で PR #139 表記統一 |
| 8 retro 採用提案件数 (合計) | 0 | 約 40 (PR-1 採用) + 4 (PR-2 採用) = 約 44 | +44 | dedupe 後 約 47 件中 約 44 件採用、見送り 約 3 件 (A6 / A3 / A4 持ち越し) |
| 8 retro 見送り提案件数 (後続フェーズ移送) | 0 | 16 (A6 機械検証 6 / A3 Skill 6 / A4 Skill 2 / その他 2) | +16 | `📝 harness-meta フィードバック` 見送り表で全件記録 |
| 採用率 (採用 / 採用 + 見送り) | — | 約 44 / 60 = 73% | — | dedupe 後 約 47 件 + 8 retro 由来の追加提案 13 件 = 計 60 件中、PR-1 + PR-2 で 44 件採用 |
| Rule 数 (`.claude/rules/`) | 53 | 53 | ±0 | 新規追加なし (既存 19 件の改修のみ) |
| Rule 改修件数 (本 PR スコープ) | — | 19 | +19 | `implementation-workflow` / `harness-meta-criteria` / `merge-readiness` / `commit-message` / `roadmap` / `docs-structure` / `template-language` / `rules-index` / `adr` / `secrets` / `backend-auth` / `network-client` / `pr-template` / `branch-naming` / `spec-living-sync` / `skill-authoring` / `code-reviewer-aspects` / `ui-snapshot` / CLAUDE.md |
| 横断 learnings 新規件数 | 1 (`flaky-tests.md`) | 3 (+ `template.md` / `classifier-denials.md`) | +2 | `INDEX.md` 横断的 learnings 表に 2 行追加 |
| Template 改修件数 (本 PR スコープ) | — | 3 (`harness.md` / `feature.md` / `bugfix.md`) | +3 | harness.md 3 列追加、feature / bugfix の注記 blockquote 化 |
| ADR 改修件数 (PR-2 スコープ) | — | 4 (ADR-0020 / 0021 / 0023 / 0027) | +4 | 補足追加のみ、Decision 本体不変 |
| code-reviewer aspect 実行 (subagent 並列、PR-1) | — | 4/4 pass (spec-conformance / architecture / security / code-quality) | — | test-quality / performance / visual-regression / design-tokens skip (Markdown only / A10 enable 待ち) |
| code-reviewer aspect 実行 (subagent 並列、PR-2) | — | 4/4 pass (同上) | — | 同上、PR-2 は Improvement 0 件 |
| Critical findings (PR-1 + PR-2 合計) | — | 0 | — | merge readiness 3 条件 (CI green + Critical 0 + 人間 approve / orchestrator 事前承認) 充足 |
| Improvement 件数 (PR-1) | — | 2 (architecture 1 即時消化 + code-quality 1 持ち越し) | — | 即時消化 1 件 (commit `c9a0972`)、持ち越し 1 件 (本 learning `🤖 ハーネス改善提案`) |
| Improvement 件数 (PR-2) | — | 0 | — | binary checklist 21/21 通過 |
| binary checklist 通過率 (4 aspect 集約、PR-1) | — | 28/28 ✅ | — | spec-conformance 7 + architecture 7 + security 7 + code-quality 7、Improvement は checklist 外 |
| binary checklist 通過率 (4 aspect 集約、PR-2) | — | 21/21 ✅ | — | spec-conformance 5 + architecture 5 + security 5 + code-quality 6 |
| classifier ブロック数 (本 PR + PR-2 関連の `gh` / `git` コマンド) | (A2-6 permission 拡張前) | 0 | — | A2-6 (PR #129) permission 拡張の継続効果検証、`gh pr ready` / `gh pr merge --squash` / `git push` 全て本ペインから直接実行 |
| worktree 並走数 (PR-1 + PR-2 + retrospective) | 1 | 2 (本 PR は PR-1 + PR-2 連続 self-merge を 1 worktree で実施、retrospective worktree 1 件) | +1 | `feature/harness-retro-consolidate` (PR-1 / PR-2 兼用) + `harness/learnings-pr-139-140` (本ファイル)、A2-3 (PR #135) の 3 worktree 並走と比較し効率化 |
| Phase 0-9 完走時間 (PR-1 + PR-2、概算) | — | 約 2.5 時間 | — | 8 retro dedupe + 集約消化 + code-reviewer 並列 + fix loop + PR-1 merge + PR-2 起票 + merge + worktree cleanup |
| PR-1 / PR-2 起票間隔 | — | 約 21 分 (PR-1 merge `13:22:50Z` → PR-2 起票 `13:40:54Z`) | — | 分割判断の機動性指標、PR-1 merge 後即時 PR-2 起票で連続 self-merge 完走 |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造 -->

<!-- 以下 3 件は orchestrator (subroh0508) が retro レビュー後に追加した人間追加提案、AI 自動抽出より優先度高 -->

- [ ] `[skill]` `harness-meta` / `harness-bootstrap` (retro 集約改善モード) に **dry-run フェーズを追加** (人間からの追加、最優先): 上記 `🚀 Try (人間からの追加)` の 5 ステップ準拠 (適用 / 未適用版を別 subagent で並列実行 → 出力比較 → 判定基準達成時のみ commit + push)。AI 出力品質改善の事前検証不在 (上記 `⚠️ Problem (人間からの追加)`) を解消、Generator-Evaluator 独立性 (R-13) と整合。採用判定基準 4 (Critical 派生、無効提案リスク回避) 該当
- [ ] `[rule]` `.claude/rules/harness-meta-criteria.md` に **dry-run 必須条件** を追記 (人間からの追加、最優先): rule 全文書き換え / Skill 新規追加 / template 構造変更は dry-run 必須、typo 修正 / リンク追加は不要、のような閾値を明文化。`harness-meta-criteria.md` 採用判定基準 1-5 と並ぶ §dry-run 必須条件 として独立セクション化候補。採用判定基準 4 (Critical 派生) 該当
- [ ] `[template]` `docs/harness/dry-runs/template.md` を新規追加 (人間からの追加、最優先): dry-run 結果記録のフォーマット (before/after AI 出力差分 + 判定理由 + 採用 / 破棄判定)。`docs/harness/learnings/template.md` (PR #139 で追加済) と同パターン、`docs/harness/dry-runs/YYYY-MM-DD-pr-NNN.md` 専用ディレクトリの索引 (INDEX.md) も同時設計候補。採用判定基準 4 (Critical 派生) 該当

<!-- 以下は AI 自動抽出 (consensus: code-reviewer 4 aspect + 本ペイン手動 retro 抽出) -->

- [ ] `[rule]` `.claude/rules/docs-structure.md:136-139` の NG 例直後 blockquote 注記を WHAT 説明 → WHY 説明に書き換え (code-reviewer code-quality Improvement #1 持ち越し): 採用判定基準 1 (複数 PR 反復) / 採用判定基準 3 (R-XX 対応) のいずれにも該当しないが、`harness-meta-criteria.md` §持ち越し基準 #1 「補助的な改善 / 表現の最適化」該当のため後続の `docs-structure.md` 改修 PR で対応
- [ ] `[rule]` `.claude/rules/adr.md` に「`accepted` ADR の補足追加許容範囲」セクション追加 (PR-2 / #140 由来): PR #140 の判断 (a)(b)(c) を一般化、Decision 本文不変 + コンテキスト末尾の補足段落 / 表記揺れ整合化は許容 + 新規 Decision の追加は新 ADR 起票、を明文化。採用判定基準 2 (ADR 関連) 該当
- [ ] `[rule]` `.claude/rules/harness-meta-criteria.md` §N PR vs 1 PR 包括 に本 PR (PR-1 / PR-2 分割) を test ケースとして事例集化: 「rule + template + 横断 learnings」(PR-1) vs 「ADR 補強」(PR-2) の分割が機能した条件を 1 事例として body に追加。採用判定基準 1 (複数 PR 反復、本 PR で 2 PR 連続実証) 該当
- [ ] `[rule]` `.claude/rules/harness-meta-criteria.md` §即時消化 vs 持ち越し 基準に本 PR の適用事例 2 件を rule body に追加: 「即時消化基準 #2: 複数 rule 間の表記揺れ / 規約間の二重化」(本 PR architecture #1 / commit `c9a0972`) / 「持ち越し基準 #1: 補助的な改善 / 表現の最適化」(本 PR code-quality #1)。判断境界の言語化強化。採用判定基準 1 (複数 PR 反復) 該当
- [ ] `[rule]` `.claude/rules/merge-readiness.md` §orchestrator 明示承認パス に事前承認テキスト canonical 形式 (「Phase 1-9 含む self-merge を本指示で明示的に事前承認 (R-15 該当)」) を追加: PR-1 (#139) + PR-2 (#140) で再利用された語彙の正規化。採用判定基準 4 (Critical 派生) は該当せず、採用判定基準 1 (複数 PR 反復) 該当
- [ ] `[skill]` `harness-meta` Skill 本格化 (A4) で 8 retro 集約のような大規模 KPT 取り込みを自動化: (1) merged retro 全件の `🤖 ハーネス改善提案` セクション parse、(2) dedupe / カテゴリ化 / スコープ分類、(3) `📝 harness-meta フィードバック` 一括追記 (PR 番号 placeholder 置換含む)、(4) PR 分割提案 の 4 ステップを Skill 化。`pr-retrospective` (A3) と並列で A4 着手候補。採用判定基準 1 (複数 PR 反復、8 retro × 1 集約 = 8 PR 分の手動 effort) 該当
- [ ] `[skill]` `pr-retrospective` Skill 本格化 (A3) で「集約消化 PR」専用 retrospective テンプレを仕様化: 本 PR (PR-1 + PR-2) のような「N retro × M 提案を 1-2 PR で集約消化」パターン専用、`retrospective-format.md` の拡張仕様として追加候補。採用判定基準 1 (複数 PR 反復) 該当
- [ ] `[skill]` placeholder 残存検出を A6 機械検証 (Gradle カスタムタスク) に追加: learning ファイル / PR description / commit message に `本 PR (PR-[0-9]+、...)` / `本 PR で消化` (PR 番号未確定形式) パターンが残存していたら警告。本 PR で sed 正規化 commit `4953dc2` で吸収したが機械検証で再発リスク低減。採用判定基準 3 (R-XX 対応、PR description 整合性) 該当
- [ ] `[skill]` 「複数 rule 間の表記揺れ / 規約間の二重化」を A6 静的検証で発見: Gradle カスタムタスクで「rule A.md の §X セクションが rule B.md / rule C.md を参照する場合、A.md → {B.md, C.md} のクロスリンクが本文 §関連 または body 内に存在」を検証、欠落時警告。`harness-meta-criteria.md` §即時消化基準 #2 の半自動化候補。採用判定基準 1 (複数 PR 反復) 該当
- [ ] `[template]` 横断 learnings の運用 owner / 追記頻度 / 機械検証を `flaky-tests.md` と同パターンで `docs/harness/learnings/INDEX.md` 横断的 learnings 表に列追加: 「追記 owner」「追記契機」列を追加、`classifier-denials.md` / `template.md` の運用基準を明文化。採用判定基準 1 (複数 PR 反復) 該当
- [ ] `[rule]` `harness-meta-criteria.md` に「採用率閾値」を追加検討: 集約消化 PR で採用率 80% 超過時に「採用判定基準 1-5 の厳密適用」を再レビューするフラグを設置、A6 機械検証で採用率を自動算出。本 PR の 73% は妥当判断ながら、後続の集約消化 PR では基準厳格化候補。採用判定基準 1 (複数 PR 反復) 該当

## 📝 harness-meta フィードバック

<!-- harness-meta が後から追記。提案 → 結果の往復ログを 1 ファイル内で完結 -->

### YYYY-MM-DD <PR ID> で消化 (採用)

| 提案 | <PR ID> での消化内容 |
|---|---|

### YYYY-MM-DD <PR ID> で見送り (後続フェーズへ)

| 提案 | 見送り理由 / 移行先 |
|---|---|

### YYYY-MM-DD <PR ID> で保留 (要再評価)

| 提案 | 保留理由 |
|---|---|

## 関連

- 元 retro 8 件: `docs/harness/learnings/2026-05-17-pr-{117,119,121,123,125,126,129,135}.md` (本 PR で全件 `📝 harness-meta フィードバック` セクション一括追記)
- 関連 PR (本 PR から分割): [#140](https://github.com/subroh0508/colormaster/pull/140) (ADR-0020 / 0021 / 0023 / 0027 補強)、本 retrospective ファイル `2026-05-17-pr-140.md` で短いクロスリファレンス
- 横断 learnings (本 PR で新規): [`template.md`](./template.md) / [`classifier-denials.md`](./classifier-denials.md)
- Epic: [`EPIC-A2-rules-docs-extension`](../../epics/EPIC-A2-rules-docs-extension/) (本 PR は `completed` 昇格 (PR #136) 後の harness-meta 相当タスク)
- 関連 rule: `.claude/rules/{retrospective-format,harness-meta-criteria,merge-readiness,implementation-workflow,adr,code-reviewer-aspects}.md`
- 関連 ADR: ADR-0017 / 0018 / 0019 / 0024 / 0025 / 0026 / 0027 (PR-1 触れた範囲)、ADR-0020 / 0021 / 0023 / 0027 (PR-2 補強範囲)

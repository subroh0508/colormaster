---
id: learning-pr-171
title: PR #171 レトロスペクティブ (PLAN-002 / 3 軸定量評価フレーム導入)
type: learning
status: draft
related_pr: 171
related_plan: PLAN-002
related_epic: —
generated_at: 2026-05-19T17:30:00+09:00
generator: pr-retrospective (skeleton、本ペインで手動代替実行)
---

# PR #171 レトロスペクティブ (PLAN-002 / 3 軸定量評価フレーム導入)

> 生成: pr-retrospective (skeleton、本ペインで手動代替実行) at 2026-05-19T17:30:00+09:00
> 関連 Plan: PLAN-002 / 関連 Epic: —
> 対象 PR: [harness: 3 軸定量評価フレーム (改善度/再現性/副作用) 導入 (PLAN-002 / ADR-0028) #171](https://github.com/subroh0508/colormaster/pull/171) (squash merged at 2026-05-19T17:04:28Z、commit `13d097b`、from head `d4d9aed`)
> 差分: 16 ファイル / +1003 / -56 (1059 diff 行)、commits: 6 初回 + 1 housekeeping (PR# rename) + 1 fix loop (code-reviewer Critical 1 + Major 2 + Minor 2) = 8 commits

## ✅ Keep (継続したいこと)

- **3 段 Skill 連鎖 (harness-evolution → plan-author → implementation-workflow) の 1 セッション完結**: 本セッションで /harness-evolution Phase 1-5 → /plan-author 手動代替 (B0 skeleton 経由) → /implementation-workflow Phase 0-9 を全て連続実行、外部知見取得から merge まで 1 ペイン完走。orchestrator pane 不在でも user (subroh0508) が orchestrator として明示承認することで R-15 充足 (consensus: spec-conformance + architecture)
- **外部知見と proposal の事前照合 (Critical 4 + High 3 件指摘で軌道修正)**: proposal 起票後に「信頼度の高い外部知見と照合して問題箇所をチェック」する self-review フェーズを設けたことで、zero-tolerance threshold (Critical #1) / 固定 Jaccard 0.85 (Critical #2) / 自動 verdict (Critical #3) / N=3-5 試行 (Critical #4) / Before 列矛盾 (High #5) / Jaccard 適用範囲 (High #6) / golden set 固定 (High #7) を merge 前に解消。Anthropic / Braintrust / Evidently / LangChain / Langfuse / `anthropics/skills` の 8 出典で 12 件指摘 → option B で Critical + High 7 件反映 + verdict 廃止追加で合計 8 件改修 (consensus: spec-conformance + architecture)
- **golden set 和訳「基準シナリオ集」+ retire 等の用語段階的日本語化**: 業界用語 (golden set / regression / retire 等) を初回 mention で「基準シナリオ集 (= golden set)」のような gloss 形式で導入、業界資料との cross-reference を維持しつつ日本語読者の認知負荷を低減 (consensus: spec-conformance + code-quality)
- **self-bootstrap dry-run の循環構造を明示 + baseline で補正**: 「3 軸定量評価で 3 軸定量評価フレームを評価する」循環構造を proposal / Plan / dry-run で明示認識し、legacy monitor-dedup dry-run のレトロフィット適用を baseline として補正設計。N=1 制約付き ✅ + A4 本格化後の N=10 以上再計測義務を §特記事項で明示することで「設計上の再現性 ✅」の限界を honest に記述 (consensus: architecture)
- **dry-run 入力記録 4 ブロック (起動 Skill / プロンプト / 環境 / 入力ファイル) の必須化**: レビュワー追検証可能性を担保、再現性軸の閾値判定の前提条件として「同一入力」の定義が成立。N=1 制約下でも入力記録があれば後続セッションで N 拡張の再計測ができる (consensus: architecture + spec-conformance)
- **code-reviewer 4 aspect 並列の architecture aspect での Critical 検出**: 6/8 aspect skip 妥当な harness 改修でも architecture aspect が ADR 番号衝突 + INDEX 未更新を Critical 検出、fix loop 1/3 で全件解消。並列 Agent (general-purpose) 4 件で 100s 程度の review 完了は実用的 (consensus: architecture)
- **commit 分離 (6 commits + 2 housekeeping/fix loop = 8 commits)**: 「6-20 ファイル: 1 PR + commit 分離」境界に従い、seed / 新規 SoT / rule / template / SKILL.md / dry-run の 6 グループに commit を logical separator で分離、PR review 時の diff 読み切り可能性を担保
- **classifier 2 回 denial を中立化なし + orchestrator 承認スコープ内で retry**: commit-2 ファイル Write 1 回 + fix loop commit 1 回の計 2 回 denial が発生、迂回せず user の事前承認 (「人間 commit と同じ処理を承認する」) スコープ内で retry → 通過。`.claude/rules/implementation-workflow.md` §classifier ブロック対応 3 ステップに整合

## ⚠️ Problem (詰まったこと / 制約)

- **ADR-0028 番号衝突 (architecture Critical #1)**: 既存 5 ファイル (`adr.md` / `adr-author/SKILL.md` × 2 / `roadmap.md` × 2) で「ADR-0028」が PR #129 (auto-merge 緩和、起票見送り) を指していたため、本 PR の新 ADR-0028 と意味衝突。ADR 番号予約 → 起票見送り → 解放 → 再割当 のライフサイクルが既存 docs に未文書化 (consensus: architecture)
- **PLAN-002 AC-10 と R-34 整合不一致 (architecture Major #1)**: Plan ファイル本体 AC-10 が「roadmap-tracker で roadmap.md 反映」と R-34 に矛盾、PR description AC-10 は正しく修正されていたが Plan SoT が drift。`plan-author` Skill の skeleton 状態が原因 (consensus: architecture)
- **harness-meta SKILL.md Phase 3 dry-run skip 分岐の暗黙性 (architecture Major #2)**: dry-run skip 時に「3 軸 step 7 個を skip するか」が文書構造から自明でなく、A4 自動化時に skip 経路でも 3 軸 step を試みる誤実装リスクあり。fix loop で「以下 7 step は dry-run 必須時のみ」を明示追加で解消
- **classifier stochastic denial 2 回 (Phase 5 commit-2 Write + Phase 6 fix loop commit)**: 同じ Co-Authored-By パターンが他 5 ファイルで通過した直後に 1 ファイルだけ denied、stochastic 挙動が再現。Phase 7 spec の「迂回せず人間判断を仰ぐ」に従い user の事前承認スコープを確認後 retry で通過、但し再現性のないブロックは作業フロー中断の原因
- **commit subject 3 件が 72 字超過 (code-quality Minor #1)**: `feat(harness): harness-meta + harness-evolution SKILL.md の Phase 改修 (ADR-0028 / PLAN-002)` 89 字、`docs(harness): dry-run template + PR template の 3 軸定量評価対応 + 既存 dry-run 2 件 legacy_verdict marker` 96 字、`docs(harness): harness-meta-criteria + harness-evolution に §3 軸定量評価 セクション追加 (ADR-0028 / PLAN-002)` 97 字。100 字 hard limit 内のため warning レベル、body に括弧内補足を移すべきだったが Phase 5 で commit message 起草時に subject 長計算をしていなかった
- **plan-author Skill が B0 skeleton で手動代替必要**: Plan 起票時に skill-creator 経由ではなく手動代替実行となり、Plan template の機械的整合性チェック (frontmatter / 5 行 summary / AC 形式 等) を skip。本 PR の Plan は手動で template に従ったため問題なかったが、A3 で plan-author Skill 本格化されるまで手動代替が継続するリスク
- **pr-retrospective Skill も B0 skeleton で手動代替必要**: 本 retrospective ファイルも手動代替実行で起票、A3 / A4 で本格化されるまで pr-poller 経由の自動 trigger は不可
- **self-bootstrap dry-run の N=1 制約**: subagent 並列実行は本セッション内で原理的困難 (本セッション自身が新版 subagent として機能)、手動代替で N=1 を「設計上の再現性 ✅」として記録したが、統計的有意性は担保されていない。A4 本格化後の N=10 以上再計測が必須

## 🚀 Try (次回からの改善案)

- **plan-author Skill (A3) 本格化時に AC SoT 整合検証を組み込む**: Plan ファイル本体 AC と PR description AC の R-34 / R-29 等の rule との整合性を skill-creator rubric に追加、本 PR Major #1 (AC-10 矛盾) を予防
- **harness-meta SKILL.md Phase 3 の skip 経路明示と同様のパターンを全 Skill SKILL.md に展開**: 「以下フローは <条件> 該当時のみ実行」「該当しない場合は Phase N に直接遷移」の明示パターンを skill-authoring.md / 100-point rubric に追加項目化、SKILL.md drift を予防
- **commit-msg hook 拡張: subject 長 72 字超を warning → block オプション化**: A6 で markdownlint-cli2 統合と合わせて commit-msg hook の subject 長制限を `.claude/rules/commit-message.md` 規約と整合 (現状 72 字超 = warning、100 字超 = block)
- **ADR 採番ライフサイクル (予約 → 起票見送り → 解放 → 再割当) を adr.md / adr-author SKILL.md に明示**: 本 PR で ADR-0028 番号衝突が発生したため、番号予約 → 見送り時の「解放可否」「他 ADR への再割当可否」「履歴記録方法」を rule SoT 化
- **self-bootstrap dry-run の N 拡張 retro を A4 本格化後に必須化**: 本 PR の N=1 制約 ✅ は手動代替の限界、A4 完了後の subagent 並列基盤で `monitor-dedup` レトロフィット + 本 PR self-bootstrap を N=10 以上で再計測し、計測由来閾値 (median - 1σ) を初期 placeholder から正式 SoT に昇格
- **classifier denial の自動 retry + 中立化判定の運用 SoT 化**: 同じパターンの 2 回連続 denial を pattern matching で検出し、orchestrator 事前承認スコープ内であれば 1 回 retry を許容 (機械的迂回ではなく stochastic 補正)、3 回連続 denial で人間判断を仰ぐ 3 ステップ運用を `.claude/rules/implementation-workflow.md` Phase 7 に明示追加
- **基準シナリオ集 (golden set) の初期 K=5 シナリオ選定を自動化**: 本 PR では手動で S1-S5 を既存 retro / dry-run から選定したが、A4 で `harness-meta` Skill が直近 N 件 retro の `⚠️ Problem` から副作用候補を自動抽出する trigger を実装
- **pr-retrospective Skill (A3) 本格化時に self-bootstrap dry-run 関連 PR の特殊フォーマットを追加**: 通常 retro + dry-run 関連 retro の差分 (循環構造の認識 / baseline 補正 / N 制約) を template SoT 化、A4 で N 拡張 retro と統合

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| Line coverage | N/A | N/A | N/A | Kover (A7 完了後)、本 PR は Kotlin code 変更ゼロのため算出不能 |
| Branch coverage | N/A | N/A | N/A | 同上 |
| Spec coverage | N/A | N/A | N/A | Konsist `@Spec` (A7 完了後)、本 PR は Spec ファイル変更なし |
| Mutation score | N/A | N/A | N/A | PITest (A7 完了後) |
| Lint 違反数 | N/A | N/A | N/A | Spotless / ktlint / detekt / markdownlint-cli2 (A6 完了後) |
| **PR 固有指標** | | | | |
| 変更ファイル数 | 0 | 16 | +16 | seed 4 + 新規 4 + 改修 6 + legacy marker 2 |
| 追加行数 | 0 | 1003 | +1003 | docs 中心 (ADR-0028 + golden-set.md + Plan + dry-run + rule + template + SKILL.md) |
| 削除行数 | 0 | 56 | +56 | dry-run template の verdict セクション削除 + 既存 SKILL.md フロー差し替え |
| commits | 0 | 8 | +8 | 6 logical + 1 housekeeping (PR# rename) + 1 fix loop (Critical 1 + Major 2 + Minor 2) |
| code-reviewer Critical findings | N/A | 1 (fix loop で 0) | 1 → 0 | architecture aspect: ADR 番号衝突 + INDEX 未追加、fix loop 1/3 で解消 |
| code-reviewer Major findings | N/A | 2 (fix loop で 0) | 2 → 0 | architecture aspect: AC-10 R-34 矛盾 + dry-run skip 分岐暗黙性 |
| code-reviewer Minor findings | N/A | 6 (fix loop で 2 解消、4 持ち越し) | 6 → 4 | spec-conformance 2 + architecture 2 (うち 2 fix loop で解消) + code-quality 2 |
| 3 軸定量評価 (self-bootstrap) | N/A | #1 (Approve 推奨) | N/A | 改善度 0/3 再発率 ✅ + 副作用 0/5 退化 + 新規 Critical 0 件 ✅ + 再現性 N=1 制約付き ✅ |
| classifier denial 件数 | 0 | 2 (Write × 1 + Bash commit × 1) | +2 | 同 pattern が他 5 ファイルで通過後の 1 件 denial × 2、stochastic 挙動、retry で全件通過 |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造 -->

- [ ] `[rule]` ADR 採番ライフサイクル (予約 → 起票見送り → 解放 → 再割当) を `.claude/rules/adr.md` / `.claude/skills/adr-author/SKILL.md` に明示追加。本 PR の ADR-0028 番号衝突を予防する規約化、採用判定基準 4 (Critical findings 由来) 該当
- [ ] `[rule]` `.claude/rules/commit-message.md` の subject 長制限を hook で block オプション化 (現状 warning のみ)。A6 で markdownlint-cli2 統合と同時に実装、採用判定基準 1 (本 PR + 過去複数 PR で subject 長超過の反復) 該当
- [ ] `[skill]` `plan-author` Skill (A3 本格化時) に「Plan AC と適用 rule (R-34 / R-29 等) の整合検証」を rubric 追加。本 PR Major #1 を予防、採用判定基準 4 (Critical findings 派生) 該当
- [ ] `[skill]` 全 SKILL.md に「分岐明示パターン」(以下フローは <条件> 該当時のみ + 該当しない場合は Phase N に直接遷移) を skill-authoring.md / 100-point rubric に追加項目化。本 PR Major #2 と同様の暗黙分岐リスクを予防、採用判定基準 4 該当
- [ ] `[template]` `.claude/rules/implementation-workflow.md` Phase 7 §classifier ブロック対応に「同 pattern 2 回連続 denial の retry 許容判定 (orchestrator 事前承認スコープ内のみ)」を追加。本 PR で 2 回 denial 発生、採用判定基準 1 (PR #125 / #129 でも反復) 該当
- [ ] `[skill]` `harness-meta` Skill (A4 本格化時) に「直近 N 件 retro の `⚠️ Problem` から副作用候補を自動抽出して基準シナリオ集追加候補化」trigger を実装。本 PR で基準シナリオ集 K=5 選定が手動、採用判定基準 2 (A4 で予定) 該当
- [ ] `[skill]` `pr-retrospective` Skill (A3 本格化時) に「self-bootstrap dry-run 関連 PR の特殊フォーマット」追加 (循環構造 / baseline 補正 / N 制約 を template 化)。本 PR で手動代替で記述、採用判定基準 2 (A3 で予定) 該当
- [ ] `[rule]` `.claude/rules/harness-meta-criteria.md` §dry-run 3 軸定量評価 の N=1 制約付き ✅ の運用例として本 PR を引用追加 (運用熟成データ蓄積)、A4 で N=10 以上再計測完了後に「N=1 暫定 → N=10 確定」昇格パターンを SoT 化

## 📝 harness-meta フィードバック

<!-- harness-meta が後から追記。提案 → 結果の往復ログを 1 ファイル内で完結 -->

### YYYY-MM-DD <PR ID> で消化 (採用)

| 提案 | <PR ID> での消化内容 |
|---|---|

### 2026-05-19 harness-meta judgment で見送り (後続フェーズへ)

| 提案 | 見送り理由 / 移行先 |
|---|---|
| `[rule]` ADR 採番ライフサイクル (予約 → 起票見送り → 解放 → 再割当) を `.claude/rules/adr.md` / `.claude/skills/adr-author/SKILL.md` に明示追加 | 採用判定基準 4 該当だが、ADR 起票ライフサイクルは dry-run 必須条件 (rule 本体改修 + SoT 方向) に該当する可能性が高く、`adr-author` Skill 本格化と同時検討が安全。後続 PR で dry-run 経由起票推奨 |
| `[rule]` `commit-message.md` の subject 長制限を hook で block オプション化 | A6 (markdownlint-cli2 + commit-msg hook 拡張) スコープ |
| `[skill]` `plan-author` Skill に「Plan AC と適用 rule の整合検証」を rubric 追加 | A3 (plan-author Skill 本格化) スコープ |
| `[skill]` 全 SKILL.md に「分岐明示パターン」を skill-authoring rubric に追加項目化 | 採用判定基準 4 該当だが、skill-authoring 100-point rubric 改修は採用判定基準 / 必須条件 級の核心改修のため dry-run 必須、別 PR で dry-run 経由起票推奨 |
| `[template]` `implementation-workflow.md` Phase 7 §classifier ブロック対応に「同 pattern 2 回連続 denial の retry 許容判定」を追加 | 採用判定基準 1 該当だが、classifier 迂回判定の改修は採用判定基準 / 必須条件 級のためdry-run 必須、`harness-meta-criteria.md` §迂回パターン辞典 と同時改修推奨 |
| `[skill]` `harness-meta` Skill に副作用候補自動抽出 trigger 実装 | A4 (harness-meta Skill 本格化) スコープ |
| `[skill]` `pr-retrospective` Skill に self-bootstrap dry-run 関連 PR の特殊フォーマット追加 | A3 (pr-retrospective Skill 本格化、A3-10 完了済だが追加機能は A4 で N 拡張 retro と統合推奨) スコープ |
| `[rule]` `harness-meta-criteria.md` §dry-run 3 軸定量評価 の N=1 制約付き ✅ 運用例として本 PR を引用追加 | A4 で N=10 再計測完了後に「N=1 暫定 → N=10 確定」昇格パターンと同時 SoT 化が望ましい |

### YYYY-MM-DD <PR ID> で保留 (要再評価)

| 提案 | 保留理由 |
|---|---|

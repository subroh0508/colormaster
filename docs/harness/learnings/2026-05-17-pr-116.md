---
id: learning-pr-116
title: PR #116 レトロスペクティブ (AI harness implementation plan 確定)
type: learning
status: draft
related_pr: 116
related_plan: —
related_epic: EPIC-000-harness-foundation
generated_at: 2026-05-19T17:50:00+09:00
generator: pr-retrospective (skeleton、本ペインで手動代替実行)
---

# PR #116 レトロスペクティブ (AI harness implementation plan 確定)

> **5 行以内 summary**: ColorMaster に AI 駆動開発ハーネスを段階的導入する計画書
> (`docs/harness/plan.md` +1753 行) を 1 ファイル / 29 commits で確定。Skill 16 種、
> ADR 27 件、フェーズ B0 / A1-A10 / C1-C10、リスク R-1〜R-37、Single Source of Truth
> を明示。本 PR が後続全 PR (#117〜) の harness 起点となるため、設計判断の事前固定
> 価値が高い。

> 生成: pr-retrospective (skeleton、本ペインで手動代替実行) at 2026-05-19T17:50:00+09:00
> 関連 Plan: — / 関連 Epic: EPIC-000-harness-foundation (B0 で起票)
> 対象 PR: [docs: add AI harness implementation plan #116](https://github.com/subroh0508/colormaster/pull/116) (squash merged at 2026-05-16T21:20:07Z、commit `20f7d7d`、from head `docs/harness-plan`)
> 差分: 1 ファイル / +1753 / -0 (1753 diff 行)、commits: 29 (議論履歴を 1 PR 内で集約)

## ✅ Keep (継続したいこと)

- **plan.md を Single Source of Truth として 1 ファイルに集約**: ADR 27 件 / Skill 16 種 / フェーズ B0/A1-A10/C1-C10 / リスク R-1〜R-37 / 命名規約 / コミット規約 / Markdown 検証スタック / MCP 採用方針 を 1 ファイルにまとめ、後続 PR が常に同じ場所を参照できる構造を確立 (consensus: architecture)
- **三層指標 (line/branch coverage / Spec coverage / Mutation score) を Phase A 完了時「計測可能」、Phase C 各 PR で差分 100%、Phase C 完了時に全体 100% という段階達成設計**: Goodhart's law 回避と段階的本格化を両立 (consensus: spec-conformance)
- **設計書本文 (`docs/{requirements,specifications}/**`) にコード断片を書かない原則 (§4.6 のコード禁止原則)**: 仕様 drift を予防し、AI が要件 → 仕様 → 実装の流れで自律的に読める構造を担保 (consensus: spec-conformance)
- **Skill 16 種の責務分離 + `roadmap-tracker` 横断**: feature-request / bug-fix / refactor / dependency-upgrade (Spec Gen) → implementation-workflow (Implementation) → code-reviewer (Evaluation) → pr-poller / pr-retrospective / harness-meta / harness-evolution (Retrospection + Meta) の 6 段階フロー設計、roadmap-tracker は片方向ミラーで横断 (consensus: architecture)
- **GitHub Actions から Claude API を呼ばない方針 (ADR 0017)**: コスト回避 + PII 漏洩経路集約のため、ローカル Claude Code 内ポーリング駆動に集約。後続全 Skill の起動経路を統一する基盤判断 (consensus: security + architecture)
- **PR テンプレート複数 (`feature.md` / `bugfix.md` / `refactor.md` / `dependency-upgrade.md` / `docs.md` / `harness.md`) を GitHub 標準の `--template` 指定で運用 (§4.8)**: PR 種別ごとの check list と Test plan を分離、レビュー観点の drift を予防
- **Konsist + markdownlint-cli2 + Gradle カスタムタスク (commonmark + SnakeYAML) の三系統分離による Markdown 検証スタック (§5.2)**: Kotlin file は Konsist、Markdown は markdownlint-cli2、frontmatter / 5 行 summary / 識別子参照は Gradle カスタムタスクと役割分担明確
- **29 commits で議論履歴を 1 PR 内に集約**: rebase squash でなく順次 commit を積み上げた結果、設計議論の経緯が PR diff から読み取れる。再生成可能性 (なぜこの決定に至ったか) の追跡資料として機能

## ⚠️ Problem (詰まったこと / 制約)

- **harness 規約自体が未稼働の状態で計画書を起票する self-bootstrap 構造**: 本 PR 時点で `.claude/skills/` / `.claude/rules/` 配下のファイルは未配置、機械検証も未稼働。計画書だけが先行する状態で「機械的整合性チェックが効かない」latent contradiction を許容する設計判断 (B0 で初配置、A6 で機械検証本格化)
- **29 commits の squash merge で fix loop / housekeeping / 設計議論の区別が後続レビューで困難**: 1 PR 内に「設計提案 → レビュー対応 → 数値補正 → 用語統一」が混在、merge 後の commit history では区別不能
- **plan.md 1753 行の規模**: 1 ファイルに集約した結果、AI lazy-load 戦略 (R-32 / 冒頭 5 行 summary) が plan.md 自身には適用困難。後続 PR (`docs/README.md` / `docs/glossary.md` / `docs/codebase-map.md` 起票) で索引化が必要
- **三層指標の本格化が A7 (中盤フェーズ) のため、A1〜A6 期間中の retrospective で指標が全て N/A になる**: 計画当初から想定されていたが、retrospective のフォーマット設計 (N/A 明示) が plan.md 起票時点で未確定
- **Skill 16 種のうち本 PR 時点では 0 種が実体配置**: B0 で skeleton / A3 で本格化の段階的設計だが、計画書だけ先行して 「ある前提で書かれた他の節」が増殖するリスク (例: §5.4 6 段階フローが各 Skill の存在前提)
- **R-1〜R-37 のリスク列挙が網羅性で勝負する形だが、各リスクの「予防策」「検出機構」「発火時対応」が plan.md 段階では薄い**: A1〜A6 の本格化 rule で順次補強が必要
- **harness 設計と既存実装 (Compose Multiplatform / Decompose 撤去 / Firebase 廃止) の同時進行**: 設計書 (本 PR) と実装 PR が時系列で交錯、A1 ADR 起票時点で「決定 vs 実装」の SoT が ADR 側に統一されるまで latent contradiction が残存

## 🚀 Try (次回からの改善案)

- **B0 (`.claude/{settings.json, mcp.json, skills/, rules/}` + `docs/` skeleton + `.github/PULL_REQUEST_TEMPLATE/` + `scripts/install-git-hooks.sh`) を PR #117 で実装**: plan.md §6.1 の B0 完了条件に従い、skeleton 配置で 16 Skill + 27 ADR の入れ物を準備、本格化は後続フェーズで段階的に
- **A1 で ADR-0001〜0027 を 1 PR で一括起草**: plan.md §4.0.3 の ADR 一覧と一対一対応する 27 ファイルを起票、相互参照 ~90 件で broken link 0 件を達成、A2-1 で `.claude/rules/` 本格化と並行
- **A2-1〜A2-3 で `.claude/rules/` を段階的に本格化**: A2-1 で paths / 構造 / 安全網系 rule、A2-2 で実装系 rule、A2-3 でプロセス / ハーネス系 rule の 3 段階分割、A2-4〜A2-6 で `docs/` / EPIC-A2 補強と並走
- **A3 で `.claude/skills/` 16 種を Group 1-3 で並列実装**: feature-request / bug-fix / refactor / adr-author (Group 1) → harness-meta / dependency-upgrade / harness-evolution (Group 2) → implementation-workflow / code-reviewer / pr-retrospective / pr-poller / roadmap-tracker / ui-snapshot / harness-bootstrap archived (Group 3 wave1+wave2) の 3 グループ並列で完走
- **計画書本体 (plan.md) の見直しを A6 (機械検証本格化) と A7 (三層指標本格化) で実施**: plan.md §10 「計画自体の見直し」を年次レビューでなく、各フェーズ完了時の retro Try で部分修正、`last_updated` を更新する運用に
- **`docs/README.md` / `docs/glossary.md` / `docs/codebase-map.md` / `docs/traceability.md` を B0 で起票**: plan.md 1753 行への lazy-load 入り口として 5 行 summary + 推奨読み順 + 主要パス → 責務マップを別ファイル化 (A2-4 / A2-5 で本格化)
- **plan.md §5.4 の 6 段階フロー (Spec Gen → Implementation → Evaluation → Merge → Retrospection → Meta) を `docs/harness/plan.md` Single Source of Truth、各 Skill SKILL.md は本フローの責務分離を引用する片方向リンク**: SSoT 循環参照を予防 (A1 / A2 で実装時に整理)
- **本 PR の 29 commits を squash merge する代わりに、A3 以降は 1 PR = 単一目的 + commit logical separator で再現性を高める**: 後続 PR (#125 以降) で「6-20 ファイル: 1 PR + commit 分離」境界を明示化

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| Line coverage | N/A | N/A | N/A | Kover (A7 完了後)、本 PR は実装コード変更ゼロ |
| Branch coverage | N/A | N/A | N/A | 同上 |
| Spec coverage | N/A | N/A | N/A | Konsist `@Spec` (A7 完了後) |
| Mutation score | N/A | N/A | N/A | PITest (A7 完了後) |
| Lint 違反数 | N/A | N/A | N/A | Spotless / ktlint / detekt / markdownlint-cli2 (A6 完了後) |
| **PR 固有指標** | | | | |
| 変更ファイル数 | 0 | 1 | +1 | `docs/harness/plan.md` 新規 |
| 追加行数 | 0 | 1753 | +1753 | plan.md (ADR 27 + Skill 16 + フェーズ B0/A1-A10/C1-C10 + R-1〜R-37 + 命名規約 + コミット規約 + Markdown 検証スタック + MCP 採用方針) |
| 削除行数 | 0 | 0 | ±0 | 新規 PR |
| commits | 0 | 29 | +29 | 議論履歴を 1 PR に集約 |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造 -->

- [ ] `[rule]` plan.md §5.4 の 6 段階フロー (Spec Gen → Implementation → Evaluation → Merge → Retrospection → Meta) を `.claude/rules/` 配下 rule から **片方向リンクで引用** する規約を明示。本 PR 時点では plan.md が SoT、各 rule / Skill SKILL.md が引用側。SSoT 循環参照予防 (採用判定基準 4: 後続 PR #119 / #121 / #125 retro で実例蓄積)
- [ ] `[rule]` plan.md §4 「docs 構造」+ §6 「フェーズ順序」を `docs-structure.md` / `roadmap.md` rule で引用する片方向リンクを明示。plan.md 自身は更新 PR の対象として独立、rule は plan.md を「読みに行く」設計 (採用判定基準 4 該当)
- [ ] `[template]` plan.md 1753 行への lazy-load 入り口として `docs/README.md` 5 行 summary + 推奨読み順 + 主要パス → 責務マップを B0 で起票。AI が常に「最初にどこを読むか」が plan.md 直接でなく `docs/README.md` 経由になる導線設計 (採用判定基準 2: B0 で予定)
- [ ] `[skill]` `roadmap-tracker` Skill (A3-12 で本格化) が plan.md / EPIC 進捗を片方向ミラーで `docs/harness/roadmap.md` / `docs/epics/<id>/roadmap.md` に反映する規約を SoT 化 (R-34 一方向ミラー)。本 PR 時点では未稼働、A3 完了で自動同期 (採用判定基準 2: A3 で予定)
- [ ] `[skill]` `pr-poller` + `pr-retrospective` + `harness-meta` の 3 Skill 連鎖を A3-A4 で本格化。本 PR の retro が本格化前の手動代替実行になっている self-bootstrap 構造を A4 完了後に N=10 以上で再計測 (採用判定基準 2: A4 で予定)
- [ ] `[rule]` plan.md §4.6 のコード禁止原則 (`docs/{requirements,specifications}/**` 本文にコード断片を書かない) を `.claude/rules/docs-structure.md` で機械検証可能な regex として明示。A6 で Gradle カスタムタスクが reject 判定する (採用判定基準 4: A6 で予定)

## 📝 harness-meta フィードバック

<!-- harness-meta が後から追記。提案 → 結果の往復ログを 1 ファイル内で完結 -->

### YYYY-MM-DD <PR ID> で消化 (採用)

| 提案 | <PR ID> での消化内容 |
|---|---|

### YYYY-MM-DD <PR ID> で見送り (後続フェーズへ)

| 提案 | 見送り理由 / 移行先 |
|---|---|

### YYYY-MM-DD <PR ID> で保留 (要再評価)

| 提案 | 保留理由 |
|---|---|

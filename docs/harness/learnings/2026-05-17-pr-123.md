---
id: learning-pr-123
title: PR #123 レトロスペクティブ (A2-4 docs/ コア + runbooks 拡充)
type: learning
status: draft
related_pr: 123
related_plan: —
related_epic: EPIC-A2-rules-docs-extension
generated_at: 2026-05-17T11:30:00Z
generator: pr-retrospective (skeleton、本ペインで手動代替実行)
---

# PR #123 レトロスペクティブ

> 生成: pr-retrospective Skill (skeleton 段階、A3 本格化前のため本ペインで手動代替) at 2026-05-17T11:30:00Z
> 関連 Plan: なし (EPIC-A2 直下、1 PR 完結のため Plan 不要) / 関連 Epic: EPIC-A2-rules-docs-extension
> 対象 PR: [docs(harness): A2-4 docs/ コア + runbooks 拡充 #123](https://github.com/subroh0508/colormaster/pull/123) (squash merged at 2026-05-17T10:41:47Z、commit `376018d`、from head `b395276`)
> 差分: 14 ファイル / +838 / -222 (1060 diff 行)、commits: 初回 4 件 (`57a6878` / `80e5d62` / `64f1c1d` / `2ca536a`) + fix loop `b395276` + rebase `6255389` → squash 後 `376018d`
> 関連 mirror PR: [#127](https://github.com/subroh0508/colormaster/pull/127) (merge commit `605fca7`、Merge pull request 形式、orchestrator out-of-band approval で R-15 代替)

## ✅ Keep (継続したいこと)

- **leaf → root の 9 工程直列着手順で 13 docs を本格化**: ①glossary.md → ②codebase-map.md → ③runbooks/mcp-setup.md → ④runbooks/local-development.md → ⑤runbooks/testing.md → ⑥runbooks/i18n.md → ⑦security/README.md → ⑧templates 5 件 → ⑨docs/README.md 索引更新 → EPIC-A2 progress.md 追記。各 docs が参照する側 (glossary / codebase-map / runbooks / security) を先に確定してから README の索引を最後に更新する方針で、「README が暫定リンクを指している間に他 docs が動く」事故を回避。
- **`code-reviewer` Skill (skeleton) を 4 aspect (spec-conformance / architecture / security / code-quality) で独立 system prompt 並列実行**: docs-only PR のため test-quality / performance / visual-regression / design-tokens は適用外とした選択も含めて Coordinator として正解。Generator (`implementation-workflow`) とは独立に Evaluator として動作 (R-13 / ADR 0019 準拠)。
- **Critical = 0 で初回 Pass を達成し、fix loop は Improvement #6 / #7 の即時消化のみで完了**: 4 aspect が独立に「Pass」を返したため Coordinator 集約コメントも Ready for review 即時昇格扱いで動作。binary checklist (4 aspect × 10 項目 = 40 項目) のうち 39 項目 ✅ + 1 項目 [~] (混在許容範囲)。
- **JDK 17 / Kotlin 2.1.21 / AGP 8.9.0 / Compose 1.8.0-alpha04 / Ktor 3.1.3 / SQLDelight 2.1.0 / kotest 6.0.0.M1 / Corretto 22 をすべて `gradle/libs.versions.toml` / `plugins/` / `Dockerfile` の SoT から逆算して `runbooks/local-development.md` に列挙**: docs と実 build 設定の乖離をゼロに保つアプローチを確立、architecture aspect agent が「バージョン情報が実 SoT と完全一致」と評価。後続 PR でも同パターン (SoT 起点逆算) で本文化可能。
- **Improvement #6 / #7 を即時 fix、#8 を「plan.md §4.6.3-4.6.5 が `Open Questions` を canonical 名称として規定」のため A2-3 持ち越しに分類**: docs 横断の規約整合性 (plan.md SoT / template-language.md / Epic / roadmap で `Open Questions` 使用) を尊重し、本 PR の 3 テンプレだけ翻訳して乖離を生む選択を回避。コミットメッセージ・mirror PR description に持ち越し判断ロジックを明文化。
- **EPIC-A2 progress.md の競合解消で append-only 原則を厳守**: PR #122 の A2-1 Phase 0-9 詳細 8 行を保持しつつ、私の A2-4 着手 1 行を **末尾追記** する形で解決。PR 番号 (`#121` / `#122` / `#123`) を明示参照し、後続レビュアーが時系列を追える形に。
- **mirror PR (#127) self-merge を `--merge` (merge commit 形式) で実行**: orchestrator out-of-band approval を R-15 代替として `gh pr merge 127 --merge` 実行、commit `605fca7` で完走。`harness-roadmap-mirror-a2-4` worktree + branch も自動 cleanup。
- **mirror PR 起票後に他 mirror PR (A2-2 #130 + A2-5 #128) が先行 merge した場合の再 rebase + 競合統合解決パターンを確立**: 3 ファイル (`docs/harness/roadmap.md` / EPIC-A2 `roadmap.md` / `progress.md`) で **完了根拠 merge 順序を chronological に再配置 (A2-1 → A2-4 → A2-5 → A2-2)**、append-only セクションでは A2-4 行を A2-5 行の前に挿入する形で再構築。
- **`docs/runbooks/**` 4 件全て 5KB+ に到達** (`local-development.md` 6813B / `testing.md` 6341B / `i18n.md` 5239B / `mcp-setup.md` 6947B): EPIC-A2 受け入れ基準を満たし、spec-conformance aspect agent が `wc -c` で検証済み。「現時点で実行可能なコマンド」と「A7 / A10 / C4 / C5 / C7 で追加する手順」を二分する記述パターンで Phase 進捗に応じた段階的本格化を維持。
- **Phase C 持ち越し TODO 表を全 docs に固定**: glossary.md / codebase-map.md / runbooks 4 件 / security/README.md それぞれ末尾に `| 持ち越し項目 | 持ち越し先 | 理由 |` 表で次のフェーズ責任を明示、`pr-poller` / `harness-meta` Skill が将来的に未消化 TODO を集計できる構造。

## ⚠️ Problem (詰まったこと / 制約)

- **`#8 テンプレ §11 `Open Questions` 翻訳判断**: code-quality aspect agent が「ADR 0027 / template-language.md 違反の英語見出し」と指摘したが、`docs/harness/plan.md` §4.6.3-4.6.5 が **canonical section 名として `Open Questions` を規定** していることに気付いて 1 度試行錯誤 (revert 含む)。`.claude/rules/template-language.md` の「固定セクション名は英語許容」例外追加 or plan.md 含む全 docs 一括翻訳の判断は A2-3 持ち越し。レビュー判断時に SoT 階層 (plan.md > ADR > rules > docs) を再認識した。
- **mirror PR self-merge 試行で 2 回 master rebase が必要**: 1 回目は PR #122 / #124 が merge 済み (本体 PR push 直前)、2 回目は PR #128 (A2-5 mirror) + PR #130 (A2-2 mirror) が mirror PR 起票後・merge 直前に先行 merge。並走 5 PR の mirror 系が複雑に交差する状況で `--force-with-lease` push + 競合統合の所要時間が大きい (合計 ~15 分)。
- **`code-reviewer` Skill が A3 本格化未了で手動 Coordinator が必要**: 本 PR の 4 aspect 並列起動 / 集約 / PR コメント post / Merge readiness 判定をすべて手動 (`Agent` ツール × 4 + `gh pr comment` 1 + 自己判定) で実行、A3 / A4 で `code-reviewer` + `pr-poller` + `pr-retrospective` が連動する自動化が必要。
- **squash merge 後の local branch 削除に `-D` (force) が必要**: PR #123 は `gh pr merge --squash` で merge したため local の `feature/A2-4-docs-core` ブランチは git 視点で「unmerged」扱いになり `git branch -d` が拒否、`gh pr view --json state=MERGED` で確認後 `-D` で force delete。`.claude/rules/implementation-workflow.md` Phase 9 で squash / merge / rebase merge ごとの cleanup 手順を分岐させる必要あり。
- **PR #127 (mirror) の初回 `--merge` 試行が `MERGEABLE: false` で失敗**: master が PR #125 (A2-2 本体) + PR #130 (A2-2 mirror) で進んでおり、3 ファイル競合。fetch を Phase 6 (push) → Phase 7 (merge) の間に挟まないと検出できない。Phase 6 で `gh pr view --json mergeable` を確認する規約を `.claude/rules/implementation-workflow.md` に追加すべき。
- **rebase 競合解消時の `<<<<<<<` マーカー位置から「append-only」「chronological insert」のいずれを選ぶか判断ロジックが暗黙的**: 完了根拠表は chronological insert (A2-4 を A2-1 と A2-5 の間)、着手順変更履歴は append-only (A2-4 行を A2-5 の前に挿入)、本文段落は append-only と、ファイル / セクションごとに方針が異なる。`.claude/rules/roadmap.md` に「セクション別の競合解消ポリシー」を明文化する余地あり。
- **`docs/specifications/README.md` の SPEC suffix 形式が 2 種類混在**: `docs/README.md:116` 命名規約表は `SPEC-IDOL-001-3` (連番)、`docs/specifications/README.md` のペア参照例は `SPEC-IDOL-001-basic` / `SPEC-IDOL-001-detail` (basic/detail) と表記差。architecture aspect agent 指摘 Improvement #2、後続 PR で「basic/detail suffix は省略形」注記追加が必要。
- **三層指標 (Line/Branch coverage / Spec coverage / Mutation score) が全て N/A**: 本 PR は docs のみで Kotlin code 変更ゼロ、A7 で Kover / Konsist `@Spec` / PITest 導入まで指標差分計測は不能。
- **commit subject 72 字超過警告 1 件**: `docs(harness): A2-4 runbooks 4 件本格化 (mcp-setup / local-development / testing / i18n)` が 84 字 (commit `80e5d62`)、`.claude/rules/commit-message.md` で 72 字推奨 / 100 字 hard limit としているが警告のみで通過。subject だけ短くして body にスコープ詳細を書く規律を A2-3 で本格化時に再評価。
- **9 工程直列の実装は worktree 並走では効率的だが、code-reviewer まで含む全体時間は約 90 分**: leaf docs (glossary / codebase-map) が最初に確定する分、後続 docs の参照整合性が読みやすい反面、独立した docs (security / templates) は並列化可能だった。`implementation-workflow` Phase 1 で「touch ファイル重複ゼロのサブグループを Agent 並列起動」する選択肢を A3 で検討。

## 🚀 Try (次回からの改善案)

- **`.claude/rules/template-language.md` (A2-3 本格化) に「固定セクション名は英語許容」例外を明文化**: `Open Questions` / `Gotchas` / `Keep` / `Problem` / `Try` 等、harness 内で安定して使われる section 名は英訳しない方針を例外リストに追加。本 PR の #8 持ち越し判断を将来も再現可能にする。
- **`.claude/rules/implementation-workflow.md` (A2-3 本格化) の Phase 6-7 間に「master fetch + mergeable 確認」を追加**: 本 PR の mirror PR #127 で発生した 2 回 rebase の片方 (push 後 merge 前の rebase 漏れ) を再発防止。`gh pr view --json mergeable,mergeStateStatus` の戻り値で分岐。
- **`.claude/rules/implementation-workflow.md` Phase 9 で merge 方式 (squash / merge / rebase) ごとに branch cleanup コマンドを分岐**: 本 PR は `--squash` で `git branch -d` 不可、mirror PR は `--merge` で同様に `-d` 不可だった (merge commit の親が異なるため)。`gh pr view --json state=MERGED` 確認後 `-D` を使うパターンを規約化。
- **`.claude/rules/roadmap.md` (A2-2 で本格化済) に「セクション別の競合解消ポリシー」追記を A2-3 で実施**: (a) 完了根拠表 = chronological insert (merge 日順)、(b) 着手順変更履歴 = append-only (新行を末尾に追加)、(c) 本文段落 = append-only (新段落を最後に追加)、(d) 次の推奨着手 = full replacement (最新状態に書き換え)、の 4 種類を明文化。
- **`pr-retrospective` を `pr-poller` から自動 trigger する MVP を A3 / A4 で実装**: 本ファイル含む PR #117 / #119 / #121 / #123 / #125 / #126 / #127 / #128 / #130 の learning が全て手動代替で生成されている現状を R-12 (learning ロスト対策) の最優先課題として認識。`gh pr list --state merged --search "merged:>2026-05-17"` ベースで未起票 PR を検出して `harness/learnings-batch-2026-W20` ブランチへ自動追記。
- **`docs/specifications/README.md` / `docs/README.md` 命名規約表に「basic/detail suffix」を明示**: architecture aspect Improvement #2 を後続 PR (A2-3 で `docs-structure.md` 本格化と同時) で消化、`SPEC-<entity-id>-<seq>(-basic|-detail)?` の形式注記を追加。
- **`code-reviewer` Skill (A3 本格化) で docs-only PR 時の aspect 選択ロジックを実装**: 本 PR では 8 aspect のうち 4 つだけ起動した判断 (test-quality / performance は Kotlin 変更ゼロのため適用外、visual-regression / design-tokens は A10 完了前)。`.claude/rules/code-reviewer-aspects.md` (A2-3) に「変更ファイルパターン → aspect サブセット」の対応表を追加。
- **`Co-Authored-By` の `@noreply.anthropic.com` ドメイン許可を `commit-msg` hook で機械強制**: 本 PR の 5 commits 全てで AI 帰属を正しく記録できているが目視確認に依存、A3 / A6 で hook 拡張。
- **mirror PR が他 mirror PR と並走するときの起票順序ガイドライン**: 本 PR では A2-4 mirror PR #127 起票後に A2-5 / A2-2 mirror が先行 merge し再 rebase が必要だった。mirror PR は本体 merge 直後 (他 mirror が割り込む前) に起票 → review → merge のターンアラウンドを短く保つことで rebase 回数を最小化できる。`.claude/rules/roadmap.md` に「mirror PR は本体 merge から N 分以内に起票し merge を完了する」目安を追加。
- **`implementation-workflow` Phase 1 で並列化可能サブグループを自動検出**: 本 PR の 9 工程のうち、leaf docs (glossary / codebase-map) は直列、runbooks 4 件は並列、templates 5 件も並列、README は最終固定。touch ファイル分離が成立する箇所で `Agent` 並列起動する戦略を A3 で `implementation-workflow` 本格化時に検討。

## 📊 指標

A2-4 PR は実装コード差分ゼロ (docs のみ)、三層テスト品質基盤 (Kover / Konsist Spec / PITest) は A7 で導入予定のため Before/After 差分は計測不能 (N/A)。

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| Line coverage | N/A | N/A | — | Kover 未導入 (A7 で導入予定) |
| Branch coverage | N/A | N/A | — | 同上 |
| Spec coverage | N/A | N/A | — | Konsist `@Spec` 未導入 (A7 で導入予定) |
| Mutation score | N/A | N/A | — | PITest 未導入 (A7 で導入予定) |
| Lint 違反数 | N/A | N/A | — | Spotless / ktlint / detekt / markdownlint-cli2 未導入 (A6 で導入予定) |
| ファイル数 (差分) | (A2-4 前) | 14 changed | 14 changed | 全て docs/ 配下 (`README` / `glossary` / `codebase-map` / `security/README` / `requirements/{README,template}` / `specifications/{README,basic/template,detail/template}` / `runbooks/{local-development,testing,i18n,mcp-setup}` / `epics/EPIC-A2/progress`) |
| 行数 (差分) | (A2-4 前) | +838 / -222 (squash merge 376018d) | net +616 | 全て docs |
| commits 数 (squash 前) | (A2-4 前) | 5 (`57a6878` glossary+codebase-map / `80e5d62` runbooks 4 件 / `64f1c1d` security+templates / `2ca536a` README+progress / `b395276` Improvement #6/#7 fix loop) → rebase で `6255389` 1 件統合 | +5 → squash 1 件 (376018d) | 4 コミットを論理単位で分離して code-reviewer aspect 並列の入力分割と整合 |
| code-reviewer aspect 実行 (subagent 並列) | — | 4/4 pass (spec-conformance / architecture / security / code-quality) **初回 Pass** | — | 残 4 aspect (test-quality docs 適用外 / performance Kotlin 変更ゼロ / visual-regression A10 後 / design-tokens A10 後) は skip 妥当 |
| Critical findings | — | 0 | 0 | 初回から Critical 0、fix loop は Improvement 即時消化のみ |
| Improvement findings | — | 8 (本 PR で 2 消化、残 6 件は A2-3 / 後続 PR へ) | — | spec-conformance 1 / architecture 2 / security 2 / code-quality 3 |
| 本 PR 内消化 Improvement 数 | 0 | 2 (#6 Gotchas → 注意事項、#7 持ち越し先フェーズ → 持ち越し先) | +2 | commit `b395276` |
| A2-3 持ち越し Improvement 数 | 0 | 1 (#8 テンプレ §11 `Open Questions`) | +1 | plan.md SoT との整合が必要なため A2-3 でまとめて扱う |
| 後続 PR 持ち越し Improvement 数 | 0 | 5 (#1〜#5: related_plan 単数形スカラー / SPEC suffix 形式 / Node.js LTS phase / Secrets scope 限定 / Cloudflare OAuth scope 具体例) | +5 | A2-3 + A2-2 (一部既に解消) + 後続 runbook 本格化で順次 |
| binary checklist 通過率 | — | 39 / 40 ✅ + 1 [~] | — | 4 aspect × 10 項目、code-quality #8 のみ混在許容判定 |
| 5 行 summary 遵守 | — | 14 / 14 ✅ | — | 全 docs 冒頭 5 行以内、spec-conformance aspect 検証済 |
| frontmatter block 形式 | — | 14 / 14 ✅ | — | flow `[A, B]` 形式の混入なし |
| runbooks 5KB+ 達成 | 0 / 4 | 4 / 4 ✅ | +4 | local-development 6813B / testing 6341B / i18n 5239B / mcp-setup 6947B |
| docs 本格化数 (`skeleton (B0)` → `living`) | — | 13 (README / glossary / codebase-map / security/README / requirements/README / specifications/README + basic + detail / runbooks 4 件) | +13 | テンプレ 3 件は `proposed` status を維持 (起票時 status のため) |
| rebase 実施回数 | — | 2 (本体 PR #123 で 1 回 PR #122/#124 取り込み + mirror PR #127 で 1 回 PR #128/#130 取り込み) | +2 | 並走 5 PR の mirror が交差する複雑性 |
| Phase 完走 | — | Phase 1〜9 全て自走完走 | — | Phase 0 (本 PR) + Phase 2-9、mirror PR (#127) 含めて self-merge まで完走 |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造 -->

- [ ] `[rule]` `.claude/rules/template-language.md` (A2-3 本格化) に「固定セクション名 (`Open Questions` / `Gotchas` / `Keep` / `Problem` / `Try` 等) は英語許容」例外を明文化、本 PR Improvement #8 の持ち越し判断を将来再現可能にする
- [ ] `[rule]` `.claude/rules/implementation-workflow.md` (A2-3 本格化) の Phase 6-7 間に「master fetch + `gh pr view --json mergeable,mergeStateStatus` 確認」を追加、mirror PR self-merge 時の `MERGEABLE: false` 失敗を事前検出
- [ ] `[rule]` `.claude/rules/implementation-workflow.md` Phase 9 で merge 方式 (`--squash` / `--merge` / `--rebase`) ごとに branch cleanup コマンド (`-d` / `-D`) を分岐、`gh pr view --json state=MERGED` 確認パターンを規約化
- [ ] `[rule]` `.claude/rules/roadmap.md` に「セクション別の競合解消ポリシー」追記: (a) 完了根拠表 = chronological insert、(b) 着手順変更履歴 = append-only、(c) 本文段落 = append-only、(d) 次の推奨着手 = full replacement
- [ ] `[skill]` `pr-retrospective` を `pr-poller` から自動 trigger する MVP を A3 / A4 で実装、本ファイル含む 9 件の learning 手動代替を解消 (R-12 対策)
- [ ] `[rule]` `.claude/rules/docs-structure.md` 命名規約表に「`SPEC-<entity-id>-<seq>(-basic|-detail)?` の suffix オプション」を明示、architecture aspect Improvement #2 を A2-3 で消化
- [ ] `[skill]` `code-reviewer` Skill (A3 本格化) で「変更ファイルパターン → aspect サブセット」対応表を `.claude/rules/code-reviewer-aspects.md` (A2-3) に追加、docs-only / Kotlin-only / Skill SKILL.md-only 等の選択ロジックを機械化
- [ ] `[rule]` `.claude/rules/roadmap.md` に「mirror PR は本体 merge から N 分以内に起票 → merge を完了させて rebase 回数を最小化」目安を追加
- [ ] `[skill]` `implementation-workflow` Phase 1 で並列化可能サブグループ (touch ファイル分離が成立する docs / runbooks / templates) を自動検出して Agent 並列起動する戦略を A3 で検討
- [ ] `[skill]` `commit-msg` hook で `Co-Authored-By` の `@noreply.anthropic.com` ドメイン許可を機械強制、A3 / A6 で hook 拡張
- [ ] `[template]` PR description テンプレ `harness.md` の「変更内容」表に「持ち越し Improvement」列を追加し、code-reviewer 集約コメントの Improvement 番号と本 PR 内消化 / 後続持ち越しの紐付けを明示化
- [ ] `[rule]` `docs/runbooks/secrets-rotation.md` (Phase A〜C で本格化) に「漏洩 token のスコープ限定原則 (R-25 連携)」を明文化、security aspect Improvement #1 を消化
- [ ] `[rule]` `docs/runbooks/mcp-setup.md` Cloudflare MCP セクションに「OAuth スコープ最小権限例 (`R2 Storage: Edit` + `Zone: Read` 等)」を追加、security aspect Improvement #2 を後続 runbook 本格化 PR で消化
- [ ] `[rule]` `.claude/rules/docs-structure.md` の必須キー定義で `related_plan` (単数形スカラー、本 PR の 4 docs で使用中) を `related_plans` (複数形 block) に統一する移行手順を A2-3 で扱う、spec-conformance aspect Improvement を消化

## 📝 harness-meta フィードバック

<!-- harness-meta が後から追記。提案 → 結果の往復ログを 1 ファイル内で完結 -->

### 2026-05-17 PR #139 で消化 (採用)

| 提案 | 本 PR での消化内容 |
|---|---|
| `[rule]` template-language.md 固定セクション名英語許容 | `.claude/rules/template-language.md` §例外 に「固定セクション名 (`Open Questions` / `Gotchas` / `Keep` / `Problem` / `Try` / `Blockers` / `Workarounds`) は英語のまま許容」追加 |
| `[rule]` implementation-workflow Phase 6-7 master fetch + mergeable 確認 | `.claude/rules/implementation-workflow.md` §Phase 6 直後の二段 fetch + mergeable 確認 セクション新規追加 (`gh pr view --json mergeable,mergeStateStatus`) |
| `[rule]` implementation-workflow Phase 9 merge 方式別 cleanup 分岐 | `.claude/rules/implementation-workflow.md` Phase 9 セクション全面改定、squash / merge / rebase ごとの `branch -d` / `-D` 切替手順を明文化 |
| `[rule]` roadmap.md セクション別競合解消ポリシー | `.claude/rules/roadmap.md` §セクション別の競合解消ポリシー 新規追加 (chronological insert / append-only / full replacement の 3 種類を明文化) |
| `[rule]` docs-structure SPEC suffix 形式 (`-basic\|-detail`) | `.claude/rules/docs-structure.md` 命名規約表に連番形式 + basic/detail suffix 形式の使い分け明示 |
| `[skill]` code-reviewer aspect 動的選択ルール (touch ファイル × aspect サブセット) | `.claude/rules/code-reviewer-aspects.md` §aspect 動的選択ルール 新規追加、`.claude/skills/code-reviewer/SKILL.md` Gotchas にも反映 |
| `[rule]` roadmap.md mirror PR は本体 merge から 30 分以内に起票 SLA | `.claude/rules/roadmap.md` §mirror PR 起票 SLA 新規追加 |
| `[template]` harness.md「持ち越し Improvement」列追加 | `.github/PULL_REQUEST_TEMPLATE/harness.md` 「変更内容」表に「持ち越し Improvement」列追加 |
| `[rule]` docs-structure `related_plan` → `related_plans` 統一移行 | `.claude/rules/docs-structure.md` §related_plan の単複表記 新規追加、移行手順を明文化 |

### 2026-05-17 本 PR で見送り (後続フェーズへ)

| 提案 | 見送り理由 / 移行先 |
|---|---|
| `[skill]` pr-retrospective を pr-poller から自動 trigger MVP | A3 / A4 Skill 本格化で対応予定 |
| `[skill]` implementation-workflow Phase 1 並列化サブグループ自動検出 | A3 Skill 本格化で対応予定 |
| `[skill]` commit-msg hook 拡張で Co-Authored-By ドメイン許可 | A3 / A6 hook 拡張で対応予定 |
| `[rule]` runbooks/secrets-rotation.md 漏洩 token スコープ限定原則 | 別 PR (runbook 本格化 PR) で対応予定 |
| `[rule]` runbooks/mcp-setup.md Cloudflare OAuth 最小権限例 | 別 PR (runbook 本格化 PR) で対応予定 |

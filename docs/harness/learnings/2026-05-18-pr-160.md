---
id: learning-pr-160
title: PR #160 レトロスペクティブ (Monitor dedup v3 dry-run 起票、escalate verdict)
type: learning
status: draft
related_pr: 160
related_plan: —
related_epic: —
generated_at: 2026-05-19T18:05:00+09:00
generator: pr-retrospective (skeleton、本ペインで手動代替実行)
---

# PR #160 レトロスペクティブ (Monitor dedup v3 dry-run 起票、escalate verdict)

> **5 行以内 summary**: 旧 orchestrator pane (workspace:2) の累計 14 PR 監督セッションで観測した
> Monitor dedup ロジック v1 (md5 hash) → v2 (集約 prev_file + comm -23) → v3 (workspace prefix
> + tail -3) の進化を 4 シナリオ S1-S4 で構造化記録した dry-run 起票 PR。subagent 並列比較が
> cmux + persist file 環境依存で原理的に困難なため verdict は escalate (orchestrator 委任)、
> SoT 反映は subroh0508 adopt 判断後の別 PR に持ち越し。

> 生成: pr-retrospective (skeleton、本ペインで手動代替実行) at 2026-05-19T18:05:00+09:00
> 関連 Plan: — / 関連 Epic: — (orchestrator skill 既存配置への dry-run 起票)
> 対象 PR: [docs(harness): Monitor dedup v3 dry-run 起票 (改修候補 #2、escalate verdict) #160](https://github.com/subroh0508/colormaster/pull/160) (squash merged at 2026-05-18T01:29:13Z、commit `f36efe9`、from head `harness/dry-run-monitor-dedup`)
> 差分: 2 ファイル / +213 / -1 (214 diff 行)、commits: 1 (fix loop ゼロ完走)

## ✅ Keep (継続したいこと)

- **Monitor dedup v1 → v2 → v3 の進化を 4 シナリオ S1-S4 で構造化記録**: v1 (md5 hash 偽陽性連射) / v2 (集約 prev_file + comm -23 だが workspace 混線) / v3 (workspace prefix + tail -3) の差分を観測ベースで分離、subagent 並列比較が困難な場面で「retrospective 観測」を判定根拠として採用 (consensus: spec-conformance + architecture)
- **escalate verdict を SoT 反映前に明示**: subagent 並列比較が cmux + persist file 環境依存で原理的に困難な場合、自動判定でなく orchestrator (subroh0508) 委任で adopt 判断を保留する verdict を採用、後続 PR (harness/orchestrator-monitor-dedup-v3-sot 想定) で SoT 反映の段階分離 (consensus: architecture)
- **dry-run 配置と SoT 反映の責務分離**: 本 PR は dry-run 起票のみ、SoT 反映は別 PR で実施、「dry-run 起票 PR / Skill SoT 反映 PR の責務分離」パターンを実証 (consensus: architecture)
- **`docs/harness/dry-runs/INDEX.md` を status skeleton → living 昇格**: 2 件目の dry-run 行追加でディレクトリの実運用化を実証、PR #142 (dry-run phase 配置) の SoT が本 PR で初の本格利用 (consensus: spec-conformance)
- **harness-meta-criteria.md §dry-run 必須条件 「Skill フロー追加 / 削除」該当判定**: 改修候補 #2 (orchestrator SKILL.md §Phase 3 + §Monitor 思考動詞辞書 への Monitor dedup v3 SoT 追加) が dry-run 必須条件に該当することを起票時に明示、判定プロセスの透明性確保
- **2 ファイル / 1 commit の小規模 PR でフォーカス維持**: dry-run 結果ファイル新規 + INDEX.md 更新のみ、SoT 反映と混在させず scope を絞ることでレビュー負荷低減 + classifier denial ゼロ
- **4 シナリオ S1-S4 で観測内容を構造化**: 単純な「動かなかった事例」記述でなく「シナリオ 1: 単一 workspace」「シナリオ 2: 2 workspace 並列」のように網羅性を担保
- **fix loop ゼロ完走**: code-reviewer 4 aspect (spec / arch / security / code-quality) Critical 0、initial commit 1 件で完走、orchestrator 事前承認テキストで classifier ブロックゼロ self-merge

## ⚠️ Problem (詰まったこと / 制約)

- **subagent 並列比較が cmux + persist file 環境依存で原理的に困難**: dry-run の本格的な subagent 並列実行 + 出力比較 + 自動判定が cmux + persist file の協調動作に依存、A4 harness-meta 本格化前は手動代替で「retrospective 観測」を判定根拠とする escalate verdict が継続
- **escalate verdict 採用判断が subroh0508 (orchestrator) 個人依存**: adopt / reject の判断アルゴリズムが SoT 化されていない、本 PR では「retrospective 観測 4 シナリオ + verdict escalate」までで停止
- **元 learnings が `/tmp/orchestrator-handover-20260518.md` という ephemeral path に置かれている**: 引き継ぎ summary が `/tmp/` 配下のため永続性なし、merge 後の retro 起票で `docs/harness/learnings/` 配下に正規化予定だが本 PR 時点では参照不可
- **改修候補 #2 単独で 1 PR 起票**: PR #159 (採用 3 件集約) と PR #161 (見送り 5 件うち 4 件 #3/#4/#6/#7 集約) と分離、改修候補ごとに PR 粒度が異なる構造、harness-meta-criteria.md §持ち越し先決定 に PR 分割粒度の SoT が未整備
- **`docs/harness/dry-runs/` 配下の dry-run 結果ファイル naming convention が `YYYY-MM-DD-<slug>.md`**: PR 番号と紐づかないため、後続 PR が「どの dry-run がどの PR で消化されたか」を辿るには INDEX.md 参照が必須、機械検証なし
- **dry-run 結果ファイルの判定基準が 4 シナリオの「観測有無」のみ**: 数値的な判定基準 (改善度 / 再現性 / 副作用 の 3 軸定量評価) が本 PR 時点では未導入、PR #171 (PLAN-002 / ADR-0028) の 3 軸定量評価フレーム導入で後付け正規化
- **adopt 判断後の別 PR ブランチ名 `harness/orchestrator-monitor-dedup-v3-sot` が事前命名で起票見送りリスク**: PR description に記載した想定ブランチ名が実際に使われるか不確実、escalate verdict が長期保留化する場合に dangling reference 化

## 🚀 Try (次回からの改善案)

- **subagent 並列比較が困難な dry-run の判定基準テンプレを harness-meta-criteria.md §dry-run 必須条件 に追加**: 「retrospective 観測 N シナリオ + verdict escalate」を adopt 判定基準として SoT 化、本 PR をユースケース引用
- **escalate verdict の adopt 判断アルゴリズムを SoT 化**: 「観測 N シナリオ全件で v3 優位 → adopt」「観測 N/2 シナリオ以下で v3 優位 → reject」のような閾値ベース判定を `.claude/rules/harness-meta-criteria.md` に追加、subroh0508 個人依存を緩和
- **引き継ぎ summary を `/tmp/` でなく `docs/harness/handovers/<date>-<workspace-id>.md` に永続化**: ephemeral path 依存を予防、merge 後の retro 起票時に dangling reference を回避
- **改修候補ごとの PR 分割粒度を `.claude/rules/harness-meta-criteria.md` §PR 分割粒度 に SoT 化**: 「採用集約 PR / 見送り集約 PR / dry-run 起票 PR / SoT 反映 PR」の 4 種の責務分離パターンを規範化、本 PR を「dry-run 起票 PR」のユースケース引用
- **dry-run 結果ファイル naming convention に PR 番号を含める案検討**: `YYYY-MM-DD-pr<N>-<slug>.md` に変更すれば後続 PR の参照経路追跡が機械検証可能、現状は INDEX.md 経由のみ
- **3 軸定量評価 (改善度 / 再現性 / 副作用) を本 PR の dry-run 結果ファイルに後付けレトロフィット**: PR #171 / ADR-0028 で導入されたフレームを過去 dry-run 結果に適用、本 PR の verdict escalate を 3 軸スコア化することで判断アルゴリズムの SoT 化に寄与

## 📊 指標

| 指標 | Before | After | Δ | 備考 |
|---|---|---|---|---|
| Line coverage | N/A | N/A | N/A | Kover (A7 完了後)、本 PR は実装コード変更ゼロ |
| Branch coverage | N/A | N/A | N/A | 同上 |
| Spec coverage | N/A | N/A | N/A | Konsist `@Spec` (A7 完了後) |
| Mutation score | N/A | N/A | N/A | PITest (A7 完了後) |
| Lint 違反数 | N/A | N/A | N/A | Spotless / ktlint / detekt / markdownlint-cli2 (A6 完了後) |
| **PR 固有指標** | | | | |
| 変更ファイル数 | 0 | 2 | +2 | dry-run 結果ファイル新規 + INDEX.md 更新 |
| 追加行数 | 0 | 213 | +213 | Monitor dedup v1/v2/v3 差分 + 4 シナリオ S1-S4 + verdict escalate |
| 削除行数 | 0 | 1 | +1 | INDEX.md status skeleton 行 |
| commits | 0 | 1 | +1 | fix loop ゼロ完走 |
| dry-run シナリオ数 | 0 | 4 | +4 | S1-S4 (単一 workspace / 2 workspace 並列 / 3+ workspace 並列 / scroll 履歴 10+ 回再 emit) |
| verdict 採用 / 見送り | N/A | escalate | N/A | adopt 判断は別 PR で subroh0508 委任 |

## 🤖 ハーネス改善提案

<!-- harness-meta が parse する正規構造 -->

- [ ] `[rule]` `.claude/rules/harness-meta-criteria.md` §dry-run 必須条件 に「retrospective 観測 N シナリオ + verdict escalate」を adopt 判定基準として SoT 化。本 PR の judgement プロセスを規範化、採用判定基準 1 (本 PR + 後続 dry-run で反復見込み) 該当
- [ ] `[rule]` escalate verdict の adopt 判断アルゴリズムを `.claude/rules/harness-meta-criteria.md` §escalate 判定 に新規追加。「観測 N シナリオ全件で優位 → adopt / N/2 以下で優位 → reject」の閾値ベース、採用判定基準 4 (Critical 派生 = 個人依存リスク) 該当
- [ ] `[rule]` 引き継ぎ summary を `/tmp/` でなく `docs/harness/handovers/<date>-<workspace-id>.md` に永続化する規約を `.claude/rules/docs-structure.md` に追加。ephemeral path 依存を予防、採用判定基準 4 該当
- [ ] `[rule]` `.claude/rules/harness-meta-criteria.md` §PR 分割粒度 に「採用集約 PR / 見送り集約 PR / dry-run 起票 PR / SoT 反映 PR」の 4 種責務分離を SoT 化。本 PR + PR #159 / #161 を引用、採用判定基準 1 該当
- [ ] `[template]` `docs/harness/dry-runs/template.md` の判定基準セクションに 3 軸定量評価 (改善度 / 再現性 / 副作用) 表を追加 (PR #171 / ADR-0028 由来、本 PR retrofit 対象)、採用判定基準 2 (PR #171 で導入済) 該当
- [ ] `[skill]` `harness-meta` Skill (A4 本格化時) に escalate verdict の adopt 判断自動化を実装。観測 N シナリオの判定をスコア化、subroh0508 個人依存を緩和、採用判定基準 2 (A4 で予定) 該当

## 📝 harness-meta フィードバック

<!-- harness-meta が後から追記。提案 → 結果の往復ログを 1 ファイル内で完結 -->

### YYYY-MM-DD <PR ID> で消化 (採用)

| 提案 | <PR ID> での消化内容 |
|---|---|

### YYYY-MM-DD <PR ID> で見送り (後続フェーズへ)

| 提案 | 見送り理由 / 移行先 |
|---|---|

### YYYY-MM-DD <PR ID> で保留 (要再評価)

| 提案 | 保留理由 |
|---|---|

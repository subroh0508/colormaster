const path = require('path');

const rootPath = path.resolve(__dirname, '../../../../')

const webAppPath = path.resolve(rootPath, 'js/app');
const nodeModulePath = path.resolve(rootPath, 'build/js/node_modules');
